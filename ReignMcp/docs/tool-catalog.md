## Validation ownership (2026-09-12)

Conversation naturalness and accepted item gifts (2026-09-14) use the existing artifact-bound `reign_run_offline_verification` quick/offline `prompt_efficiency` suite. Its `pipeline.prompt_caching` includes captured dialogue-loop excerpts, relevance/persistence and planner-choice contracts. No new tool or runtime operation is added. See `docs/agent/DIALOGUE_NATURALNESS_FIX.md` and the dialogue testing procedure; provider prose and native transfer acceptance remain separate.

Throughput replay summary fields include original/candidate average phase timings and actual evaluation ranges. Both routes deserialize the saved inputs independently; daily hashes cover current-day pair rows and final hashes cover all rows. Sample JSONL files retain partial progress. Retained verification reports preserve their referenced short sandbox directories. A startup exception is a failed CLI run even when an older executable incorrectly exits zero.

Relationship replay uses the existing `reign_run_offline_verification` tool at tier `quick`: `relationship_throughput_contracts`, `relationship_throughput_smoke` (five days), or `relationship_throughput` (100 days and strict throughput acceptance). Supply the exact successful Release validation run and normal isolated-verification confirmation. Replay v3 retains input bytes and original random conception identifiers with SHA-256 hashes, state hashes and timing reports. Its bounded read-only loopback ReignValidation transport probe writes `transport.json` before warm-up, preserving configured TLS and installed settings; probe v2 compares synchronous/asynchronous requests. See the focused proof procedure and `reign.testing.json` for fixture limits and cleanup.

Relationship throughput follow-up: campaign catch-up consumes `/world-test/overview?view=readiness` (`reign_world_readiness_v1`) without the historical overview cache. Results expose `catchUpMs` and overlapping `blockedObservationMs`; eight observations separated by at least two seconds remain mandatory. `ReignMcp/scripts/prepare-reign-database.ps1` defaults to non-mutating Plan; Provision/Backup/Restore/Verify require exact mode confirmation and never perform cutover. See `docs/agent/RELATIONSHIP_THROUGHPUT_IMPLEMENTATION.md` and `reign.testing.json` for the acceptance budget and separate deployment boundary.

`reign_get_validation_status` returns `reign-validation-lane-v1` with available, busy or unavailable status. It reads the OS lease and bounded `reign-validation-lease-v1` attribution/progress: exact task UUID, process ID, profile/configuration, up to 256 changed paths plus total count, times, current operation, completed count and optional run/fingerprint/report. An available snapshot has no owner; last-run metadata is advisory and never establishes success.

Supply the current task UUID as optional `requestingTaskId` on `reign_validate`; it is attribution, not authority. The non-listening CLI accepts `--task-id` with `CODEX_THREAD_ID` fallback; `--validation-status-cli` only reads status. No status route builds, creates/removes a lease, cancels another task, starts a listener, changes approvals or controls the Reign/game lifetime. The OS lease remains authoritative. Only matching lease IDs bind an atomically published progress sidecar to a held lease; legacy/corrupt metadata remains busy with unknown attribution. Failed inspection is unavailable and must not imply permission to start or delete anything.

Coordinate only actual overlaps with active same-project tasks; preserve stopped tasks and existing user scope. See [coordination](../../docs/agent/TASK_COORDINATION.md) and [the indexed testing guide](../../docs/agent/TESTING_TOOL_GUIDE.md). The catalog's `guideDocuments` retains the focused procedures; existing per-feature contract assertions follow that index.

## Court audience artwork (2026-09-09)

Cache identity uses whole-year appearance age; ordinary fractional aging does not trigger another scene. The native portrait maturity guard is preserved: unsupported infant/toddler references fail before a partial-cast request rather than borrowing an adult face. Missing references remain a distinct, observable artwork failure.

`reign-court-audience-art-v1` uses the existing castle-scene route with exactly one composite containing every involved courtier and the approved host-town hall. `CourtAudienceSceneTests` runs the production compositor, fake provider boundary, cache, failure and view-lifecycle seams through manifest-selected `changed` validation. No paid per-person portrait preparation occurs; missing cached portraits use bounded free native appearance captures. Native capture/age/likeness and provider image quality still require Bannerlord acceptance.

Existing `snapshot`, `noble_snapshot` and `court_life_snapshot` expose `audienceArt` with pending/ready/failed/disabled status and, when ready, the `CourtPetitionArt/<snapshotKey>.png.json` receipt. It contains complete participant IDs, host/culture, reference/composite/image hashes and logical input-image/scene-request counts. Logical counts are not billing receipts; retain actual provider receipts separately. Court Life snapshots remain read-only diagnostics while dialogue/art are pending. The fixed hall and textual international `sharedScene` never establish generated-image success. Dialogue/decision controls retain their independent readiness. Existing provider authorization, exact disposable-save and snapshot gates remain unchanged; no new endpoint, command or confirmation phrase. Verify full cast, cross-culture halls, responsive dialogue, cache/reload and close-during-generation after deployment, and keep the roadmap acceptance item open until that evidence exists.

## Control Center navigation (2026-09-09)

Full style extension (2026-09-09, requested after the navigation preview): all Control Center panels, headings, controls, tables, logs, character/standing cards and dynamic relationship matrices now use the canonical `palette.json` tokens. Legacy brown/bright-gold gradients and glow are removed at their original rules. The existing browser contract additionally audits computed foreground/background/border colors on every page, production-populated character and world renderers, focused/disabled inputs, hover/active buttons, checkboxes and file selectors. Brand and portrait pixels, page layouts, form values and backend behavior remain unchanged. `control_center_modern_style_contract` extends the existing architecture suite.

`reign-control-center-navigation-ui-v1` groups the ten everyday pages plus Diagnostics in the main navigation. Diagnostics exposes Overview (the original Diagnostics page), Logging, Log Viewer, Audit Viewer, NPC Dialogue Audit, World History, NPC Dialogue Lab, Relationship Director, World Test, Test Lab, Live Test Bridge and Verification Lab. Returning to Diagnostics restores the last opened child; switching pages preserves unsaved inputs. Existing loaders and backend operations retain their behavior.

Run the existing artifact-bound `interaction_architecture` offline suite. Its `control_center_navigation_preview` result names the exact emitted HTML fixture directory. Then run `ReignBetaServer/src/Modules/Platform/control-center-ui-contract.mjs` with that directory and an evidence output directory, using the existing Node/Playwright installation (`REIGN_PLAYWRIGHT_MODULE`; Edge is the default). The 66-case matrix checks every page at 1672, 1024 and 600 pixels, loader dispatch, keyboard access, selected states, remembered child, preserved drafts, navigation bounds and frozen palette tokens. Inspect the screenshots and `ui-report.json`. MCP has no web-preview command; this shared local fixture harness is the documented fallback.

All browser traffic is intercepted and page loaders are observed at their dispatch boundary. No provider, campaign, listener, save or live Control Center is contacted. This proves web navigation, not backend tool execution. No native prefab, portrait, approved-reference raster or Bannerlord adapter is changed, so native preview/fidelity and game acceptance are not applicable to this navigation edit. Install only validated artifacts through the visible unified lifecycle if deployment is requested.

Legacy audit: removed 202 lines of unbound director-sample and retired relationship-simulator JavaScript plus its unused chart/table/progress styles. The simulator controls were already absent and its four server routes explicitly return retired/no-state-change responses. Keep those compatibility responses. Relationship Director still observes current MBTI/lifecycle/political-marriage state; its Ambient Sample uses the current snapshot route. Test Lab still provides editable deterministic/provider replay cases and correlation import; Verification Lab orchestrates broader gated checks; World Test remains the passive observatory. Those active tools are not obsolete merely because their names overlap. NPC Dialogue Lab, Live Test Bridge, audits and log viewers remain reachable. No saved test cases, test history, runtime data or campaign content was deleted.

## Diplomatic completion evidence (2026-09-09)

For the asynchronous negative boundary, use `court_life_choose_confirm` with `courtLifeExpectation=pending`. Its `pending_diplomacy_confirmation_has_no_settlement_effects` assertion requires an unfinished real confirmation, a single Executing native action with Success=true/Completed=false, a pending court matter without settlement receipt or native committed flag, unchanged ruler gold, and no matter relation adjustments or history. `usedProductionChooseAndConfirm` records invocation; `productionConfirmationCompleted` and `expectedPendingDiplomaticAction` distinguish completion from the expected wait. Unrelated errors cannot pass this gate.

Government pending receipts identify the responsible kingdom and saved hearing through `governmentBusinessId`, `governmentKingdomId` and `governmentStatus` diagnostics. Only an awaiting-counterpart stage follows the linked counterpart hearing. Normal votes, reconsideration and authorization remain required. The Government snapshot contract checks current authoritative, individual-member/binding-authority wording and authority meaning; its obsolete ruler-may-agree literal is retired. This updates evidence and assertions, not government powers.

International observation adds `nativeState.international.nativeDiplomaticAction`: native service availability, matching action count, exact matterId_native ID, status, type, terms and execution snapshot. Resolved accepted diplomatic proposals must pass `accepted_diplomacy_has_one_completed_native_action` and `accepted_diplomacy_receipt_is_completed`. A successful choose-confirm operation does not prove completion. A `government_hearing_pending` receipt with Success=true and Completed=false is pending work; preserve Government authorization and advance only through the guarded campaign route. Verify no court settlement effects while pending, then exact native completion and effects once authorized. Re-prepare persistence fingerprints after the additional evidence; retain earlier failures and never infer historical completion from a progress receipt. No safety gate or mutation route changes.

## Binding an existing court encounter (2026-09-09)

Use guarded `court_life_preflight` with the desired `courtLifeSource` and optional `courtLifeTemplateId`. Its read-only `pendingMatterInventory` reports exact IDs, titles, states, completed turn counts and existing fixture bindings; at most 100 sorted matches are returned with totalCount and truncated. Narrow the template filter if truncated. Inventory membership does not waive binding eligibility.

Pass `courtLifeExistingMatterId` to `court_life_prepare` for International or Patronage. This exact selection never falls back to generation or refreshes world/history for a new fixture. It requires the armed run-owned Current, fixture confirmation, matching campaign/timeline/reign/capital and source, plus matching template when supplied. International scope checks the payload's player kingdom and ruler separately from the matter's foreign KingdomId. Committed, missing, ambiguous, already-bound or in-flight matters are rejected, as are fixture gold/loyalty/captive/age changes and participant selection. No baseline is mutated and no decision, payment, time advance or save is injected.

Successful binding stores only a test marker and the existing fingerprint. `existing_matter_binding_preserves_matter_and_native_state` checks unchanged matter/native fingerprint and matter count. Preserve the returned conversation and continue new turns through existing open/converse/choose-confirm/observe profiles. Original automatic Patronage template binding remains supported. MCP rejection and catalog tests are provider-free; native acceptance separately checks invalid-ID/template/already-bound rejection, marker-only preservation, natural conversation and actual effects on the enrolled disposable campaign.

## Testing an existing patronage encounter (2026-09-08)

## Prompt composition and complete evidence (2026-09-08)

### Dialogue marriage verification (2026-09-09)

The existing `reign_run_offline_verification` quick/offline `contracts` suite includes `contracts.dialogue_marriage`: exact spouse/consent binding, router metadata isolation, production normalization, rejection of missing/conflicting/third-party identities, and truthful pending/failure/native-confirmed dialogue. The existing authority receipt uses `personal_marriage_consent`; native `marriage_completed` includes `alreadyMarried`. No new tool or gate is introduced. Native/provider acceptance remains separate under existing disposable-save and provider gates. See `docs/agent/MARRIAGE_DIALOGUE_FIX.md` and `reign.testing.json`.

Use `reign_get_prompt_evidence` (`GET /audit/prompt`) with an exact correlationId and optional campaignId. Pin the returned `evidenceId` while paging `messageIndex`, `offset`, and `length` (1–12000 characters). Empty evidenceId selects the latest retained provider attempt. Content is redacted before storage/pagination, with exact redacted UTF-8 hashes; original character counts, rule/source/version metadata and omission reasons distinguish text from estimates. The per-campaign ledger retains the existing 8 MiB/4 MiB bounded audit policy. Missing/expired/pre-capture evidence is explicitly unavailable. No returned model reasoning, credentials, provider invocation, campaign mutation or arbitrary filesystem access is exposed.

Layout 9 retires only reviewed exact monolithic template hashes into an inactive byte-preserving archive. Unknown custom content stops activation before any file is moved. Shared rule dependencies render once; A-only/B-only coverage and scoped distinctions remain mandatory. Requests remain self-contained with subscription-safe implicit caching. Native wealth v3 separates cash, clan backing and nullable party cargo; appearance remains independent. Run manifest-selected validation, then the existing isolated `prompt_efficiency` and applicable `contracts`/`encountered_residents` suites. Provider/native acceptance still uses the existing cost/disposable-save gates. Details and migration ownership: [Prompt composition](../../docs/agent/PROMPT_COMPOSITION.md).


For an exact `courtLifeTemplateId`, guarded `court_life_prepare` binds the one existing pending patronage encounter before attempting generation. `reusedExistingMatter=true` and `fixtureChanges.markerOnly=true` distinguish this from creating a fixture. The harness preserves its conversation, quotes, participants, receipts and native state; it records only the test marker and baseline fingerprint. `existing_patronage_binding_preserves_matter_and_native_state` verifies unchanged state and matter count. Continue from the returned conversation rather than replaying earlier turns.

Binding rejects multiple pending matches, an already-bound matter, committed effects, or a mismatch in campaign, timeline, reign, kingdom, source or capital. It cannot be combined with fixture gold, loyalty or captive mutations. Existing confirmation and disposable-save gates remain required. No pending matter is deleted and no production eligibility rule is waived. Empty template selection continues to use normal generation. Read-only `patronageEligibility` diagnostics still create no binding.
## Patronage eligibility diagnostics (2026-09-08)

The guarded `court_life_preflight` profile and failed Patronage `court_life_prepare` responses include `patronageEligibility`. Supply `courtLifeTemplateId` to inspect one exact template; an empty preflight filter returns the bounded catalog. `unmetPrerequisites` distinguishes missing capital/ruler, spouse/child/coming-of-age prerequisites, prior creator work, missing public historical subjects, and `same_template_pending`. Reports include at most five public subjects and five pending matter IDs per template, plus the full pending count. Unknown template IDs return `templateKnown=false`.

These diagnostics are read-only: they do not create or bind a matter, inject history, remove pending matters, charge gold, or waive eligibility. The existing disposable-campaign gates still apply. Inspect the reason before retrying preparation; an existing pending matter is not evidence of missing history. Proclamations establish that the ruler made a public statement, not independent proof of every claim within it.
# Tool catalog

`reign_set_spymaster_agent_activation_fixture` requires an exact armed
`campaignTestRunId`. It derives the run-owned Current save, timeline, prefix,
and protected baseline through the campaign-test service before preparing the
Social Balance enrollment used for native relation and directional-affinity
changes. Caller-supplied save prefixes are not accepted.

## Spymaster destructive acceptance

`reign_start_spymaster_organic_destructive_test` accepts `controlled_assassination_success` and `controlled_capture_breakout_success`. Each profile requires the irreversible disposable-save confirmation and must run on its own restore-isolated guarded campaign-test Current. Controls are non-serialized, one-shot, and bound to the exact UI-submitted production mission GUID; the capture profile submits a governor assassination against a governed foreign settlement so its native settlement party can receive the prisoner, then binds its consequence and later breakout rolls to that same mission. Production marks attribution pending only after actual native captivity. Native death, captivity, escape, concealment, history, and memory effects are not suppressed.

`reign_get_testing_catalog` is the generated discovery entry point. It combines
the versioned `reign.testing.json` contract with the currently callable MCP tool
metadata and versioned LiveTest scenario files, so this document does not need
to duplicate every evolving input parameter.

## Read-only runtime and world tools

| Tool | Purpose |
| --- | --- |
| `reign_get_capabilities` | Report enforced configuration and boundaries |
| `reign_get_status` | Composite server/provider/background/memory/telemetry status |
| `reign_get_prompt_inventory` | UI-safe prompt inventory and cache state |
| `reign_query_logs` | Bounded, redacted operational-log query |
| `reign_query_audit` | Bounded audit-ledger query |
| `reign_list_campaigns` | Real World Test campaign/timeline discovery |
| `reign_get_campaign_storage_audit` | Physical `.sav` ownership, matching campaigns, stale/overwritten registrations, pending retirement, protected imports, orphan roots, and `_shared` storage |
| `reign_cleanup_campaigns_without_saves` | Exact-confirmation reconciliation that deletes campaigns with no physical save, prunes stale registrations, removes verified orphan roots, and preserves import-pending campaigns plus `_shared` |
| `reign_get_world_overview` | World Test health and aggregate state |
| `reign_get_world_details` | Filtered subsystem drill-down, including `political pressures`: pair filters activity orientation; nested `incident` exposes both sides' recorded pressure/rulings or null if absent; existing latest-1000 activity window applies |
| `reign_query_world_history` | Authoritative history query |
| `reign_get_world_event` | One event with entities and knowledge rules |
| `reign_get_world_correlation` | Correlated event sequence |
| `reign_get_action_health` | Action catalog and recent failures |
| `reign_list_characters` | Character summaries only |
| `reign_get_relationship_status` | Compact relationship/native projection state |
| `reign_query_rebellions` | Current rebellion state |

## Read-only testing and workspace tools

| Tool | Purpose |
| --- | --- |
| `reign_get_verification_status` | Observe active Verification Lab state |
| `reign_get_verification_results` | List/load verification evidence |
| `reign_get_live_test_status` | Observe native bridge and run state |
| `reign_get_live_test_report` | Load a completed/current run report |
| `reign_get_release_readiness` | Read readiness and social-balance evidence |
| `reign_get_workspace_status` | Discover components and test assets |
| `reign_get_testing_catalog` | Generated MCP/harness/scenario inventory, gates, guide, and fingerprint |
| `reign_get_campaign_test_status` | Durable enrollment, active save, queue health, and Save Sync capacity |
| `reign_get_campaign_test_report` | Campaign-test ledger, evidence, checkpoints, and current status |
| `reign_search_source` | Literal search in allowlisted source/docs |
| `reign_read_source` | Bounded source/document line window |

## Gated tools

| Tool | Required gate | Effect |
| --- | --- | --- |
| `reign_start_verification` | `REIGN_MCP_ALLOW_VERIFICATION_CONTROL=true` plus confirmation | Writes isolated Verification Lab state; live-LLM/game tiers may consume provider/game resources |
| `reign_cancel_verification` | same gate plus confirmation | Requests cancellation after the active check |
| `reign_build` | `REIGN_MCP_ALLOW_BUILD=true` | Writes isolated build artifacts under `.codex-build` |
| `reign_get_validation_plan` | None | Discovers projects and reports coverage without running commands |
| `reign_validate` | `REIGN_MCP_ALLOW_BUILD=true` | Runs `changed`, `core`, `product`, `tooling`, or ecosystem-release `all` validation and writes a structured report |
| `reign_run_offline_verification` | `REIGN_MCP_ALLOW_OFFLINE_VERIFICATION=true`, a successful Release `reign_validate` run ID, plus confirmation | Runs only quick/offline non-listening verification from that exact immutable validation artifact and returns its report, source-fingerprint, executable-path, and executable-hash provenance |
| `reign_prepare_campaign_test` | No runtime mutation; exact immutable enrollment | Creates durable run ownership and a descriptive disposable-save namespace |
| `reign_start_campaign_test` | verification-control gate plus confirmation | Starts visibly on the enrolled save or attaches to a running campaign |
| `reign_arm_campaign_test` | same gate plus confirmation | Arms the bridge, verifies identity/Save Sync, and creates the initial disposable copy. The baseline-only copy may accept an absent pre-existing daily rollup after exact native observation/confirmation; all current queues and failure counters still fail closed. |
| `reign_advance_campaign_test` | same gate plus confirmation | Advances only an armed disposable campaign, closes only an already-open native MapScreen Escape menu while waiting, records its recovery count, pauses, drains, and optionally checkpoints. A timed-out or cancelled controller terminates its child process, cancels only the exact heartbeat-advertised passive-world run, then attempts an independent no-save drain. |
| `reign_stop_campaign_test` | same gate plus confirmation | Preempts an exact active passive-world run, then pauses and proves queue quiescence without saving; it refuses to cancel unrelated feature modes. Daily clocks must cover the latest fully completed campaign day; a paused fractional current day is not itself complete. |
| `reign_checkpoint_campaign_test` | same gate plus confirmation | Drains first under the same completed-day rule, enforces the 15-state budget, and saves an owned checkpoint |
| `reign_restore_campaign_test_checkpoint` | same gate plus confirmation | Discards only unsaved transient fixture state by visibly reloading an existing exact owned Current checkpoint with `--no-save`; pre-reload identity/Save Sync checks intentionally allow an unsafe encounter/menu location, while the baseline remains refused and a safe settlement is mandatory after reload |
| `reign_cleanup_campaign_test` | same gate plus confirmation | Deletes only exact run-owned, inactive checkpoints; never the baseline |
| `reign_start_capital_ambassador_test` | verification-control gate, armed exact campaign-test Current, plus confirmation | Runs one schema-v2 Capital/Ambassador profile. If every otherwise eligible origin is occupied on the disposable baseline, `prepare_resident` may select exactly one deterministic living, peaceful origin with an unassigned eligible replacement candidate, establish the production `-30` ruler-relation boundary when needed, and retire its posting before fresh appointment. The authoritative server end plus native return-home route is required unless the exact server not-found response proves a native-only orphan; only then is native retirement allowed with explicit `serverPostingAbsent` evidence. Every other server error fails closed. Native retirement uses terminal status `ended` and preserves `test_fixture_replaced` as its end reason. Preflight blocker diagnostics, relation evidence, replacement evidence, and rollback are mandatory; a stale old envoy activity flag cannot block slot recovery. Deterministic/server matrices are compatible pass-once contracts; `lifecycle_a` performs designation, relocation, native owner-transfer capture, and recovery; `official_dialogue_corpus` sends eight base natural player requests through visible production dialogue, followed by one bounded natural grant exchange against an actually granted charter permission when the fixed commercial request was denied; `lifecycle_b_transition` proves migration, shelter/recovery, and war recall/removal. Ambassador UI evidence uses `ui-action --target ambassador --text select-ambassador --value <posting-or-hero-or-origin-id>`; LiveTest forwards the selector unchanged and the production screen resolves it. `save_prepare` records only a feature fingerprint and never creates a fixed save; guarded campaign-test checkpoint/restart owns both required reload boundaries. The fingerprint retains stable capital, session identity/authority/state, posting, envoy, and charter state while excluding only continuously advancing court-session day/revision counters. |
| `reign_get_royal_council_test_manifest` | None | Returns the provider-free and guarded native Royal Council acceptance profiles, required UI states, routing cases, persistence boundaries, and capital-relocation coverage. |
| `reign_start_royal_council_test` | verification-control gate, armed exact campaign-test Current, plus confirmation for native mutation/provider profiles | Starts one cataloged Royal Council profile. Read-only UI and deterministic profiles exercise the production screen and contract seams; provider, save/reload, and relocation profiles require the exact enrolled disposable Current and fail closed unless their native operation is available. The route never accepts arbitrary campaign actions or a caller-authored advisor packet. |
| `reign_get_ruler_docket_test_manifest` | None | Returns the launch-ready ordinary-petition and 68-template noble-matter matrix, production-UI path, Chancellor lifecycle, family/death/custody/reversal/stay/proclamation branches, persistence, dialogue/art, and 180-day natural-player acceptance plan. |
| `reign_start_ruler_docket_test` | verification-control gate, armed exact campaign-test Current, plus confirmation | First opens the production Court at the active safe capital for every court-dependent profile, establishing capital-scoped royal access before preflight or fixture work. It then creates an exact native settlement need or noble eligibility on the guarded Current, derives production state, accepts the template minimum through the four-character active-audience cap, opens the visible audience, exchanges a natural ruler turn with every involved participant, exercises the selected bound player control, and records authoritative effects. Every unique `fixtureRunId` deterministically chooses among distinct participant sets generated by a bounded search of the production day/slot selector; the receipt retains the eligibility slot, chosen slot, selected variant index, available variant count, and participant identity, clan, culture, gender, age, occupation, marital, clan-leader, and role metadata for aggregate release-diversity auditing. It also returns every persisted conversation line verbatim with speaker/role attribution and a per-line forbidden-statistics verdict; any NPC percentage, hidden tier, relationship score/value/loss, penalty, or game-mechanics language fails the observe gate, while ordinary in-world quantities remain valid. Opening, ruler-question, and closing-reaction waits scale from the actual one-to-four participant count at 120 seconds per speaker, each capped at 480 seconds inside a separately bounded 1800-second live command. Marriage and divorce observations independently require reciprocal native spouse creation or complete spouse clearing when their typed terms commit. Reputation-tag observations flush World History and query the authoritative social profile; a pass requires the expected active tag's activation source to carry the exact matter judgment correlation. Docket tags use only the catalog-authorized `noble_judgment` direct producer, and repeated judgments refresh exact provenance without making ordinary rumor-derived tags generally writable. Denied divorce requires the marriage to remain intact and proves that no judgment-correlated `divorcee` tag was created. An effects hash alone is not tag proof. Noble profiles cover both sides, natural-language acceptance stances with no visible statistics or game mechanics, evidence, tags, marriage, divorce, murder, investigation, custody, reversal, execution, overnight Court stay, save/reload, and Royal Proclamation. Destructive family/death branches require restored disposable copies. Time advancement and saving remain external guarded campaign-test responsibilities. |
| `reign_get_kingdom_event_test_manifest` | None | Returns the authoritative schema-v2 risk-based acceptance manifest: 15 once-per-archetype production forces plus six representative first/middle/last ordering additions, for 21 forced cases. |
| `reign_start_kingdom_event_test` | verification-control gate, armed exact campaign-test Current, plus confirmation for destructive profiles | Runs one Kingdom Event profile after deriving the run-owned Current and passing it to the client for an independent active-native-save check. `force` and `observe` prove production selection, formulas, duration, exact consequence, announcement/history correlation, idempotency, and one-shot forcing. `no_target` proves the three immediate adapters refuse an absent exact target without mutation. `organic_prepare` fails closed before creating a ledger or arming an override until campaign initialization, autonomous-world preparation, native-save quiescence, and Save Sync alignment are complete. It then normally predicts a native eligible one-percent day; its explicit `accelerateOrganicTrigger` option instead arms one non-serialized exact-run next-day trigger without changing the production constant or original stable roll. Guarded campaign-test control advances time, `organic_verify` proves the exactly-once non-forced result and consumed override, and same-run cleanup clears an unused override. `save_prepare` only marks a feature fingerprint and game instance; guarded Current checkpoint/restart owns persistence, and `save_verify` requires a different instance. The harness never creates fixed saves or advances campaign time internally. |
| `reign_get_government_test_manifest` | None | Returns the authoritative cultural Government acceptance profiles, exact 104-resolution/two-route contract, authority weights and penalties, people-versus-noble pressure boundary, persistence protocol, and native acceptance matrix. |
| `reign_start_government_test` | verification-control gate, armed exact campaign-test Current, and exact confirmation | Runs one Government profile after deriving the run-owned Current and requiring the client to independently match Bannerlord's active save. Profiles cover culture institutions, weighted saved authority, multi-plank parties and speakers, individual relationship-aware voting, 104 tangible two-route resolutions, target-specific progress, levels 1-5, additive people pressure, ruler/ambassador knowledge, and save fingerprints. The harness never advances time, creates or infers a save, uses native Influence, changes Public Standing, or directly changes rebellion probabilities. |
| `reign_prepare_government_certification` | existing campaign-test enrollment and immutable fingerprints/evidence | Binds one durable 85-instance Government release run to exact campaign/timeline/baseline/Current, source/client/server/provider/catalog/Bannerlord fingerprints, and retained validation/deployment evidence without starting Bannerlord. |
| `reign_get_government_certification_status` | None | Returns the prepared enrollment, executions, evidence, and unstarted manifest cases without contacting the live server. |
| `reign_carry_forward_government_passes` | prepared source/destination ledgers, exact audited scope, explicit case instances, and exact confirmation | Revalidates each selected durable report before copying unaffected evidence into the destination ledger. The hidden-language/readiness scope excludes persistence, soak, and cleanup; the lifecycle-prune scope admits only soak prepare/verify; the consent-persistence-harness scope admits only consent prepare/verify, unaffected full-feature persistence, and cleanup. Every scope requires identical guarded enrollment/provider/Bannerlord boundaries, complete current assertions, a real production dialogue send plus active clean-room receipt where applicable, contextual quality, zero direct-action bypass, required external evidence, and source-run/live-run/report hash provenance. It never starts Bannerlord or mutates a campaign, and it rejects affected, incomplete, failed, or structurally invalid reports. |
| `reign_start_government_certification_case` | verification-control gate, prepared run, armed exact Current, and exact confirmation | Starts one of 6 deterministic, 36 natural-language, or 43 native/UI/persistence/pressure/soak cases. The native set includes one case for every supported culture institution and a restored forced fallback Council of Estates fixture. Dialogue cases use ordinary Individual Chat and an exact-command, exact-disposable-save clean-room scope that removes only earlier Government-certification dialogue; the server receipt is mandatory, native personality/relationship/party/realm state remains authoritative, and no outcome is forced. Supportive cases receive a manifest-owned second natural player turn only when the immediately preceding production reply lacks the exact structured consent action. Immediate agreements skip it, and prose is never action authority. Structured fixture steps set prerequisites and observe results only. Every native case requires case-specific assertions. After a process restart, `soak_verify` accepts a retained `soakPreparationLiveRunId` only when that report completed `government_soak_prepare` and its fingerprint exactly matches; the bounded preparation snapshot is rehydrated without repeating time advancement. |
| `reign_record_government_certification_evidence` | existing workspace artifact, exact SHA-256, concrete review note, and exact confirmation | Attaches retained native screenshot, mouse-interaction, manual-observation, soak, validation, or deployment evidence to an exact manifest case; it cannot create or fabricate the artifact. |
| `reign_evaluate_government_release_readiness` | prepared enrollment and retained live reports | Fails closed unless every required case passes on one immutable run, tag/action/safety accuracy is 100%, contextual dialogue quality is at least 95%, false-positive consent and direct-action bypass are zero, persistence/soak/cleanup are complete, and bound validation/deployment evidence still exists. |
| `reign_get_government_certification_report` | None | Returns the durable state, execution ledger, and evidence paths for recovery and audit without contacting Bannerlord. |
| `reign_start_campaign_command_profile` | verification-control gate, armed exact campaign-test Current, plus confirmation | Starts one final-fingerprint Campaign Command profile. The language matrix is a stratified 300-case natural-player corpus; native profiles use only the enrolled disposable save. `save_roundtrip` prepares the persisted order but does not claim reload evidence. |
| `reign_verify_campaign_command_save_roundtrip` | verification-control gate, exact guarded restart receipt, armed exact campaign-test Current, plus confirmation | Rejects invented or cross-run receipts, then verifies the prepared order in a different native game instance and records persistence, checkpoint, and cleanup receipts. |
| `reign_start_social_reputation_test` | verification-control gate, prepared Social Reputation enrollment, armed exact disposable save, plus confirmation | Starts one profile. `preflight` records a bounded 15-case non-focused sample behind the deterministic suite, native role coverage, and a timed 12-ruler/1000-NPC formula probe; it is subsystem proof rather than full release readiness. Its isolated suite runs once on an enrollment-keyed worker task, controller completion retries join that task instead of restarting temporary campaigns, and authoritative recording is serialized and cached. `npc_favoring_presence` executes the real native streak/reset/day-eleven pair processor with in-place existing-record mutation and an exact rare-branch source. `player_favoring_dialogue` is a conversation count and per-target isolation test: it requires two clean-history NPCs, sends six visible configured-model exchanges to the primary and one to the independent control, and requires observable replies and normal closes. It does not grade conversational coherence because reply semantics do not affect this feature outcome. The server accepts only clean target isolation, six-versus-one exactly-once receipts and independent counters, the player-ruler gate, and the native 10% tier-scaled roll; its multi-turn command may run for 3600 seconds, while each provider-backed dialogue response has a dedicated observable 900-second client timeout and control traffic retains shorter limits. `favoring_rebellion` combines exact run-owned native rebellion fixtures with one real non-player ruler transition and immediate restore. `longitudinal_90_day` stores structured day 0/30/60/90 checkpoints and passes only after at least 89.5 days with bounded state growth and ruler turnover. The report exports stored case metadata and parsed assertion evidence. Profile fingerprints invalidate only affected coverage while preserving compatible focused passes. `favoring_projection` produces two independent native parameterized favorites and lets the server prove collapsed global cost, favorite/non-favorite/foreign observer math, and no relationship-pair creation. `favoring_jealousy_charm` reuses that compatible fixture for a real saved low/high-Jealousy matrix, exact Charm math, neutral-player behavior, and another no-pair-creation audit. `player_parity` uses the native player Unchaste producer and requires compatible player Flirt, Affair, and NPC gender-matrix passes before the server proves the exact player gender value. `save_prepare` stages a save-backed marker and native/server state fingerprints but never passes persistence; after one aligned rolling save and a different-game-instance reload, `save_verify` alone may pass the idempotency case. `cleanup_marker` removes only exact run-owned persistence evidence and overrides. The server oracle alone records each case result; readiness exposes explicit ready/verdict/blockers. |

Social Reputation's current mutation route additionally requires the exact armed
`campaignTestRunId`. MCP derives its run-owned `Current`, timeline, prefix, and
protected baseline; the native host independently compares Bannerlord's active
save slot. The older `Reign_SocialBalance_` wording above describes compatible
legacy evidence only and does not authorize a new unmanaged save.

## Ruler Docket transcript acceptance

The noble-docket launch-acceptance transcript contract recognizes natural promises to honor, obey, submit to, or accept the ruler's judgment, including a post-judgment acknowledgement that it is given or heard. Tier-two reconciliation additionally requires an explicit promise not to keep, renew, continue, or pursue the quarrel or a declaration that the quarrel ends here. Explicit refusal or appeal language has precedence and fails closed even when the same reply contains acceptance vocabulary. Every NPC line remains subject to the forbidden-statistics and game-mechanics gate.

## Resources and prompts

Resources expose the roadmap, MCP architecture, testing catalog and guide,
runtime test-tool extraction plan, tool catalog, current runtime status, and
campaign World Test overview. Prompts provide evidence-first workflows for
world diagnosis, verification review, and feature planning.

Codex process recovery is deliberately external to Reign. When a restart is
genuinely required, the project catalog points to the installed
`restart_codex_and_resume_task` MCP tool. Its tray watchdog requires the
requesting task to record a durable checkpoint. Recovery is Desktop-only for
both planned restarts and unexpected closures: unfinished tasks are reopened
in Codex Desktop, and the watchdog never starts a hidden `codex exec resume`
process. Windows and the user can activate a task independently, so background
CLI ownership cannot be made reliably exclusive. This single-writer rule
prevents task locks, competing mutations, and missing-tool-host retry loops.
# Arrest equivalence certification

`reign_get_arrest_test_manifest` returns the versioned v2 equivalence manifest from the owned live-test feature directory. `reign_run_arrest_contract_tests` is read-only and calls the server-authoritative 36-utterance, evidence-authority, policy-correction, idempotency, reclassification, and rescission contract without starting Bannerlord or mutating a campaign. Prepared certification uses `reign_prepare_arrest_certification`, `reign_run_arrest_certification_contract`, `reign_carry_forward_arrest_passes`, `reign_start_arrest_certification_case`, and `reign_evaluate_arrest_release_readiness`. It binds evidence to guarded enrollment and immutable source/client/server/provider/catalog/Bannerlord fingerprints, derives the exact Current save, carries only compatible language/deterministic pass-once evidence, and requires durable native, cleanup, real-map-battle, and staged save/reload reports. The legacy `reign_start_arrest_test` remains available for focused diagnosis but is not sufficient release evidence by itself.

Arrest carry-forward also supports two audited narrow scopes. `campaign_checkpoint_restore_harness_only` permits only recovery-MCP/catalog changes with identical runtime builds. `arrest_sovereignty_fixture_only` requires the same enrollment, baseline, disposable save, server/provider/Bannerlord fingerprints and a changed client build; despite its retained compatibility name, it permits only the client sovereignty-migration behavior, its distinct-foreign-clan native fixture, and the MCP/catalog carry audit. Neither scope carries native, cleanup, or save/reload evidence.

## Rebellion certification

`reign_get_rebellion_test_manifest` returns the compact manifest. `reign_run_rebellion_contract_tests` is read-only; prepared certification uses `reign_prepare_rebellion_certification`, `reign_run_rebellion_certification_contract`, `reign_carry_forward_rebellion_passes`, `reign_start_rebellion_certification_case`, and `reign_evaluate_rebellion_release_readiness`. The server-only contract credits only bounded language-contract adapters. Organic direct/mail pledge, refusal, and report cases require visible production Individual Chat or Correspondence reports. Mail sends the concern-sampling and decisive letters in separate six-day native advancement phases and waits after each for the client correspondence tick counter to remain idle for a bounded 25-second window. Guarded passive-world advancement dismisses only the standard Reign letter-arrival inquiry as `Not Now`, records the count, and never opens a reply or treats the notice as evidence. The native travel case likewise uses natural concern/follow-up language that asks only for support or refusal. Deterministic client state-machine profiles require their own reports. Starts require the exact armed guarded Current save and immutable source/client/server/provider/catalog/Bannerlord fingerprints. Checkpoint and restart remain separate guarded campaign-test operations. Carry-forward preserves only compatible language/deterministic report references with identical fingerprints; native, save/load, transfer, reintegration, and cleanup evidence is never carried.

### Court-life extensions

Family evidence includes the exact scoped `familyBaseline`, current `familyTransition` and a `familyNeglectEvidence` checkpoint created only by a successful `neglected` observation. Named expectations prove a new exact-visit dismissal, new patience-threshold neglect, or later reconciliation from that retained proof. `snapshot` remains observational. The initial clear state cannot satisfy `reconciled`.

The existing `reign_get_ruler_docket_test_manifest` and `reign_start_ruler_docket_test` expose ten production court-life phases: `court_life_preflight`, `court_life_prepare`, `court_life_open`, `court_life_converse`, `court_life_choose_confirm`, `court_life_snapshot`, `court_life_observe`, `court_life_clock_observe`, `court_life_save_prepare`, and `court_life_save_verify`. Source, exact template/hero/option, named expectation, expected turns, and bounded native gold/loyalty fixture inputs are declared in the tool schema. Evidence is `reign-court-life-evidence-v1`, including attributed conversation, exact matter/receipt, production commissions and durable works, native loyalty delivery before/after receipts, visitor residency/release, family hidden-state forensics, and reload fingerprint. See `docs/agent/TESTING_TOOL_GUIDE.md` and `reign.testing.json` for the gates and sequence.

For `court_life_converse` and `court_life_choose_confirm`, an exact still-pending audience in `technical-failure` is returned through the production control and reopened once before the provider-readiness wait. The reopened view model resumes the saved pending player turn from its per-speaker receipts, so completed replies are not requested again. Recovery refuses a changed, missing, resolved, committed, or unsafe-to-close audience and records `technicalAudienceRecovered` in evidence.

International `accept` authority distinguishes unilateral custody from negotiated agreement. Releasing a named captive without ransom may execute on the player ruler's confirmed order even when the ambassador truthfully lacks authority to bind the foreign sovereign. Ransom, payment, trade, goodwill, compromise and changed terms still require their established assent or referral. The captive-release receipt proves the exact hero, ended custody and zero ransom without creating a payment receipt.

The production helper `GET /court/patronage/public-history` returns bounded, scoped `reign-patronage-public-history-v1` subjects using existing native world-history public knowledge rules. It consumes no model call and performs no fixture mutation. There is currently no MCP preview command; deterministic interface verification uses the documented shared `GauntletXmlPreviewer` contract/render/fidelity tools, with Codex connectivity disabled. Four court-life preview-only states and the alternate production ViewModel are cataloged; browser results never substitute for Bannerlord acceptance.

The optional `courtLifeFixtureAgeRulerTo34` flag defaults to false and applies only to `court_life_family_fixture`. With explicit user authority to adjust the disposable test ruler, true sets an existing adult male ruler aged 18 through 34 to age 34 before household creation. The fixture records prior/resulting age and completion, requires the same flag on retry, and never repeats a completed adjustment, rejuvenates an older ruler, converts a child ruler, or advances world time. All empty-household, exact armed Current, confirmation, marriage eligibility, and separate named-baseline promotion gates remain in force. The default still requires age 32 or older and preserves age.

Household fixture origin selection uses the ruler culture native LordTemplates plus matching adult female noble source characters, requires native CreateSpecialHero to clone its source without changing the original hero, and records template/culture/origin-clan IDs. It prefers a same-culture clan in the player kingdom, then a same-culture clan in another kingdom at peace with it; hostile kingdoms remain excluded and native marriage eligibility is still required. Missing template and missing clan are reported separately before creating a wife.

Court Life captive setup: `courtLifeFixtureCaptiveHeroId` defaults empty (custody unchanged). Read `court_life_preflight.captiveInventory` for up to 64 sorted eligible IDs plus count/truncation, then make a quiescent guarded checkpoint before using an exact ID with International `court_life_prepare` and a RequiresCaptive template. Existing armed run-owned Current and fixture confirmations remain required. Candidates are actual adult foreign partyless nobles, excluding rulers, family, prisoners, assignments and quests; their peaceful kingdom must retain another eligible envoy and have no existing player-held captive. A selected hero participating in a pending matter is excluded; an unrelated pending matter in that kingdom does not exclude the hero. Preflight also reports exclusionCounts and up to 64 sorted existingPlayerHeldCaptives. Native TakePrisonerAction establishes custody; normal production eligibility and the exact captive binding must then pass. Capture intent and before/after custody are persisted as `reign-court-life-custody-v1`. Partial failures block reuse of that fixture ID: inspect the receipt and restore the guarded pre-fixture checkpoint before retrying; never save over that rollback point with a partial fixture. No payment, acceptance, pressure, war, time advancement or save is injected. International `nativeState.international` reports exact captive custody, foreign ruler gold and the player/foreign kingdom pair with `warStateKnown` and nullable `atWar`. Re-prepare save fingerprints after this evidence expansion; prior fingerprints are not fresh persistence proof. Native capture, real conversational payment/release and reload acceptance remain required in addition to catalog validation.

Court Life ruler recognition uses `formal_court_audience` only when structured courtLifeContext matches current authoritative native facts: campaign/timeline, actual speaker and ruler, ruler office and kingdom, capital/host settlement, observer location and observation day. The generic identity adapter preserves that context; formal presentation may upgrade an existing unknown acquaintance, but ordinary visits, free-text titles, another speaker or capital, stale observations and changed office/realm cannot. Existing `contracts.sovereign_identity` and `pipeline.identity` checks include `court_audience_*` qualification boundaries and actual prompt-adapter persistence through a fresh database connection. Native acceptance must inspect the real foreign envoy/visitor opening and its exact identity audit. Preserve previously verified ruling effects instead of repeating the judgment to retest recognition.

Court Life save comparison retains authoritative ages and hero existence in `nativeState.familyNativeAges`. When that field exists, the fingerprint excludes only the family mirror observation `worldDay` and cached `profile_json.age`; every other family value remains included, and raw evidence is unchanged. Isolated contracts require cached-age refresh to compare equal, while native age, child status, dismissal and row-content changes still fail. Identity-keyed members and visits remain order-independent. Prepare a new fingerprint before verifying in another game process; previous failed fingerprints are not upgraded or silently accepted.

Production Test Lab `POST /tests/run` in forced canned mode uses `BuildTestMessages` and the same dialogue/event builders as game requests. It now also captures complete evidence labeled `captureStage=test_lab_assembly`; that label never claims a provider call. The existing MCP does not wrap this older Test Lab endpoint, so the before/after deployment comparison uses that documented API with fully supplied snapshots, a task-owned `verify_prompt_comparison_20260908` campaign namespace, no snapshot paths, `live=false` and `canned=true`. The comparison is restricted to the successful prompt phase, not the later action-normalization result of a deliberately action-free canned reply. Full baseline messages are available through this test route; the original in-game audit remains truncated. The installed server is verified after deployment through the same route and through isolated Verification Lab checks.

The focused quick/offline prompt_efficiency suite explicitly executes contracts.dialogue_prompt_budget and pipeline.prompt_caching. It measures assembled commoner/noble system prefixes (45000-character bound), not a sum of selected template files. A catalog-only result is insufficient proof. The existing pipeline suite still runs the same caching contracts as part of its wider coverage.


Clan Accords: `runtime.clanAccords` reports availability and pending saved synchronization. The campaign-test quiescence gate requires schema `reign-clan-accords-runtime-v1`, available=true and pending=false before saving. `ui-open --target clan-accords` uses the existing guarded UI calibration route and requires a player ruler. Offline ledger, consent and isolated database replay checks run in world_diplomacy; actual native effects, dialogue, save reload and rendering remain post-deployment acceptance on the exact enrolled disposable Current. Deployment is on hold pending the user's approval.

Wanderer population: reign_get_live_test_status exposes runtime.wandererPopulation (town counts, coverage, pending contact checks, and at most 32 local identities). Safe current-town menus are eligible and prioritized for top-up; active missions and sieges remain mutation-free. The existing POST /dialogue/history wandererContactOnly branch is observational, requires exact campaign/active timeline/hero/player IDs, and does not construct profiles or mutate game state. The wanderer_population quick/offline suite is provider-free; native acceptance still requires exact enrolled disposable Current and deployment authorization.

The existing live-test scenario operation clan_accords_test accepts profile contracts only and emits reign-clan-accord-native-contract-v1 with 38 real-native math and isolated SavedState JSON assertions. It uses the existing armed bridge and main-thread dispatcher, with no provider call or live campaign mutation; it cannot advance time, save, create an accord or prove an actual fresh-process save roundtrip. This runtime check remains after authorized deployment. Read runtime.clanAccords for pending synchronization.

## Character narratives and interests (2026-09-09)

`reign_inspect_character_narrative` reads the complete base/effective narrative, defining trio, retained development projection and full-roster diversity counts through `GET /characters/narrative`. It never materializes a character or calls a provider. Supply a hero ID and an optional campaign ID; absent campaign reads the shipped catalog. Missing data is explicit.

`reign_author_character_narratives` uses the installed Character Construction provider/model, with exact confirmation `author character narratives using configured provider` and trusted verification/offline permissions. It runs only a successful Release validation artifact, without a listener, in the ReignValidation database and a build-root-only runtime. Batch size is 1-25, offset selects catalog order, and authoringRunId is a simple confined task name. One writer holds the run lease. Each profile resumes validated life/interests stages; three failed attempts stop that stage without generic substitutions. Provider settings are read without modification. No campaigns, deployed files or active catalog are changed. Output and evidence: `.codex-build/reign-mcp/narrative/<authoringRunId>/batch-<offset>.json`. A catalog is assembled only after all profiles pass. Cancelled batches retain completed profile checkpoints. Authoring can incur provider usage; offline contracts cannot invoke this path.

Run `character_narrative` through the artifact-bound quick Verification Lab and manifest-selected validation. The contract covers weighted influence, stable semantic selection, complete generation, privacy/relevance rules and gated development. Provider dialogue behavior and native fresh-campaign save/reload acceptance remain separate requirements. Earlier campaigns retain v4; only the native persisted fresh-campaign marker enables v5. Deployment remains a separate stopped-runtime operation using successful validated artifacts.

The `operation` parameter accepts `author` (default) or `evaluate`, with the same exact provider confirmation for both. Authoring uses at most three independent profile workers; stages within each profile remain sequential, batch writers hold an exclusive run lease, and completed checkpoints are reused only when current native facts, personality, library and blueprint fingerprints match. The staged v5 catalog references one validated `narrative/profiles/<hero-hash>.json` per character and assembles only when the entire roster is complete. `evaluate` uses the saved Dialogue model plus an independent Character Construction judge for three ordinary scenarios and three 1/5/10 interest contrasts per selected character. Case replies, judgments and fingerprints are retained under `evaluation/`; structural, privacy and canon checks require 100%, behavior at least 95%. This is provider evidence, not native game acceptance.

The narrative library now contains 664 hobbies across 32 families and 1,904 other concepts. Children under twelve use authored play, attachment and learning concepts; native age governs voice and interpretation. Adult options include status-seeking, traditional, selfish and conflicted motives as well as generous ones. Native rulers receive an additional provider case with a consuming kitten interest during an urgent military supply discussion. Previous failed provider evidence is retained before a retry.

The provider-free `character_narrative` suite emits `data/tests/verification/character-narrative/control-center.html` and `fixture.json`. Run `ReignBetaServer/src/Modules/Characters/narrative-editor-ui-contract.mjs` with that fixture directory and an evidence directory, using bundled Node/Playwright (`REIGN_PLAYWRIGHT_MODULE` may point to the installed package). The harness intercepts every browser request; it does not open the installed Control Center or start a listener. It checks 1672/1024/600 widths, current/expanded/long-text/history states, first/last cards, escaping, editing, add/remove, private history labels, hidden construction metadata, and legacy empty state. Reports and screenshots are written only to the selected evidence directory. This web-editor change adds no native shell or portrait aperture; native campaign acceptance remains a separate deployment follow-up.


Character narrative construction (`single_call_v1`) keeps permanent IDs and ratings local, normally uses one compact provider request, and checkpoints valid sections for bounded targeted repairs. Batch evidence adds providerCalls/elapsedMs/reused; attempts add durationMs/requestCharacters/responseCharacters/requestedSlots. Existing authoring confirmation, isolated artifact root and no-deployment gates are unchanged. Initial roster authoring belongs to the implementation task, not the game provider.


Character narrative dream compatibility is local, personality-aware selection before the single construction request. The reviewed dream-compatibility.json covers every adult dream; low compassion cannot receive a genuinely philanthropic dream and low Court honor cannot receive an integrity-based dream. The character_narrative offline suite checks exclusions, supporting-trait weighting, trait-band boundaries, policy coverage, and zero incompatibleDreamCount in the shipped roster audit. This adds no provider-consuming operation and does not change deployment or campaign gates.


Adult desires and values also use reviewed personality eligibility from ReignBetaServer/ProfileLibrary/narrative/concern-compatibility.json. Required traits must exceed40; requireNotHigh excludes an opposing trait in the61-100 high band. Neutral craft, comfort and autonomy remain available. The existing offline character_narrative suite covers care/honor distinctions, upper and lower boundaries, mixed-personality populations and complete catalog rule coverage. The roster audit adds incompatibleConcernCount and bounded incompatibleConcerns item references alongside the existing dream-only evidence. The initial roster must report zero incompatible concerns. This is local selection and read-only audit; no new provider, runtime, save or deployment authority is introduced.


Narrative factual prerequisites are declared in ReignBetaServer/ProfileLibrary/narrative/fact-compatibility.json. Kin-dependent concepts require known siblingIds, olderSiblingIds or livingParentIds; an absent field is unknown and selects another concept, never an invented relative. Premade enrichment uses matching native parent IDs, ages and living status without changing the source facts. Dynamic inputs with no structured kin evidence retain the many non-kin concepts. Explicit minimum ages prevent adolescent past apprentice-supervision claims. Existing character_narrative contracts cover unknown and younger-only siblings, known older kin, living-parent and mentorship-age boundaries, and immutable enrichment. The complete roster audit adds incompatibleFactCount and bounded incompatibleFacts; require zero. No provider calls, database writes, native changes or new authority are introduced.

The narrative fact policy also accepts an optional maximum age. Routine requests to be picked up when tired are limited to age six and younger; older children retain the broader child concern pool. This is an offline eligibility rule with no additional provider or campaign authority.

Court Life snapshot diagnostics (2026-09-09): `court_life_snapshot` retains the exact visible fixture matter and armed disposable-save gates, but can capture a resolved or busy audience. A read-only picture requires neither `AutomationCanDecide` nor completed provider replies and never proves a completed turn, acceptance, or decision authority. `court_life_converse` and `court_life_choose_confirm` retain their full readiness gates. Use `court_life_observe` with `resolved` for resolution receipts and the guarded save/verify pair for persistence. Native acceptance captures the resolved audience before it closes on the next distinct untested encounter, retaining the original ferry failure. Resolved matters cannot be reopened through production eligibility; never replay a judgment or relax eligibility to obtain a picture. Family rule coverage includes patience 0, 1, 10, 11, 40, 41, 50 and 100, zero-before-first-dismissal, and the production missing-trait default of 50; native family transitions remain separate evidence.
# Codex image verification

`reign-codex-image-compatibility-v1` uses existing tools: `reign_run_offline_verification` with explicit `suite=codex_images` for provider-free contracts; after validated deployment and explicit subscription-usage authorization, `reign_start_verification` with `suite=codex_images`, `tier=live-llm`, `liveCaseCap=1..3`, `repeat=1`, and `confirmation="start Reign verification"`. Results remain available through `reign_get_verification_results`; detailed image hashes and paths are in the sandbox's `codex-images/report.json`. No new MCP mutation method or game/save operation is introduced. See the `codexImages` entry in `reign.testing.json`.


## OpenRouter providers (2026-09-09)

`reign-openrouter-providers-v1` adds independently saved NanoGPT and OpenRouter chat credentials and model selections while retaining Codex and custom APIs. `openRouterApiKey` is shared by OpenRouter chat and all four image profiles; blank/masked/null submissions preserve it, explicit clearing never falls back to another key. Legacy API keys migrate only to their endpoint owner once. `llmModelProfiles` stores only the twelve allowlisted model fields per provider; active flat model fields remain the runtime contract. OpenRouter uses its fixed HTTPS endpoints, prefixed chat model IDs, native reasoning controls including direct-selector exclusion, and reference-image requests to `/api/v1/images`.

Use the existing artifact-bound `interaction_architecture` offline suite and `contracts.image_generation_profiles` (included in quick `contracts`; the focused `codex_images` quick suite also includes all image adapter contracts). Injected image transport proves all five catalog models across four profiles, exact source reference bytes, endpoint/key selection, empty/error/echo rejection and timeout/missing-key behavior. `OpenRouterCatalogTests` checks catalog/help drift. No new tool, live profile, confirmation phrase or campaign mutation route is introduced. The existing GLM cache probe is enabled only for NanoGPT in the UI; its backend rejects non-NanoGPT endpoints before making a provider request.

The architecture suite emits `control-center.html`. Run `chat-provider-ui-contract.mjs` with that HTML path and an output directory; run `portrait-profile-ui-contract.mjs` on the same fixture. Both live under `ReignBetaServer/src/Modules/` (Platform and Portraits respectively) and intercept all requests. The chat matrix covers four providers at 1672/1024/600, draft switching, all model roles, masked-key preservation and save/reload; the image matrix covers NanoGPT/OpenRouter/AtlasCloud and normal-only Codex. Reuse `control-center-ui-contract.mjs` for full-page palette/navigation regression. MCP has no browser-preview tool; these documented shared fixture harnesses are the fallback. Native Bannerlord, portrait-layering and approved-raster fidelity are not applicable to these web form changes. Provider-backed output still requires a configured OpenRouter key and separately authorized paid smoke testing.

Image presets are exact matches verified from `GET https://openrouter.ai/api/v1/images/models` on 2026-09-09: `openai/gpt-image-1`, `google/gemini-3-pro-image-preview`, `bytedance-seed/seedream-4.5`, `bytedance-seed/seedream-5-0-pro`, `x-ai/grok-imagine-image-quality`. GPT Image 1.5, WAN and Flux Kontext exact variants were absent and are not silently mapped to another model. Existing AtlasCloud/NanoGPT catalogs remain intact. See https://openrouter.ai/docs/guides/overview/multimodal/image-generation for request capabilities.

## Codex performance controls and isolated comparisons

`reign-codex-performance-report-v1` is cataloged under `codexPerformance` in `reign.testing.json`. Run `reign_run_offline_verification` with the exact successful Release validation run ID and suite `codex_performance` for injected runtime, conversation, settings migration, dispatch-cap, and report contracts. Run `interaction_architecture` for the shared Control Center fixture, then its three browser contracts at 1672, 1024, and 600 pixels with all traffic intercepted. This is a web control change: native artwork raster/fidelity checks do not apply.

The optional provider comparison is explicit-only: `reign_start_verification`, tier `live-llm`, suite `codex_performance`, the usual `start Reign verification` confirmation, and `codexPerformanceJson`. The JSON must name the exact model, caseIds, full baselineOptions and variantOptions, the single changedOption, a mandatory totalProviderCallCap from 1 through 1000, and confirmation `run bounded Codex performance comparison`. The run-wide cap covers every physical turn/start across primary generation, repair, tier retries, probes, judges and repeat passes. Discovery and thread creation are not generation calls. Cancellation/closed budgets prevent later dispatch. The suite uses isolated Verification Lab campaign data, preserves the selected live provider/settings, and is never included implicitly in aggregate live suites.

Model checking is separate: POST `/api/codex/models/verify` requires `maxRequests:1`, `bounded:true`, and confirmation `verify one Codex model request`. The Control Center explains usage before confirmation. Discovery remains free. A timeout remains inconclusive; request-model echoes do not establish actual served model identity. No user entitlement or shared Codex configuration is modified.

Reports separate baseline/variant latency and quality outcomes, retain unknown usage/fingerprints as unknown, and keep clean attempts, successful repairs, accepted repair overrides, and unusable results separate. Runtime/conversation diagnostics contain no reasoning or rejected draft text. GPT-5.4 access, actual Fast-mode service, and live roleplay quality require later user-authorized tests. See `docs/agent/CODEX_PERFORMANCE.md` for controls and limitations.

The provider comparison plan also requires `validationRunId`: the server checks its own assembly SHA-256 against that successful Release run's exact artifact before dispatch. Source and validation fingerprints come from that report; they are never inferred from model settings. Supply this ID inside codexPerformanceJson (or the CLI `--codex-performance-plan` JSON file). Ordinary quick/offline contracts do not require or authorize a provider plan.

Injected Codex contracts are CLI-only: use artifact-bound `reign_run_offline_verification`. The listening server refuses this injected suite so its test transport cannot intercept gameplay requests. The dedicated Codex browser script wraps the shared provider script; run that wrapper once, then the separate full Control Center matrix.

The MCP supplies its trusted workspace as `validationWorkspaceRoot` so an installed server can locate the exact report. CLI comparison plans may supply that exact Reign workspace explicitly. The report verifies the running assembly hash and records the actual runtime executable hash when its path is known; accepted replies are retained only for later human roleplay evaluation.

Codex comparison fixtures must identify a shipped character or include a completed character snapshot (constructed, traits, and a complete narrative when using narrative v5). Each isolated arm preserves the supplied character stack; fixture personality authoring is not part of the latency comparison. Both arms must keep the same reasoning mode and schema version. Served-model consistency uses authoritative runtime evidence; a request-model echo leaves verification unknown.
# Paired repository validation

The existing workspace/source/validation tools support the declared sibling ReignServer source junctions when `reign.repositories.json` selects `paired` mode. `reign_get_validation_plan` verifies the projections; `reign_validate` audits both exact Git destinations and fingerprints the combined authored source under one unchanged lease. No new publishing, provider, runtime-control or campaign permissions are granted. Initial uploads require the clean-install evidence described in `../../docs/agent/testing/RELEASE_PACKAGING.md`.

## Release runtime build




ReignServer is Linux-only. Canonical server verification uses WSL on Windows, an isolated ReignValidation database, run-owned data and a bounded Linux process group. See `../../docs/agent/testing/RELEASE_PACKAGING.md`.
