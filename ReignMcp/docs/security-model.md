## Validation ownership (2026-09-12)

`reign_build_linux_server` uses the same build/restore gates, ownership plan, paired hygiene audit, source fingerprint, and OS lease as the canonical validator. It cross-publishes into a run-owned directory and emits an exhaustive SHA-256 artifact inventory. It does not deploy, open a listener, access campaigns, or call providers. Its component report cannot substitute for full validation. DwemerDistro separately validates the artifact inventory, activates an isolated version, and checks loopback health; persistent data and previous versions are retained.

Conversation naturalness contracts (2026-09-14) run inside the existing non-listening Verification Lab and isolated fixture campaigns, without providers or live campaign writes. Read-time tone/character projections preserve stored originals. Accepted-gift candidate preservation does not queue an action or bypass consent, native item resolution or authority. Existing bounded repair, second-attempt, provider-cost, runtime-control and disposable-save gates are unchanged; never replay a historical gift automatically. See `docs/agent/DIALOGUE_NATURALNESS_FIX.md`.

Transport probe v2 compares synchronous/asynchronous execution for the same isolated scalar reads: at most 576 length requests plus four TLS-status reads, with the existing 30-second request budget and 10-second timeouts. The connection endpoint, TLS policy, four buffer sizes and synthetic payload limits are unchanged. No production query, snapshot isolation or installed setting changes are introduced by this diagnostic.

Replay v3 captures and reuses original random lifecycle conception identifiers because existing commitment rolls depend on them. This thread-local input source is confined to its exact campaign and the non-listening verification CLI, clears after each route and on failure, and rejects missing, extra or duplicate attempts. It exposes no HTTP switch and does not alter normal gameplay randomness. Full commitment rolls, payloads and reservations remain compared and retained as synthetic diagnostics.

The replay's read-only transport probe requires a literal loopback ReignValidation endpoint and preserves configured TLS. It opens four unpooled connections, issues at most 576 scalar length reads with fixed synthetic payloads plus four unmeasured TLS-status reads, stops starting requests after 30 seconds and uses 10-second connection/command timeouts. It precedes warm-up and timing. `reign_relationship_transport_probe_v2` evidence contains sizes, TLS status and timings only. A skipped/incomplete probe cannot establish performance. It never modifies listeners, networking, PostgreSQL settings, schemas, campaigns or installed files.

Source launchers keep the existing `DwemerAI4Skyrim3:5432` defaults until explicit database cutover. User shortcuts should target the installed launcher rather than mutable build workspace files. Verification sandbox retention accepts only direct child paths beneath the known sandbox root and never follows reparse points during pruning. CLI startup failures return a nonzero exit code.

The relationship throughput suites require the non-listening Verification Lab CLI and its existing isolated database route. The original read-path oracle is thread-local and has no HTTP setting. Each run owns one newly generated `__relationship_replay_` campaign; only that schema and directory are removed. Retained input/report files are synthetic, contain no provider credentials, and cannot trigger live time advancement. The full suite's performance failure is not authority to lower the launch budget or deploy. Native capture, soak and migration execution retain their separate gates.

Replay v2 separates functional observers from the timed candidate route, resetting parsed-document caches before each identical warm-up. Unique semantic native-action/conception identities replace only opaque storage IDs in comparisons; full payloads, referenced conception content, reservations and commitments remain included. Ambiguous identities fail. Raw referenced-state diagnostics stay in the isolated synthetic evidence directory. Timed samples are persisted after the measured arrivals, so an interrupted timed run is incomplete evidence. The 100-day, two-second-arrival, strict subsecond p95 gate is unchanged.

Relationship readiness uses the fresh `reign_world_readiness_v1` view; missing evidence refuses completion and the eight-observation/two-second minimum remains. Native `requires_observation` targets need a real current-revision client receipt before legacy flags can clear. The dedicated-database preparation script is Plan-only by default, requires an exact mode confirmation for execution, rejects source/target aliasing and existing restore targets, verifies artifact hashes, and never switches launchers or modifies Skyrim services. Provisioning and live restoration are deployment operations, not validation. See `docs/agent/RELATIONSHIP_THROUGHPUT_IMPLEMENTATION.md`.

`reign_get_validation_status` returns `reign-validation-lane-v1` with available, busy or unavailable status. It reads the OS lease and bounded `reign-validation-lease-v1` attribution/progress: exact task UUID, process ID, profile/configuration, up to 256 changed paths plus total count, times, current operation, completed count and optional run/fingerprint/report. An available snapshot has no owner; last-run metadata is advisory and never establishes success.

Supply the current task UUID as optional `requestingTaskId` on `reign_validate`; it is attribution, not authority. The non-listening CLI accepts `--task-id` with `CODEX_THREAD_ID` fallback; `--validation-status-cli` only reads status. No status route builds, creates/removes a lease, cancels another task, starts a listener, changes approvals or controls the Reign/game lifetime. The OS lease remains authoritative. Only matching lease IDs bind an atomically published progress sidecar to a held lease; legacy/corrupt metadata remains busy with unknown attribution. Failed inspection is unavailable and must not imply permission to start or delete anything.

Coordinate only actual overlaps with active same-project tasks; preserve stopped tasks and existing user scope. See [coordination](../../docs/agent/TASK_COORDINATION.md) and [the indexed testing guide](../../docs/agent/TESTING_TOOL_GUIDE.md). The catalog's `guideDocuments` retains the focused procedures; existing per-feature contract assertions follow that index.

## Court audience artwork (2026-09-09)

Cache identity uses whole-year appearance age; ordinary fractional aging does not trigger another scene. The native portrait maturity guard is preserved: unsupported infant/toddler references fail before a partial-cast request rather than borrowing an adult face. Missing references remain a distinct, observable artwork failure.

`reign-court-audience-art-v1` uses the existing castle-scene route with exactly one composite containing every involved courtier and the approved host-town hall. `CourtAudienceSceneTests` runs the production compositor, fake provider boundary, cache, failure and view-lifecycle seams through manifest-selected `changed` validation. No paid per-person portrait preparation occurs; missing cached portraits use bounded free native appearance captures. Native capture/age/likeness and provider image quality still require Bannerlord acceptance.

Existing `snapshot`, `noble_snapshot` and `court_life_snapshot` expose `audienceArt` with pending/ready/failed/disabled status and, when ready, the `CourtPetitionArt/<snapshotKey>.png.json` receipt. It contains complete participant IDs, host/culture, reference/composite/image hashes and logical input-image/scene-request counts. Logical counts are not billing receipts; retain actual provider receipts separately. Court Life snapshots remain read-only diagnostics while dialogue/art are pending. The fixed hall and textual international `sharedScene` never establish generated-image success. Dialogue/decision controls retain their independent readiness. Existing provider authorization, exact disposable-save and snapshot gates remain unchanged; no new endpoint, command or confirmation phrase. Verify full cast, cross-culture halls, responsive dialogue, cache/reload and close-during-generation after deployment, and keep the roadmap acceptance item open until that evidence exists.

## Control Center navigation (2026-09-09)

Full style extension (2026-09-09, requested after the navigation preview): all Control Center panels, headings, controls, tables, logs, character/standing cards and dynamic relationship matrices now use the canonical `palette.json` tokens. Legacy brown/bright-gold gradients and glow are removed at their original rules. The existing browser contract additionally audits computed foreground/background/border colors on every page, production-populated character and world renderers, focused/disabled inputs, hover/active buttons, checkboxes and file selectors. Brand and portrait pixels, page layouts, form values and backend behavior remain unchanged. `control_center_modern_style_contract` extends the existing architecture suite.

`reign-control-center-navigation-ui-v1` groups the ten everyday pages plus Diagnostics in the main navigation. Diagnostics exposes Overview (the original Diagnostics page), Logging, Log Viewer, Audit Viewer, NPC Dialogue Audit, World History, NPC Dialogue Lab, Relationship Director, World Test, Test Lab, Live Test Bridge and Verification Lab. Returning to Diagnostics restores the last opened child; switching pages preserves unsaved inputs. Existing loaders and backend operations retain their behavior.

Run the existing artifact-bound `interaction_architecture` offline suite. Its `control_center_navigation_preview` result names the exact emitted HTML fixture directory. Then run `ReignServer/src/Modules/Platform/control-center-ui-contract.mjs` with that directory and an evidence output directory, using the existing Node/Playwright installation (`REIGN_PLAYWRIGHT_MODULE`; Edge is the default). The 66-case matrix checks every page at 1672, 1024 and 600 pixels, loader dispatch, keyboard access, selected states, remembered child, preserved drafts, navigation bounds and frozen palette tokens. Inspect the screenshots and `ui-report.json`. MCP has no web-preview command; this shared local fixture harness is the documented fallback.

All browser traffic is intercepted and page loaders are observed at their dispatch boundary. No provider, campaign, listener, save or live Control Center is contacted. This proves web navigation, not backend tool execution. No native prefab, portrait, approved-reference raster or Bannerlord adapter is changed, so native preview/fidelity and game acceptance are not applicable to this navigation edit. Install only validated artifacts through the visible unified lifecycle if deployment is requested.

Legacy audit: removed 202 lines of unbound director-sample and retired relationship-simulator JavaScript plus its unused chart/table/progress styles. The simulator controls were already absent and its four server routes explicitly return retired/no-state-change responses. Keep those compatibility responses. Relationship Director still observes current MBTI/lifecycle/political-marriage state; its Ambient Sample uses the current snapshot route. Test Lab still provides editable deterministic/provider replay cases and correlation import; Verification Lab orchestrates broader gated checks; World Test remains the passive observatory. Those active tools are not obsolete merely because their names overlap. NPC Dialogue Lab, Live Test Bridge, audits and log viewers remain reachable. No saved test cases, test history, runtime data or campaign content was deleted.

## Government input observation (2026-09-09)

The existing `ui-status` operation exposes `governmentInput` with schema `reign-government-input-observation-v1` and `courtPetitionOpen`. It reads focus ownership, layer attachment, input allowances, active business identity and per-opening press/release counters. It does not open a fixture, select business, vote, invoke dialogue, advance time or save. Counters remain non-serialized and cannot certify command execution. Existing live-test arming and command gates remain in force; use an observational Government run, not an unrelated feature enrollment, when a prior run is terminal.

## Diplomatic completion evidence (2026-09-09)

For the asynchronous negative boundary, use `court_life_choose_confirm` with `courtLifeExpectation=pending`. Its `pending_diplomacy_confirmation_has_no_settlement_effects` assertion requires an unfinished real confirmation, a single Executing native action with Success=true/Completed=false, a pending court matter without settlement receipt or native committed flag, unchanged ruler gold, and no matter relation adjustments or history. `usedProductionChooseAndConfirm` records invocation; `productionConfirmationCompleted` and `expectedPendingDiplomaticAction` distinguish completion from the expected wait. Unrelated errors cannot pass this gate.

Government pending receipts identify the responsible kingdom and saved hearing through `governmentBusinessId`, `governmentKingdomId` and `governmentStatus` diagnostics. Only an awaiting-counterpart stage follows the linked counterpart hearing. Normal votes, reconsideration and authorization remain required. The Government snapshot contract checks current authoritative, individual-member/binding-authority wording and authority meaning; its obsolete ruler-may-agree literal is retired. This updates evidence and assertions, not government powers.

International observation adds `nativeState.international.nativeDiplomaticAction`: native service availability, matching action count, exact matterId_native ID, status, type, terms and execution snapshot. Resolved accepted diplomatic proposals must pass `accepted_diplomacy_has_one_completed_native_action` and `accepted_diplomacy_receipt_is_completed`. A successful choose-confirm operation does not prove completion. A `government_hearing_pending` receipt with Success=true and Completed=false is pending work; preserve Government authorization and advance only through the guarded campaign route. Verify no court settlement effects while pending, then exact native completion and effects once authorized. Re-prepare persistence fingerprints after the additional evidence; retain earlier failures and never infer historical completion from a progress receipt. No safety gate or mutation route changes.

## Binding an existing court encounter (2026-09-09)

Use guarded `court_life_preflight` with the desired `courtLifeSource` and optional `courtLifeTemplateId`. Its read-only `pendingMatterInventory` reports exact IDs, titles, states, completed turn counts and existing fixture bindings; at most 100 sorted matches are returned with totalCount and truncated. Narrow the template filter if truncated. Inventory membership does not waive binding eligibility.

Pass `courtLifeExistingMatterId` to `court_life_prepare` for International or Patronage. This exact selection never falls back to generation or refreshes world/history for a new fixture. It requires the armed run-owned Current, fixture confirmation, matching campaign/timeline/reign/capital and source, plus matching template when supplied. International scope checks the payload's player kingdom and ruler separately from the matter's foreign KingdomId. Committed, missing, ambiguous, already-bound or in-flight matters are rejected, as are fixture gold/loyalty/captive/age changes and participant selection. No baseline is mutated and no decision, payment, time advance or save is injected.

Successful binding stores only a test marker and the existing fingerprint. `existing_matter_binding_preserves_matter_and_native_state` checks unchanged matter/native fingerprint and matter count. Preserve the returned conversation and continue new turns through existing open/converse/choose-confirm/observe profiles. Original automatic Patronage template binding remains supported. MCP rejection and catalog tests are provider-free; native acceptance separately checks invalid-ID/template/already-bound rejection, marker-only preservation, natural conversation and actual effects on the enrolled disposable campaign.

## Testing an existing patronage encounter (2026-09-08)

## Prompt composition and complete evidence (2026-09-08)

### Dialogue marriage authority (2026-09-09)

`contracts.dialogue_marriage` runs within the existing isolated, non-listening quick/offline verifier and never queues a marriage or calls a provider. Personal marriage requires the exact player and consenting NPC, the existing hashed authority receipt with `personal_marriage_consent`, and native suitability; no third-party substitution or clan-only consent inference is permitted. Native `marriage_completed` evidence adds `alreadyMarried` and requires reciprocal spouse links. Model prose is not completion evidence. Existing provider-cost, disposable-save, deployment and campaign protections remain unchanged; offline success does not authorize live mutation.

Use `reign_get_prompt_evidence` (`GET /audit/prompt`) with an exact correlationId and optional campaignId. Pin the returned `evidenceId` while paging `messageIndex`, `offset`, and `length` (1–12000 characters). Empty evidenceId selects the latest retained provider attempt. Content is redacted before storage/pagination, with exact redacted UTF-8 hashes; original character counts, rule/source/version metadata and omission reasons distinguish text from estimates. The per-campaign ledger retains the existing 8 MiB/4 MiB bounded audit policy. Missing/expired/pre-capture evidence is explicitly unavailable. No returned model reasoning, credentials, provider invocation, campaign mutation or arbitrary filesystem access is exposed.

Layout 9 retires only reviewed exact monolithic template hashes into an inactive byte-preserving archive. Unknown custom content stops activation before any file is moved. Shared rule dependencies render once; A-only/B-only coverage and scoped distinctions remain mandatory. Requests remain self-contained with subscription-safe implicit caching. Native wealth v3 separates cash, clan backing and nullable party cargo; appearance remains independent. Run manifest-selected validation, then the existing isolated `prompt_efficiency` and applicable `contracts`/`encountered_residents` suites. Provider/native acceptance still uses the existing cost/disposable-save gates. Details and migration ownership: [Prompt composition](../../docs/agent/PROMPT_COMPOSITION.md).


For an exact `courtLifeTemplateId`, guarded `court_life_prepare` binds the one existing pending patronage encounter before attempting generation. `reusedExistingMatter=true` and `fixtureChanges.markerOnly=true` distinguish this from creating a fixture. The harness preserves its conversation, quotes, participants, receipts and native state; it records only the test marker and baseline fingerprint. `existing_patronage_binding_preserves_matter_and_native_state` verifies unchanged state and matter count. Continue from the returned conversation rather than replaying earlier turns.

Binding rejects multiple pending matches, an already-bound matter, committed effects, or a mismatch in campaign, timeline, reign, kingdom, source or capital. It cannot be combined with fixture gold, loyalty or captive mutations. Existing confirmation and disposable-save gates remain required. No pending matter is deleted and no production eligibility rule is waived. Empty template selection continues to use normal generation. Read-only `patronageEligibility` diagnostics still create no binding.
## Patronage eligibility diagnostics (2026-09-08)

The guarded `court_life_preflight` profile and failed Patronage `court_life_prepare` responses include `patronageEligibility`. Supply `courtLifeTemplateId` to inspect one exact template; an empty preflight filter returns the bounded catalog. `unmetPrerequisites` distinguishes missing capital/ruler, spouse/child/coming-of-age prerequisites, prior creator work, missing public historical subjects, and `same_template_pending`. Reports include at most five public subjects and five pending matter IDs per template, plus the full pending count. Unknown template IDs return `templateKnown=false`.

These diagnostics are read-only: they do not create or bind a matter, inject history, remove pending matters, charge gold, or waive eligibility. The existing disposable-campaign gates still apply. Inspect the reason before retrying preparation; an existing pending matter is not evidence of missing history. Proclamations establish that the ruler made a public statement, not independent proof of every claim within it.
# Security model

Campaign provider-wait contracts use only the artifact-bound non-listening CLI and its isolated validation database. They do not authorize provider calls or user-campaign operations. They use controlled waits and real fixture Save Sync registration/restore to prove that late responses cannot cross a replaced campaign boundary. Production network-wait suspension is limited to owned synchronous HTTP API construction/social requests; it does not relax write/background leases or the Codex conversation-state adapter. Snapshot creation preserves requests, while restore/import/deletion conservatively invalidates suspended requests process-wide until per-campaign identity is proven at every caller.

Political pressure observation uses only the existing bounded and redacted World Test GET. The `political pressures` subsystem and optional kingdom pair expose recorded activity orientation, including player targets. Optional incident details join only records from the same campaign and timeline and expose both sides' recorded pressure and choices; missing records remain null. This capability cannot generate incidents, adjust pressure, invoke providers, advance a campaign or save it; existing mutation and campaign enrollment gates are unchanged.

Spymaster destructive acceptance remains limited to an explicitly enrolled disposable campaign-test Current and requires `run irreversible Reign Spymaster test on disposable save`. Its rare-branch controls cannot be globally armed: they are non-serialized, self-consuming, and keyed to exact production mission GUIDs captured after visible UI submission. Successful assassination and capture/breakout run on separate restore branches. The capture profile targets a governed foreign settlement so its native settlement party can receive the prisoner. Production searches bounded native holder sources and refuses to report capture or leave attribution pending when `TakePrisonerAction` did not produce actual captivity. The guarded Current must be restored after each evidence report.

## Prompt-assisted test scope

`promptOverrideAssisted` is restricted to the guarded Social Reputation `player_affair` profile on an explicitly enrolled disposable save. Natural player text remains unchanged; a separate directive is authorized independently for each request only when the server finds the exact armed and currently executing `player_affair` live-test command, its prepared Social Reputation enrollment, the same current game instance, and Bannerlord's exact active campaign-test Current save. No global or session override is written. Each response returns the server authorization receipt, and the authoritative profile evaluator rejects missing, inactive, mismatched, or non-`request_only` receipts. Other profiles reject the flag, and assisted results must not be represented as independent NPC-acceptance evidence.

Government release certification has one separate allowlisted clean-room directive. It is authorized only for the exact currently executing natural-language `GOV-LANG-*` send inside an `individual_chat` live run, after the server matches its case-prepare command, current game instance, armed bridge, and exact `_Current` disposable save. It removes recalled summaries and out-of-session memory bundles from earlier Government-certification conversations so repeated use of a limited representative roster remains an independent first-encounter sample. Native personality, native relationship, party posture, realm facts, consent standards, and every turn in the current conversation remain active; the exact directive explicitly forbids forcing agreement or refusal. Every response carries an authorization receipt, and Government readiness rejects a missing or inactive receipt. The scope is request-only, is never written globally or to the save, and is unavailable to ordinary play or non-Government cases.

`forceRareAffairExposureAfterNaturalMiss` is likewise restricted to guarded `player_affair`. It is consumed only after one exact validated intimacy signal has generated one eligible natural production miss. The server binds the override to the enrolled run, case, and signal, retains the unchanged production probability and stored natural roll, records the applied roll and consumption, and routes occurrence creation through the production registrar. It is non-serialized and cannot be applied to unrelated profiles, signals, campaigns, or saves.

`npc_favoring_presence` has a separate internal rare-branch seam. It is reachable only while that exact profile is executing on its exact campaign-test Current save, and the client binds it to one run-owned ruler/favorite/day source key after exercising the production separation and day-ten gates through the same extracted pair processor as the daily roster scan. The evidence records the pair-processor revision and exact bounded call count. The production five-percent base chance and native clan-tier multiplier remain unchanged; the server requires the exact World History correlation, completed receipt, occurrence, parameterized tag, and established Reputation before it records a pass. The seam is never serialized or exposed as a general probability control.

## Trust boundaries

- MCP arguments are untrusted.
- Reign API responses, campaign text, logs, and test evidence are untrusted content.
- Tool annotations are hints; enforcement lives in the MCP host and Reign.
- STDIO inherits the launching user's authority, so the MCP configuration must be trusted and local.

## Enforced controls

- The Reign URL must be plain HTTP on a numeric loopback address.
- There is no arbitrary endpoint proxy.
- API responses have hard byte limits and recursive secret redaction.
- Workspace reads are path-confined, extension-allowlisted, and bounded.
- Runtime data, settings, logs, portraits, staging, binaries, build output, and generated artifacts are excluded from source tools.
- Reparse-point directories are not traversed.
- Build and test projects are discovered only beneath catalog-managed roots;
  any unclassified project fails validation before execution.
- Final build output is redirected under `.codex-build/reign-mcp`; ordinary MSBuild intermediate files remain in each source project's workspace-local `obj` directory so `--no-restore` can reuse its locked dependency graph.
- Package restore, verification control, and offline verification have
  independent environment gates and remain approval-gated in the full
  development configuration.
- Mutating test controls require exact confirmation text.
- Social Reputation player-ruler favoring treats conversation state as an
  evidence-isolation boundary: both selected NPCs must have empty persisted
  production dialogue history, and each exchange must return an observable
  model reply and close normally.
  This profile is a conversation count and per-target isolation test and does
  not grade conversational coherence because reply semantics do not affect the
  feature outcome. Server-side assertions reject contaminated targets,
  incomplete exchanges, duplicate receipts, mixed target counters, and invalid
  ruler or chance/tier evidence before the case can pass. The report exposes the
  stored parsed count and isolation evidence. A versioned player-favoring
  capability fingerprint preserves that proven behavior across unrelated batch
  edits, while the separate acceptance-harness fingerprint hashes the exact
  shared source contracts and invalidates the pending batch cases.
- Social Reputation batch acceptance remains bound to the exact enrolled
  disposable save. Preflight is an explicitly bounded 15-case sample plus an
  in-process formula probe and is not full release-readiness proof; its isolated
  deterministic suite runs once on an exact
  enrollment-keyed worker task. Controller completion retries join that same
  task instead of restarting temporary campaigns, and authoritative recording
  is serialized and cached for the run.
  Favoring/rebellion fixtures are exact-run-owned, restore the
  original non-player ruler in a `finally` path, and invoke confirmation-gated
  fixture cleanup. Longitudinal evidence is accepted only from four guarded
  native checkpoints spanning at least 89.5 campaign days; callers still must
  pause, drain, and use the campaign-test quiescence gate before any save.
  Readiness is read-only and returns every remaining blocker explicitly.

## Prohibited capabilities

The MCP host has no:

- arbitrary process or shell execution;
- arbitrary HTTP requests;
- deployment or installed-module writes;
- arbitrary Reign/Bannerlord process execution, executable selection, arguments, or target URLs;
- campaign import, baseline deletion, Save Sync load/rollback, or arbitrary data-file access;

Campaign storage retirement has one separately bounded destructive tool. `reign_get_campaign_storage_audit` is read-only and reconciles stored registrations against the physical Bannerlord `.sav` directory; a retained Reign snapshot alone does not count as a native save. `reign_cleanup_campaigns_without_saves` requires the exact phrase `delete Reign campaigns with no saves`; the server repeats that check, refuses while Bannerlord is running or when the save directory is unavailable, re-audits immediately before mutation, selects only campaigns with zero matching physical saves plus verified direct-child orphan roots, prunes stale registrations from retained campaigns, and preserves imported campaigns awaiting first load plus `_shared`. Automatic retirement is narrower: it can start only from a matched registered native-save deletion whose resulting point count is zero, or from a confirmed native-slot overwrite that transfers the globally unique slot to a newer campaign registration. Active campaigns persist `pending_final_save_deletion`; unload, game exit, or safe startup completes it, while a new confirmed save cancels it. Paths are fixed campaign-owned roots, reparse points and traversal are refused, staging lives outside `PortraitCache`, and partial cleanup remains explicit and retryable.

Portable complete campaign backup remains a Control Center operation rather than an MCP filesystem surface. Version-3 export includes all matching physical `.sav` files and hash-inventories them with the campaign data. Import validates before mutation, installs native saves atomically, and requires a distinct `replaceNativeSaves` confirmation before overwriting any different local save with the same name; ordinary campaign replacement does not grant that authority. Legacy version-2 archives can still be imported but do not manufacture or imply possession of a native save.
- unrestricted native game action, dialogue command, or normal-campaign mutation;
- API-key access or settings mutation.

## Build and verification effects

An isolated build redirects deployable output to the configured workspace build root. It may update ordinary workspace-local `obj` intermediates, but it does not use the projects' normal final output folders or any installed module directory.

Quick/offline CLI verification uses Reign's supported non-listening mode and
isolated verification campaign storage. The standalone tool refuses to overlap
a run reported active by the visible Reign server. It requires the exact run ID
of a successful Release `reign_validate` report, confines selection beneath the
configured validation root, verifies report/run/artifact-path consistency, and
executes only that run's immutable server artifact. Its result records the
validation report, source fingerprint, executable path, and executable hash;
it never falls back to a packaged workspace or installed runtime. Canonical
validation runs a freshly built server inside its unique artifact directory, so
it shares neither the visible server process nor its test storage. Live-LLM and
game tiers are excluded from the CLI tool.

Starting or cancelling the live server's Verification Lab is an explicit gated operation. MCP approval UX is supplemental; the environment gate and exact confirmation are mandatory.

The sole server-lifecycle mutation is `reign_shutdown_server`. It targets only the configured numeric-loopback Reign server, refuses instances that do not report the supported visible unified Control Center and lifetime job, requires the verification-control environment gate and exact confirmation text, invokes only the fixed `/api/shutdown` route, and verifies that Reign health becomes unreachable. It cannot start, restart, activate, kill, or target arbitrary processes.

Game mutation is restricted to fixed, confirmation-gated feature harnesses and
campaign-test tools. Campaign tests enroll an exact immutable baseline, derive
a run-owned `ReignTest_...` namespace, verify the actual native active save,
campaign, timeline, settlement safety, and finalized Save Sync, and then allow
only fixed LiveTest lifecycle/world commands. Advancement and saving require
queue quiescence. Daily clocks must cover the latest fully completed campaign
day; a paused fractional current day is not treated as a completed daily cycle.
Guarded advancement may close only Bannerlord's already-open native MapScreen
Escape menu before and during settlement waiting. It calls the native close
route on the game thread, records the recovery count, sends no synthetic input,
and does not dismiss another inquiry, screen, or menu through that recovery.
Checkpoint preparation additionally finishes a manually opened party conversation,
including Family Chambers, through the production conversation-finish route. It
closes only the same idle view after a successful finish. A busy response, changed
view, or unsuccessful finish refuses saving with `nativeSaveQueued=false`.
The full native receipt records `visiblePartyConversation`; disposal, mission,
quiescence, and Save Sync gates remain unchanged. No unrelated inquiry is dismissed.

Guarded checkpoint and arm saves apply the same narrowly scoped native close
route immediately before `SaveAs`, allow the MapScreen UI transition to settle,
and record `escapeMenuRecovered` in the completed save receipt. This prevents a
native save accepted behind `Return to the Game` from remaining indefinitely
deferred; no other modal, inquiry, screen, or menu is dismissed.
While `world_advance` executes, the native heartbeat exposes its exact run id
and `passive_world` mode. A timed-out or cancelled MCP controller terminates
only its spawned controller process, requests cancellation for that exact live
run, waits for the native bridge to become idle, and attempts a
cancellation-independent queue drain without saving. The explicit stop route
performs the same preemption before snapshot and drain. Both routes refuse to
cancel a busy non-passive feature run and fail closed unless cancellation and
quiescence are proved.
New unique saves fail closed when Save Sync capacity cannot be
proved or its 15-state limit is full. Cleanup accepts only exact enrolled,
inactive checkpoints and cannot delete the baseline. Campaign drain,
checkpoint, and advancement commands return bounded receipts while retaining
full native evidence in their referenced live-test reports, preventing an
oversized status payload from erasing a successful checkpoint receipt.
Checkpoint restoration is destructive only to unsaved transient state:
`reign_restore_campaign_test_checkpoint` verifies the exact active owned
Current save, campaign, timeline, and finalized Save Sync while intentionally
allowing the unsaved pre-reload encounter/menu location to be unsafe. It
requires that save in the run-owned checkpoint ledger, refuses the baseline,
never issues a save, and fails unless the enrolled
campaign/timeline/save reloads at a safe settlement. Its receipt is recovery
evidence and cannot satisfy a feature save-roundtrip gate.

The legacy save-name-inferred Bannerlord start route is not exposed. All
autonomous native testing starts through the guarded campaign-test enrollment,
which can use only its immutable baseline or exact run-owned Current save.
The start tool may attach to an existing Bannerlord campaign only when the native
active-save identity exactly matches that enrollment-selected save. A different
save or a running process without a campaign fails closed, and every successful
start or attach is followed by a fresh campaign, timeline, save, Save Sync, and
safe-settlement snapshot before success is recorded.
Campaign Command, Social Reputation, and the Spymaster agent-activation fixture additionally require the enrollment's
`campaignTestRunId`; the MCP service derives the save identity and proves the
campaign and active-save match before starting a feature profile. The guarded
route invokes the packaged workspace `ReignLiveTest.exe` controller, which uses
the official visible BLSE launcher and cannot select another executable,
working directory, server, command, or arbitrary process argument.
Provider-backed Campaign Command certification sends only synthetic evaluation
utterances and authority labels. Responses are schema-validated, retried only
within a fixed bound, stripped of invented identifiers, and persisted as a
run-owned 300-row base decision ledger with a SHA-256 receipt; up to 100 fresh
targeted rows are permitted only for a measured failure cluster or confidence
gap, and temporary sandbox paths cannot satisfy the release gate. Campaign
Command save persistence additionally requires a guarded campaign-test restart
receipt; a same-process checkpoint cannot satisfy the different-instance gate.

Party Agency preparation is non-mutating and binds one certification run to
the campaign-test enrollment's immutable baseline, exact run-owned Current
save, and concrete source, client, server, provider, catalog, and Bannerlord
fingerprints. `reign_start_party_agency_test` requires verification control,
the exact confirmation `start Reign Party Agency test on disposable save`, and
fresh proof that the enrolled Current save is actually loaded. Its case text is
read only from the workspace-confined versioned manifest. Guest invitations,
renewals, departures, and hostility decisions enter through production
Individual Chat or targeted NPC-first Party Chat as natural player speech; the
harness cannot substitute a direct guest-action operation. Structured commands
remain limited to fixed fixture, time, save/load, battle, provider-fault, and
evidence control. Readiness consumes bounded live-test reports and cannot
become green with a missing case variant, incomplete transcript, direct-action
bypass, unknown fingerprint, or mismatched campaign-test enrollment.
The two manifest-owned hostility-review language cases remain subject to the
ordinary safe-settlement start gate. Each independently creates a guarded
foreign party-leader fixture, obtains consent through natural production
dialogue, declares native war through the fixed hostility operation, and queues
the mandatory targeted review before sending warning-decision language. They
do not attach to an arbitrary existing conversation or replay the separately
certified unwarned-battle case.
The native regression receipt also requires the guest's battle and civilian
equipment signatures to remain unchanged while Bannerlord's equipment-change
permission and Reign equipped-item actions are locked.
The guarded missing-source and invalid-return-target recovery fixtures apply the
native failure state before advancing the preserved natural response through the
production return lifecycle, preventing a successful reunion from masking the
failure mode under test.
They temporarily clear only the disposable fixture record's source-party reference
before native party destruction, so the production camp-protection hook remains
unchanged while the intended missing-party state is created. The receipt proves
that state before the return tick and does not infer it from post-recovery state.
`reign_carry_forward_party_agency_passes` never touches Bannerlord or campaign
state. It accepts only audited equipment-lock-only, guest-relationship-
isolation-only, waiting-camp-encounter-exclusion-only, hostile-battle-fixture-only, hostility-language-fixture-only, partyless-invitation-fixture-only, remaining-native-invitation-fixture-only, ineligible-native-fixture-only, companion-native-fixture-only, native-time-driver-only, save-roundtrip-state-driver-only, accepted-lifecycle-router-only, simultaneous-review-queue-only, departure-language-fixture-only, return-timing-evidence-only, recovery-fixture-only, missing-return-recovery-fixture-only, final-fingerprint-harness-only, and transcript-evidence-parser-only
scopes, requires matching enrollment/provider/Bannerlord plus the unaffected
runtime fingerprint (both client and server for the parser-only, hostility-language-fixture, partyless-invitation-fixture, remaining-native-invitation-fixture, departure-language-fixture, and final-fingerprint-harness scopes; client for the server-only accepted-lifecycle-router scope; server for the client-only waiting-camp-encounter-exclusion, ineligible-native-fixture, companion-native-fixture, native-time-driver, save-roundtrip-state-driver, simultaneous-review-queue, return-timing-evidence, recovery-fixture, and missing-return-recovery-fixture scopes),
revalidates each old report against the current manifest and bypass checks,
refuses the cases affected by the selected scope and the final-fingerprint case,
and persists original build provenance.
The missing-return-recovery-fixture scope rejects only the missing-source-party and invalid-return-target PA-NATIVE-020 variants (plus the final fingerprint), so already-proven death and capture evidence remains eligible for revalidation and carry-forward.
The final-fingerprint-harness scope rejects only PA-NATIVE-022, requires identical client and server builds, and leaves prior-report and immutable-fingerprint proof with the MCP readiness evaluator rather than trusting caller text or asking the native client to prove MCP-owned state.
The guarded save-roundtrip route can restart Bannerlord only after the exact
run-owned Current save has passed the pause, quiescence, and checkpoint gates.
It issues an opaque run-bound reload receipt; the Party Agency verifier accepts
only that receipt and cannot select a different save, executable, campaign, or
arbitrary game command.

Codex restart continuity is not implemented by Reign MCP. The testing catalog
documents the independently installed `restart_codex_and_resume_task` tool. A
restart requires a durable task checkpoint; the external tray watchdog owns
whole-app recovery. Recovery is Desktop-only for both planned restarts and
unexpected closures. The watchdog reopens durable unfinished tasks in Desktop
and never starts a hidden `codex exec resume` process. Because Windows or the
user may activate a task independently, a background CLI cannot be made
reliably exclusive from Desktop. Prohibiting that second writer avoids task
locks, competing mutations, and fail-closed missing-tool-host retry loops.
## Arrest equivalence tests

`reign_run_arrest_contract_tests` is read-only: it evaluates bounded in-memory language and policy contracts on the visible server and cannot start Bannerlord, arm a bridge, or mutate campaign state. Prepared Arrest certification persists only run metadata and evidence references. Its native start tool requires a matching prepared enrollment and `RequireArmedOwnedSaveAsync`, then the game independently compares that derived exact save with `MBSaveLoad.ActiveSaveSlotName` before any fixture effect. Pass-once carry-forward normally requires exact enrollment and immutable fingerprint equality. Its audited `campaign_checkpoint_restore_harness_only` scope still requires identical runtime builds, while `arrest_sovereignty_fixture_only` requires the same enrollment, baseline, disposable save, server, provider, and Bannerlord version plus a changed client build and permits only the client sovereignty-migration behavior, its distinct-foreign-clan native fixture, and the MCP/catalog carry audit. No scope carries native, cleanup, or save/reload evidence. A `Reign_` or `ReignTest_` prefix alone is never authorization. Run-owned cleanup may rescind and remove only `arrest_test_` cases and must restore any temporary settlement ownership before returning.

## Rebellion certification tests

Rebellion preparation is non-mutating and binds exact campaign-test enrollment plus source, client, server, provider, catalog, and Bannerlord fingerprints. The native case tool calls `RequireArmedOwnedSaveAsync`; the client separately verifies Bannerlord's active loaded slot before fixture mutation. NPC-facing decisions enter only through visible production Individual Chat or Correspondence. The server-only contract may credit only bounded language-contract adapters, never organic UI or client state-machine cases. Live reports must echo the prepared enrollment and fingerprint values before readiness accepts them. Pass-once carry-forward retains only original compatible language/deterministic report references under identical immutable inputs. LiveTest does not checkpoint saves: quiescent checkpoint/restart and exact cleanup stay under guarded campaign-test tools, and the protected baseline is never a deletion target.

## Capital and Ambassador lifecycle tests

`reign_start_capital_ambassador_test` requires verification control, the exact confirmation, and an already armed live-test bridge on the guarded campaign-test Current. The client fixture independently requires its persisted disposable ledger before lifecycle mutation. When every otherwise eligible foreign origin is occupied on that disposable baseline, `prepare_resident` may select exactly one deterministic living, peaceful origin with an unassigned eligible replacement candidate, establish the production `-30` ruler-relation boundary when needed, and retire that origin's active posting. It requires the authoritative server end route plus native return-home unless the server returns the exact not-found response, which proves that the saved posting is a native-only orphan and permits guarded native retirement with explicit `serverPostingAbsent` evidence; all other server errors remain fail-closed. Native retirement uses terminal status `ended`, with `test_fixture_replaced` retained as the end reason. A stale old envoy activity flag cannot block slot recovery. Preflight emits bounded blocker diagnostics, prepare records relation and before/after posting evidence, the route cannot run without the fixture confirmation, and all mutations are undone only by guarded baseline rollback. `lifecycle_a` may transfer only the prepared alternate capital to the prepared foreign ruler through Bannerlord's native owner-change action and requires rollback to the exact guarded Current. `lifecycle_b_transition` operates only on the prepared resident posting and uses production relocation, shelter, and war-recall callbacks. The natural corpus is fixed in the MCP tool, uses visible production `ambassador_official` dialogue, and cannot accept caller-authored action JSON. UI evidence may select only an existing posting, hero, or origin through the non-mutating `ui-action --value` selector route; the controller forwards the selector unchanged and the client re-resolves it against the open production screen. `save_prepare` cannot name or create a save; both reload boundaries remain owned by guarded campaign-test checkpoint/restart. Its fingerprint excludes only the court-session agenda day, player-sitting day, and revision counters that continuously advance after reload; stable capital, session identity/authority/state, posting, envoy, and charter state remain bound. Cleanup removes only the exact feature marker and exact inactive run-owned checkpoint; the enrolled baseline is never modified or deleted.

## Royal Council acceptance tests

`reign_get_royal_council_test_manifest` is read-only and returns only the fixed cataloged profile contract. `reign_start_royal_council_test` does not accept arbitrary advisor packets, prompts, campaign actions, save names, or relocation targets. Native provider, save/reload, and capital-relocation profiles require verification control, the exact confirmation, and an armed campaign-test run; MCP derives the run-owned `Current`, and the client must independently verify that exact active save before mutation. Provider-free UI and deterministic routes remain advisory-only and must prove that irrelevant prompts make zero provider calls. The harness cannot create saves, advance time, or mutate relationship, reputation, or political-pressure state. Save checkpoints, restart receipts, rollback, and cleanup remain owned by the guarded campaign-test lifecycle; the enrolled baseline is never modified or deleted.

## Ruler Docket acceptance tests

Natural reconciliation is semantic, not tied to one canned sentence. The tier-two oracle accepts an in-world promise to honor, obey, submit to, or accept the ruler's judgment, including a post-judgment acknowledgement that it is given or heard, when it is paired with an explicit promise not to keep, renew, continue, or pursue the quarrel or a declaration that the quarrel ends here. Explicit refusal or appeal language takes precedence over those acceptance words and must still fail closed.

An active noble audience owns its automation transcript, scene-preparation error, expected-speaker count, and bound action routing ahead of any retained petition calibration ViewModel. This prevents stale petition state from hiding a completed provider turn or receiving an unrelated noble judgment. Each natural question owns a monotonic send revision; the client counts a speaker only after the visible reply and structured turn are applied. The harness requires the matching completed revision, full persisted line count, an exact player-role line in the authoritative matter conversation, cleared busy state, and normal production decision readiness before invoking judgment. It never treats a presentation-formatted UI speaker label as persisted player identity, and timeout evidence reports each gate independently.

For Noble Docket language acceptance, a nonnegative `expectedAcceptanceTier` is also the explicit provider-required gate. The audience tracks provider and fallback replies independently for opening, natural conversation, and closing. A fallback cannot be reclassified as provider evidence merely because its visible wording is safe or the mechanical judgment succeeds; opening and natural-turn fallbacks block judgment, while a closing fallback fails the disposable case and requires checkpoint restoration.

Noble-matter profiles use the same guarded `reign_start_ruler_docket_test` boundary. They may adjust only the run-selected participants' relationship eligibility, add one run-owned matter, and then invoke production audience and judgment controls on the exact armed Current. Marriage, divorce, murder, execution, reversal, investigation, and Court-stay cases require restore-isolated disposable branches. Murder preparation merely reserves a protected victim and four suspects; only opening the production hearing may commit the native death, exactly once. The harness cannot advance time, save, infer disposability, target the baseline, invent evidence, bypass custody, or turn provider prose into mechanics. Structured evidence requires a discoverable id and an exact visible supporting quote; acceptance tiers additionally require natural in-world language that conveys resistance, guarded obedience with a continuing grievance, sincere reconciliation, or wholehearted acceptance without resentment. The persisted transcript is returned verbatim with speaker and role attribution and a deterministic verdict on every non-player line. Provider stdout and stderr are decoded as strict UTF-8 rather than the ambient Windows console code page; malformed bytes fail the connection and visible mojibake cannot be accepted or repaired after persistence. NPC-visible text that mentions tiers, percentages, scores, relationship values or losses, penalties, or game mechanics fails closed; ordinary in-world dates, sums, troop counts, survey ordinals, and distances are not treated as hidden statistics. Unsupported terms and nonparticipant identities are discarded. Each unique fixture run deterministically chooses among the distinct participant sets returned by a bounded production day/slot search and reports the available count plus participant identity and demographic metadata, so release evidence can reject repeated reliance on one convenient pair rather than trusting slot numbers alone. Royal Proclamation records exact text with immutable/no-mechanics metadata only after the guarded phase reaches sovereign Court access.

For a committed Side-B divorce denial, the native observer treats preservation as mandatory even though no divorce mutation term exists. It identifies the exact `principal_a`/`principal_b` pair and requires `nativeFamilyState.required`, `deniedDivorcePreservationRequired`, `reciprocalSpouseStatePreserved`, and `consistent` to be true; any family mutation term, missing hero, or one-sided spouse link fails the evidence. The separate reputation oracle must also prove that neither spouse received a judgment-correlated `divorcee` tag.

`reign_get_ruler_docket_test_manifest` is read-only. `reign_start_ruler_docket_test` requires verification control, exact confirmation, and an armed campaign-test run; MCP derives the exact run-owned `Current`, and the client independently compares Bannerlord's active save before every phase. The petition fixture may create only the bounded settlement need and resource capacity required for the selected petition on that disposable save. It derives the petition through production eligibility and term logic, opens the production Court Petition movie, and invokes the same bound ViewModel command as the visible player control. `petition_insufficient` removes only the selected run-owned direct resource after opening, records the still-pending/no-effect assertion, and then refuses the fixture for UI cleanup. `petition_invalid_identity` replaces only the run-owned petitioner id and permits only the production technical-return control, with no decision or effect. Expedition-return preparation can touch only the active soldier expedition owned by the supplied fixture run. Its optional fallback exercise replaces only that disposable expedition's original-owner and original-capital references with impossible run-scoped identifiers; it cannot alter settlement ownership, choose a return garrison, advance time, or mark the expedition returned. Verification accepts only the production return state, exact manifest casualty/survivor accounting, valid kingdom garrison, history, and completed commitment after guarded campaign advancement. Chancellor fixtures require a vacant office, use a production appointment seam only on the disposable Current, and cover zero inactive pay, full active pay, unpaid automatic inactivity, suppression, deferred scheduling, eligibility, and validated dismissal. Emergency acceptance uses a non-serialized boolean captivity override only to enter the unchanged production selection and handoff lifecycle; it cannot nominate the successor or satisfy world-history/private-memory delivery, both of which require actual receipts. Legacy migration creates only run-owned placeholder records on the disposable branch. Natural-soak phases only mark and inspect; guarded campaign-test owns the 7-60 day advancement. The harness cannot advance time, create or name a save, or turn an AI prompt override into a mechanical pass. Guarded baseline rollback owns fixture cleanup.

## Kingdom Event launch-acceptance tests

`reign_start_kingdom_event_test` requires verification control, the exact fixture confirmation for destructive profiles, and an exact armed campaign-test run. The MCP derives the run-owned `Current` through `RequireArmedOwnedSaveAsync` and passes it as `expectedSaveName`; the client independently compares it with Bannerlord's active native slot before forcing, observing, marking reload evidence, organic preparation/verification, or cleanup. A name prefix is never authorization. The v2 manifest limits ordinary acceptance to 15 once-per-archetype production forces plus six representative target-order additions. `no_target` may request only an exact absent kingdom and must prove no marker or consequence changed. Organic preparation fails closed without creating a ledger or arming an override while campaign initialization, autonomous-world preparation, native-save quiescence, or Save Sync alignment is pending. It then observes the unchanged one-percent production threshold and normally records the predicted native day. Its optional acceleration is accepted only by `organic_prepare`, is non-serialized, bound to the exact fixture run and next disposable-save day, retains the original stable roll, and is consumed before selection/effects; verification proves it is gone and same-run cleanup clears it if unused. Guarded campaign-test control owns all time advancement. `save_prepare` records only an exact feature fingerprint and game instance. It cannot create or name a save, and `save_verify` requires a different game instance after a guarded Current checkpoint/restart. Cleanup removes only the exact run-owned feature ledger; guarded campaign-test cleanup alone may delete the inactive Current and can never delete the enrolled baseline.

## Government feature-harness tests

`reign_start_government_test` requires verification control, the exact confirmation `start Reign government test on disposable save`, and an armed campaign-test enrollment. The MCP derives the exact run-owned `Current` through `RequireArmedOwnedSaveAsync`; callers cannot nominate a save, infer disposability from a prefix, or target the enrolled baseline. The native client independently compares that exact name with Bannerlord's active save before every profile. The harness owns no save, time-advance, restart, or deletion operation. Synthetic route records are exact-run in-memory fixtures and are removed in `finally` blocks. Native authority, lobbying, resolution, action-pressure, consent, UI, and rebellion acceptance uses restore-isolated campaign-test checkpoints. `save_prepare` returns a stable feature fingerprint and game-instance ID; campaign-test control owns checkpoint/restart, and `save_verify` requires a different game instance plus the exact caller-carried fingerprint. Government tests never spend native Influence, mutate Public Standing, or directly rewrite rebellion probabilities.

The autonomous release layer first uses `reign_prepare_government_certification` to bind one run id to the immutable enrollment, source/client/server/provider/catalog/Bannerlord fingerprints, and existing workspace-contained validation/deployment evidence. `reign_start_government_certification_case` accepts only manifest-owned `GOV-*` cases and again proves the exact armed Current. Its 36 dialogue cases open ordinary production Individual Chat and send the fixed plausible player utterance; fixture commands may set only bounded relationship, party, authority, history, resource, decision, or pressure prerequisites and observe the result. They cannot submit `consent_government_reduction` directly. The exact-command clean-room authorization described above prevents prior certification conversations from contaminating an independent case, and readiness requires its active receipt. A supportive case may define one second natural player turn. The server skips that queued follow-up only when the immediately preceding completed production `send` exposes `consent_government_reduction` in its structured `queuedDialogueActions`; it never matches reply text, scans older turns, or creates the action. Player/NPC text describes a small or limited change in government control and never exposes the hidden numbered authority scale. A no-consent case that creates any tag is a release-blocking false positive. Eight culture-institution cases use eligible native kingdoms for the seven installed culture forms and a bounded non-serialized forced fixture for the fallback Council of Estates; the fallback state fields are restored in `finally` and no campaign culture is changed. Every native case declares case-specific required assertions, so manifest ownership or a merely loaded realm cannot satisfy readiness. The external-evidence recorder accepts only an existing workspace file, exact SHA-256, case id, allowlisted evidence kind, concrete review note, and explicit confirmation; it never creates the evidence or marks a native result passed by itself. For an audited harness/readiness-only repair, `reign_carry_forward_government_passes` can copy only explicitly selected unaffected reports after revalidating guarded enrollment, current assertions, real production dialogue sends and clean-room receipts, contextual quality, no direct bypass, required external evidence, and durable report provenance. It never contacts Bannerlord or mutates campaign state, and affected or invalid reports remain ineligible. The readiness evaluator requires all 85 instances, 100% action/safety correctness, at least 95% contextual quality, zero direct-action bypass, both persistence pairs, the externally advanced 210-day soak, exact validation/deployment, and cleanup. Campaign-test control remains the sole owner of time, save, restart, restore, and checkpoint deletion; the baseline remains immutable.

For post-restart Government soak verification, the caller must nominate the retained preparation live-run id and exact 64-character fingerprint. The MCP reads that report through the normal live-report API, requires completed `government_soak_prepare` evidence, validates every bounded count and the fingerprint, and passes only that retained snapshot to the client. The client rejects missing, malformed, or mismatched evidence. This recovery path cannot advance time, create a replacement baseline, nominate another save, or mutate the protected campaign.

## UI interaction calibration

`ui-action` and `ui-snapshot` are accepted only for the exact target opened by the current calibration session; a native snapshot must also match the exact active native target before the allowlisted movie can be exported. Ordinary standalone opens fail closed after a bounded 15-second native open-and-layout deadline. War Council alone receives 120 seconds for cold loading of its lossless 16K tiled map, but it must still prove the target is open and complete at least two native late-update frames; the larger bound does not relax readiness or widen another target's deadline. Individual Chat actions and close additionally require its provider-free calibration view model, whose send path returns before any provider or campaign write. Native Conversation portrait actions and close require a fresh per-open token captured by the exact production mixin; stale or ordinary conversations fail closed, close ends the conversation only while that exact token remains bound, and token-specific probe cleanup always runs in `finally`. Continue is intentionally absent because it can execute dialogue consequences. Native snapshot attachment authorizes only one exact cataloged base movie for the lifetime of the current headless native target, and the authorization is cleared on close or failed open. An owner-scoped automation lease is acquired before standalone or native UI routing and released after failed open or close, so it cannot disable a separately enabled supported calibration run. Direct actions are VM-state corroboration, not proof of XML bindings, keyboard focus, hit testing, Harmony click routing, overlay interception, or rendered geometry; those require visible native input and screenshot/widget-bounds evidence. The calibration lifecycle closes only its own fixture and never advances or saves the campaign.

### Court-life audiences and public patronage subjects

Family transition assertions require the prepared exact campaign/timeline/ruler/member/visit baseline and a quiescent server mirror. Dismissal must be new for that visit with a one-point counter increment; neglect must newly cross the patience threshold. Reconciliation requires retained, successfully observed neglect for the same visit followed by clear state. Prior unrelated state and an initially clear member cannot pass, and snapshots never create transition proof. These observations do not alter attention, relationships or campaign time.

The `court_life_*` profiles retain the Ruler Docket armed disposable-save, verification-control and exact confirmation gates. `court_life_prepare` adds only the exact run-owned production matter; optional fixture gold and town/castle loyalty are actual bounded native values on that disposable save. Eligibility, history, personalities, actor authority, decision receipts and delivery effects are never manufactured by the language harness. Natural turns use the visible production VM input/send binding, exact matter identity, completed provider replies and persisted player text. Selection and confirmation remain separate production controls. Clock and reload profiles observe only; campaign-test owns advancement, quiescence and saves. No cooperation override changes these assertions.

`GET /court/patronage/public-history` is a read-only production route, not a fixture writer. It requires an explicit campaign, timeline, kingdom and finite world day, reads at most 200 completed native candidates, checks actual global or own-kingdom public knowledge availability, and returns at most eight sanitized subjects per typed context (`reign-patronage-public-history-v1`). Participant-only evidence, premature news and private Court History are excluded. Client caching is bound to the campaign state instance and scope and rejects rollback-time or stale-load responses.

The optional `courtLifeFixtureAgeRulerTo34` flag defaults to false and applies only to `court_life_family_fixture`. With explicit user authority to adjust the disposable test ruler, true sets an existing adult male ruler aged 18 through 34 to age 34 before household creation. The fixture records prior/resulting age and completion, requires the same flag on retry, and never repeats a completed adjustment, rejuvenates an older ruler, converts a child ruler, or advances world time. All empty-household, exact armed Current, confirmation, marriage eligibility, and separate named-baseline promotion gates remain in force. The default still requires age 32 or older and preserves age.

Household fixture origin selection uses the ruler culture native LordTemplates plus matching adult female noble source characters, requires native CreateSpecialHero to clone its source without changing the original hero, and records template/culture/origin-clan IDs. It prefers a same-culture clan in the player kingdom, then a same-culture clan in another kingdom at peace with it; hostile kingdoms remain excluded and native marriage eligibility is still required. Missing template and missing clan are reported separately before creating a wife.

Court Life captive setup: `courtLifeFixtureCaptiveHeroId` defaults empty (custody unchanged). Read `court_life_preflight.captiveInventory` for up to 64 sorted eligible IDs plus count/truncation, then make a quiescent guarded checkpoint before using an exact ID with International `court_life_prepare` and a RequiresCaptive template. Existing armed run-owned Current and fixture confirmations remain required. Candidates are actual adult foreign partyless nobles, excluding rulers, family, prisoners, assignments and quests; their peaceful kingdom must retain another eligible envoy and have no existing player-held captive. A selected hero participating in a pending matter is excluded; an unrelated pending matter in that kingdom does not exclude the hero. Preflight also reports exclusionCounts and up to 64 sorted existingPlayerHeldCaptives. Native TakePrisonerAction establishes custody; normal production eligibility and the exact captive binding must then pass. Capture intent and before/after custody are persisted as `reign-court-life-custody-v1`. Partial failures block reuse of that fixture ID: inspect the receipt and restore the guarded pre-fixture checkpoint before retrying; never save over that rollback point with a partial fixture. No payment, acceptance, pressure, war, time advancement or save is injected. International `nativeState.international` reports exact captive custody, foreign ruler gold and the player/foreign kingdom pair with `warStateKnown` and nullable `atWar`. Re-prepare save fingerprints after this evidence expansion; prior fingerprints are not fresh persistence proof. Native capture, real conversational payment/release and reload acceptance remain required in addition to catalog validation.

Court Life ruler recognition uses `formal_court_audience` only when structured courtLifeContext matches current authoritative native facts: campaign/timeline, actual speaker and ruler, ruler office and kingdom, capital/host settlement, observer location and observation day. The generic identity adapter preserves that context; formal presentation may upgrade an existing unknown acquaintance, but ordinary visits, free-text titles, another speaker or capital, stale observations and changed office/realm cannot. Existing `contracts.sovereign_identity` and `pipeline.identity` checks include `court_audience_*` qualification boundaries and actual prompt-adapter persistence through a fresh database connection. Native acceptance must inspect the real foreign envoy/visitor opening and its exact identity audit. Preserve previously verified ruling effects instead of repeating the judgment to retest recognition.

Court Life save comparison retains authoritative ages and hero existence in `nativeState.familyNativeAges`. When that field exists, the fingerprint excludes only the family mirror observation `worldDay` and cached `profile_json.age`; every other family value remains included, and raw evidence is unchanged. Isolated contracts require cached-age refresh to compare equal, while native age, child status, dismissal and row-content changes still fail. Identity-keyed members and visits remain order-independent. Prepare a new fingerprint before verifying in another game process; previous failed fingerprints are not upgraded or silently accepted.

Production Test Lab `POST /tests/run` in forced canned mode uses `BuildTestMessages` and the same dialogue/event builders as game requests. It now also captures complete evidence labeled `captureStage=test_lab_assembly`; that label never claims a provider call. The existing MCP does not wrap this older Test Lab endpoint, so the before/after deployment comparison uses that documented API with fully supplied snapshots, a task-owned `verify_prompt_comparison_20260908` campaign namespace, no snapshot paths, `live=false` and `canned=true`. The comparison is restricted to the successful prompt phase, not the later action-normalization result of a deliberately action-free canned reply. Full baseline messages are available through this test route; the original in-game audit remains truncated. The installed server is verified after deployment through the same route and through isolated Verification Lab checks.

The focused quick/offline prompt_efficiency suite explicitly executes contracts.dialogue_prompt_budget and pipeline.prompt_caching. It measures assembled commoner/noble system prefixes (45000-character bound), not a sum of selected template files. A catalog-only result is insufficient proof. The existing pipeline suite still runs the same caching contracts as part of its wider coverage.


Clan Accords: `runtime.clanAccords` reports availability and pending saved synchronization. The campaign-test quiescence gate requires schema `reign-clan-accords-runtime-v1`, available=true and pending=false before saving. `ui-open --target clan-accords` uses the existing guarded UI calibration route and requires a player ruler. Offline ledger, consent and isolated database replay checks run in world_diplomacy; actual native effects, dialogue, save reload and rendering remain post-deployment acceptance on the exact enrolled disposable Current. Deployment is on hold pending the user's approval.

Wanderer population: reign_get_live_test_status exposes runtime.wandererPopulation (town counts, coverage, pending contact checks, and at most 32 local identities). Safe current-town menus are eligible and prioritized for top-up; active missions and sieges remain mutation-free. The existing POST /dialogue/history wandererContactOnly branch is observational, requires exact campaign/active timeline/hero/player IDs, and does not construct profiles or mutate game state. The wanderer_population quick/offline suite is provider-free; native acceptance still requires exact enrolled disposable Current and deployment authorization.

The existing live-test scenario operation clan_accords_test accepts profile contracts only and emits reign-clan-accord-native-contract-v1 with 38 real-native math and isolated SavedState JSON assertions. It uses the existing armed bridge and main-thread dispatcher, with no provider call or live campaign mutation; it cannot advance time, save, create an accord or prove an actual fresh-process save roundtrip. This runtime check remains after authorized deployment. Read runtime.clanAccords for pending synchronization.

## Scoped provider-free interface evidence (2026-09-08)

The rendered-preview `--targets` option selects exact catalog interfaces, declared states or native augmentations and rejects unknown, empty and duplicate IDs before browser launch. Full catalog inventory and per-case binding/asset checks remain enforced. A `partialScope=true` report never establishes full-catalog completion or native acceptance; aggregate readiness remains `not-run-partial-scope`. Keep scoped reports outside the global readiness discovery tree. The option grants no provider, campaign, deployment, installation or native capture authority; existing confirmations and disposable-save protections remain unchanged.

## Character narratives and interests (2026-09-09)

`reign_inspect_character_narrative` reads the complete base/effective narrative, defining trio, retained development projection and full-roster diversity counts through `GET /characters/narrative`. It never materializes a character or calls a provider. Supply a hero ID and an optional campaign ID; absent campaign reads the shipped catalog. Missing data is explicit.

`reign_author_character_narratives` uses the installed Character Construction provider/model, with exact confirmation `author character narratives using configured provider` and trusted verification/offline permissions. It runs only a successful Release validation artifact, without a listener, in the ReignValidation database and a build-root-only runtime. Batch size is 1-25, offset selects catalog order, and authoringRunId is a simple confined task name. One writer holds the run lease. Each profile resumes validated life/interests stages; three failed attempts stop that stage without generic substitutions. Provider settings are read without modification. No campaigns, deployed files or active catalog are changed. Output and evidence: `.codex-build/reign-mcp/narrative/<authoringRunId>/batch-<offset>.json`. A catalog is assembled only after all profiles pass. Cancelled batches retain completed profile checkpoints. Authoring can incur provider usage; offline contracts cannot invoke this path.

Run `character_narrative` through the artifact-bound quick Verification Lab and manifest-selected validation. The contract covers weighted influence, stable semantic selection, complete generation, privacy/relevance rules and gated development. Provider dialogue behavior and native fresh-campaign save/reload acceptance remain separate requirements. Earlier campaigns retain v4; only the native persisted fresh-campaign marker enables v5. Deployment remains a separate stopped-runtime operation using successful validated artifacts.

The `operation` parameter accepts `author` (default) or `evaluate`, with the same exact provider confirmation for both. Authoring uses at most three independent profile workers; stages within each profile remain sequential, batch writers hold an exclusive run lease, and completed checkpoints are reused only when current native facts, personality, library and blueprint fingerprints match. The staged v5 catalog references one validated `narrative/profiles/<hero-hash>.json` per character and assembles only when the entire roster is complete. `evaluate` uses the saved Dialogue model plus an independent Character Construction judge for three ordinary scenarios and three 1/5/10 interest contrasts per selected character. Case replies, judgments and fingerprints are retained under `evaluation/`; structural, privacy and canon checks require 100%, behavior at least 95%. This is provider evidence, not native game acceptance.

The narrative library now contains 664 hobbies across 32 families and 1,904 other concepts. Children under twelve use authored play, attachment and learning concepts; native age governs voice and interpretation. Adult options include status-seeking, traditional, selfish and conflicted motives as well as generous ones. Native rulers receive an additional provider case with a consuming kitten interest during an urgent military supply discussion. Previous failed provider evidence is retained before a retry.

The provider-free `character_narrative` suite emits `data/tests/verification/character-narrative/control-center.html` and `fixture.json`. Run `ReignServer/src/Modules/Characters/narrative-editor-ui-contract.mjs` with that fixture directory and an evidence directory, using bundled Node/Playwright (`REIGN_PLAYWRIGHT_MODULE` may point to the installed package). The harness intercepts every browser request; it does not open the installed Control Center or start a listener. It checks 1672/1024/600 widths, current/expanded/long-text/history states, first/last cards, escaping, editing, add/remove, private history labels, hidden construction metadata, and legacy empty state. Reports and screenshots are written only to the selected evidence directory. This web-editor change adds no native shell or portrait aperture; native campaign acceptance remains a separate deployment follow-up.

Government hearing_compose preserves campaign-test authorization and native exact-save verification. It accepts only a bounded natural-language message for the exact already-open interactive hearing. It sets unsent UI text only; native Speak owns submission. Other Government profiles reject these parameters. No case creation, selection, native actions, provider calls, or votes occur in the compose operation.


Character narrative construction (`single_call_v1`) keeps permanent IDs and ratings local, normally uses one compact provider request, and checkpoints valid sections for bounded targeted repairs. Batch evidence adds providerCalls/elapsedMs/reused; attempts add durationMs/requestCharacters/responseCharacters/requestedSlots. Existing authoring confirmation, isolated artifact root and no-deployment gates are unchanged. Initial roster authoring belongs to the implementation task, not the game provider.


Character narrative dream compatibility is local, personality-aware selection before the single construction request. The reviewed dream-compatibility.json covers every adult dream; low compassion cannot receive a genuinely philanthropic dream and low Court honor cannot receive an integrity-based dream. The character_narrative offline suite checks exclusions, supporting-trait weighting, trait-band boundaries, policy coverage, and zero incompatibleDreamCount in the shipped roster audit. This adds no provider-consuming operation and does not change deployment or campaign gates.


Adult desires and values also use reviewed personality eligibility from ReignServer/profiles/narrative/concern-compatibility.json. Required traits must exceed40; requireNotHigh excludes an opposing trait in the61-100 high band. Neutral craft, comfort and autonomy remain available. The existing offline character_narrative suite covers care/honor distinctions, upper and lower boundaries, mixed-personality populations and complete catalog rule coverage. The roster audit adds incompatibleConcernCount and bounded incompatibleConcerns item references alongside the existing dream-only evidence. The initial roster must report zero incompatible concerns. This is local selection and read-only audit; no new provider, runtime, save or deployment authority is introduced.

Enrolled `court` and `family-chambers` UI setup preserves paused native time.
The guard accepts existing top-level campaign-test fields or nested enrollment
with `campaignTestRunId` and `disposableSaveName`; it does not grant enrollment
or bypass any campaign/save authorization. `docketClock` records the before/after
day and pause state. Native menu exit and outgoing view cleanup must not restore
running time. Opening-time drift fails; advancement stays with the guarded
campaign controller. Native acceptance must check an enrolled Family Chambers
opening from settlement waiting, exact selection, and an unchanged campaign day.


Narrative factual prerequisites are declared in ReignServer/profiles/narrative/fact-compatibility.json. Kin-dependent concepts require known siblingIds, olderSiblingIds or livingParentIds; an absent field is unknown and selects another concept, never an invented relative. Premade enrichment uses matching native parent IDs, ages and living status without changing the source facts. Dynamic inputs with no structured kin evidence retain the many non-kin concepts. Explicit minimum ages prevent adolescent past apprentice-supervision claims. Existing character_narrative contracts cover unknown and younger-only siblings, known older kin, living-parent and mentorship-age boundaries, and immutable enrichment. The complete roster audit adds incompatibleFactCount and bounded incompatibleFacts; require zero. No provider calls, database writes, native changes or new authority are introduced.

The narrative fact policy also accepts an optional maximum age. Routine requests to be picked up when tired are limited to age six and younger; older children retain the broader child concern pool. This is an offline eligibility rule with no additional provider or campaign authority.

Court Life snapshot diagnostics (2026-09-09): `court_life_snapshot` retains the exact visible fixture matter and armed disposable-save gates, but can capture a resolved or busy audience. A read-only picture requires neither `AutomationCanDecide` nor completed provider replies and never proves a completed turn, acceptance, or decision authority. `court_life_converse` and `court_life_choose_confirm` retain their full readiness gates. Use `court_life_observe` with `resolved` for resolution receipts and the guarded save/verify pair for persistence. Native acceptance captures the resolved audience before it closes on the next distinct untested encounter, retaining the original ferry failure. Resolved matters cannot be reopened through production eligibility; never replay a judgment or relax eligibility to obtain a picture. Family rule coverage includes patience 0, 1, 10, 11, 40, 41, 50 and 100, zero-before-first-dismissal, and the production missing-trait default of 50; native family transitions remain separate evidence.
# Codex image verification boundary

`reign-codex-image-compatibility-v1` remains behind existing Verification Lab controls and explicit subscription-usage authorization. `codex_images` consumes usage only when explicitly selected with the live-llm tier; cap initial compatibility at one sample and never exceed three per pass. Quick/offline checks consume none. The provider rejects adult profiles before reference writes or runtime startup and uses official ChatGPT authentication without reading tokens or API keys. Each image has a bounded job-owned helper, ephemeral directory, disabled inherited MCP/shell/agents/network, strict PNG and output-containment checks, and cleanup after helper shutdown. Cancellation must not stop dialogue's shared Codex process. Human visual acceptance is separate from structural success; no campaign or save mutation is authorized by this suite.


## OpenRouter providers (2026-09-09)

`reign-openrouter-providers-v1` adds independently saved NanoGPT and OpenRouter chat credentials and model selections while retaining Codex and custom APIs. `openRouterApiKey` is shared by OpenRouter chat and all four image profiles; blank/masked/null submissions preserve it, explicit clearing never falls back to another key. Legacy API keys migrate only to their endpoint owner once. `llmModelProfiles` stores only the twelve allowlisted model fields per provider; active flat model fields remain the runtime contract. OpenRouter uses its fixed HTTPS endpoints, prefixed chat model IDs, native reasoning controls including direct-selector exclusion, and reference-image requests to `/api/v1/images`.

Use the existing artifact-bound `interaction_architecture` offline suite and `contracts.image_generation_profiles` (included in quick `contracts`; the focused `codex_images` quick suite also includes all image adapter contracts). Injected image transport proves all five catalog models across four profiles, exact source reference bytes, endpoint/key selection, empty/error/echo rejection and timeout/missing-key behavior. `OpenRouterCatalogTests` checks catalog/help drift. No new tool, live profile, confirmation phrase or campaign mutation route is introduced. The existing GLM cache probe is enabled only for NanoGPT in the UI; its backend rejects non-NanoGPT endpoints before making a provider request.

The architecture suite emits `control-center.html`. Run `chat-provider-ui-contract.mjs` with that HTML path and an output directory; run `portrait-profile-ui-contract.mjs` on the same fixture. Both live under `ReignServer/src/Modules/` (Platform and Portraits respectively) and intercept all requests. The chat matrix covers four providers at 1672/1024/600, draft switching, all model roles, masked-key preservation and save/reload; the image matrix covers NanoGPT/OpenRouter/AtlasCloud and normal-only Codex. Reuse `control-center-ui-contract.mjs` for full-page palette/navigation regression. MCP has no browser-preview tool; these documented shared fixture harnesses are the fallback. Native Bannerlord, portrait-layering and approved-raster fidelity are not applicable to these web form changes. Provider-backed output still requires a configured OpenRouter key and separately authorized paid smoke testing.

Image presets are exact matches verified from `GET https://openrouter.ai/api/v1/images/models` on 2026-09-09: `openai/gpt-image-1`, `google/gemini-3-pro-image-preview`, `bytedance-seed/seedream-4.5`, `bytedance-seed/seedream-5-0-pro`, `x-ai/grok-imagine-image-quality`. GPT Image 1.5, WAN and Flux Kontext exact variants were absent and are not silently mapped to another model. Existing AtlasCloud/NanoGPT catalogs remain intact. See https://openrouter.ai/docs/guides/overview/multimodal/image-generation for request capabilities.

## Codex performance controls and isolated comparisons

`reign-codex-performance-report-v1` is cataloged under `codexPerformance` in `reign.testing.json`. Run `reign_run_offline_verification` with the exact successful Release validation run ID and suite `codex_performance` for injected runtime, conversation, settings migration, dispatch-cap, and report contracts. Run `interaction_architecture` for the shared Control Center fixture, then its three browser contracts at 1672, 1024, and 600 pixels with all traffic intercepted. This is a web control change: native artwork raster/fidelity checks do not apply.

The optional provider comparison is explicit-only: `reign_start_verification`, tier `live-llm`, suite `codex_performance`, the usual `start Reign verification` confirmation, and `codexPerformanceJson`. The JSON must name the exact model, caseIds, full baselineOptions and variantOptions, the single changedOption, a mandatory totalProviderCallCap from 1 through 1000, and confirmation `run bounded Codex performance comparison`. The run-wide cap covers every physical turn/start across primary generation, repair, tier retries, probes, judges and repeat passes. Discovery and thread creation are not generation calls. Cancellation/closed budgets prevent later dispatch. The suite uses isolated Verification Lab campaign data, preserves the selected live provider/settings, and is never included implicitly in aggregate live suites.

Model checking is separate: POST `/api/codex/models/verify` requires `maxRequests:1`, `bounded:true`, and confirmation `verify one Codex model request`. The Control Center explains usage before confirmation. Discovery remains free. A timeout remains inconclusive; request-model echoes do not establish actual served model identity. No user entitlement or shared Codex configuration is modified.

Reports separate baseline/variant latency and quality outcomes, retain unknown usage/fingerprints as unknown, and keep clean attempts, successful repairs, accepted repair overrides, and unusable results separate. Runtime/conversation diagnostics contain no reasoning or rejected draft text. GPT-5.4 access, actual Fast-mode service, and live roleplay quality require later user-authorized tests. See `docs/agent/CODEX_PERFORMANCE.md` for controls and limitations.

The provider comparison plan also requires `validationRunId`: the server checks its own assembly SHA-256 against that successful Release run's exact artifact before dispatch. Source and validation fingerprints come from that report; they are never inferred from model settings. Supply this ID inside codexPerformanceJson (or the CLI `--codex-performance-plan` JSON file). Ordinary quick/offline contracts do not require or authorize a provider plan.

Injected Codex contracts are CLI-only: use artifact-bound `reign_run_offline_verification`. The listening server refuses this injected suite so its test transport cannot intercept gameplay requests. The dedicated Codex browser script wraps the shared provider script; run that wrapper once, then the separate full Control Center matrix.

The MCP supplies its trusted workspace as `validationWorkspaceRoot` so an installed server can locate the exact report. CLI comparison plans may supply that exact Reign workspace explicitly. The report verifies the running assembly hash and records the actual runtime executable hash when its path is known; accepted replies are retained only for later human roleplay evaluation.

Codex comparison fixtures must identify a shipped character or include a completed character snapshot (constructed, traits, and a complete narrative when using narrative v5). Each isolated arm preserves the supplied character stack; fixture personality authoring is not part of the latency comparison. Both arms must keep the same reasoning mode and schema version. Served-model consistency uses authoritative runtime evidence; a request-model echo leaves verification unknown.
# Paired repository source boundary

`reign.repositories.json` permits only five declared source directories from the sibling `ReignServer` checkout. Source search, project discovery, coverage audit and fingerprinting follow those exact verified junctions. Other reparse points remain excluded. Validation checks both repositories with enforced hygiene and requires their exact authorized fetch/push origins. Public server builds leave private verification-source copying disabled; the private integration build opts in, and release packaging excludes the entire verification workspace. Installation credentials travel through child-process environment or per-user protected storage, never PostgreSQL command arguments. See `../../docs/agent/testing/RELEASE_PACKAGING.md`.

## Release runtime isolation



ReignServer is Linux-only. Canonical server verification uses WSL on Windows, an isolated ReignValidation database, run-owned data and a bounded Linux process group. See `../../docs/agent/testing/RELEASE_PACKAGING.md`.
