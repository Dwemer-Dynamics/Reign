# Relationship throughput implementation — 2026-09-13

## Deployed package and native findings — 2026-09-14, 03:23 America/Chicago

The user-authorized coordinated deployment with **Investigate unnatural game counting** is complete. Final `changed` Tier 3 Release validation **20260914-073242-8401bb5d** passed all eight selected operations in **269.879 seconds** (565 MCP tests, 107/107 quick checks, enforce hygiene zero issues). Source fingerprint: `6dfb1221f530097701f61243af36c9a90d4c75d89015802785b2352b393adaa6`. Validation and hygiene receipts are in `.codex-build/reign-mcp/validation/20260914-073242-8401bb5d/`. Final dialogue/gift proof **verify-1789371460461-b49c18a1** passed **146/146** focused cases on that exact artifact.

Full 100-day replay **verify-1789371616610-6004965c** completed in **676.972 seconds**. All **38/38** focused contracts and every daily/final semantic comparison matched, including **129 conceptions and 129 commitments**. Candidate p95 **1,540.6805 ms**, original-read-route p95 **1,924.0997 ms**, maximum queue wait **15 ms** at independent two-second arrivals. **The performance suite remains failed** because the strict subsecond target is unmet. This is a synthetic workload and an original read path in the same executable, not the historical 18–20-second executable or captured native input.

Canonical Apply **20260914-075352-200-9a8a2072** copied and verified 13 changed files with 1,140 already matching. Backup/journal and receipt are under `.codex-build/relationship-regression-20260913/coordinated-deployment/`; installed server SHA-256 `6964b523ec4e8264178bd517b8bd8e843017086c6cc384bfe8a99471aedd7170`. Settings hashes were unchanged through installation and visible startup. HTTP 5101, the dedicated Control Center and vector worker were verified healthy. The database remains DwemerAI4Skyrim3/PostgreSQL 5432; no migration, TLS or network change was made.

The guarded native check reused the exact enrolled `ReignTest_RelationshipThroughputIn_8db15d0b_Current` save. The first four-day request overshot by **1.5134 days**, producing five measured relationship days at **1,557–1,574 pairs/day**. Those days averaged **3,298.6 ms inside the processor**, **1,484.4 ms waiting for campaign access**, and **4.8503 seconds total worker time** (range 4.1044–5.7351 seconds). Actual native arrivals were roughly 24 seconds apart, so this cannot prove real-data capacity at two-second arrivals. No relationship/native synchronization backlog remained at the drained boundary.

That first native run revealed a deployment coverage gap: `CampaignTestService.RunControllerAsync` selects `<workspace>/ReignBeta/server/app/ReignLiveTest.exe`, not the installed module's controller. Its packaged September 9 copy had SHA-256 `a534e03eacfecc9681dfb855851096d916af9716c1502a9953fb4d556a2e890c`. After all controllers exited, it was backed up and replaced with the exact validated controller (`f705dbb301ac9273c2cb6c1e91d845b4521750151c47ea492f4bed959a373f8a`). No source/build or gate bypass was used; the repair receipt is `coordinated-deployment/mcp-controller-repair/receipt.json`. Old-controller native results do not prove the updated readiness rules. Future canonical deployment should verify the MCP-selected controller path/hash, not only installed artifacts.

With the corrected controller, an already-caught-up stop completed in **18.418 seconds**, including **16.727 seconds** for all eight stable observations. A subsequent fresh one-day advance stopped at **91263.90149** versus target **91263.89337**, but catch-up took **75.272 seconds** and therefore **failed the 30-second acceptance target**. Its overlapping sampled blockers were native relationships 14.858 s, clan conflicts 5.030 s, Court Life 11.487 s, diplomacy 5.030 s, World History 2.030 s, native drain commands 4.112 s and cumulative stable-observation time 42.093 s. Do not sum overlapping categories. The last category includes repeated resets, not a relaxed or lengthened required window. Functional drainage passed with zero failed actions; no save was made. Receipts: `native-current-controller-drain.json`, `native-final-advance.json` and `native-postdeploy-observation.json` under the coordinated-deployment directory.

Remaining launch work is explicit: optimize real-data service and campaign access waits; isolate the measured large-payload transport delay; improve actual native synchronization/refresh completion and reliable boundary stopping while preserving the full gate; capture representative native inputs; prove both fast-forward modes, the 126-day soak and restoration of newly versioned hero observations. The current Court Life flag combines refresh, postings, referrals and receipts; a sampled `internationalPending=true` cannot identify which substage was unfinished. Add bounded substage telemetry and control-flow proof before changing that gate. The generated consolidated report is `.codex-build/relationship-regression-20260913/COORDINATED_DEPLOYMENT_REPORT.md`. Earlier hold/pending notes below remain historical evidence, not the current deployment state.

## Coordinated deployment and transport result — 2026-09-14, 02:03 America/Chicago

The user now authorizes coordinating the active tasks and deploying when ready. This task owns the single shared deployment with **Investigate unnatural game counting**, which owns its dialogue/gift implementation and subsequent validation. Notify the user when the final package is ready for game/server closure; verify fresh stopped state before replacement. Earlier deployment-hold notes below are historical. Dedicated database migration, native acceptance and the performance release target are still outstanding and are not implied by deploying the current repairs.

`changed` Tier 3 Release **20260914-064839-1c9cb5d6** passed all 11 selected operations in **583.138 seconds**: **233/233 relationship**, **62/62 persistence**, **565 MCP** and **11 kernel** checks, with zero hygiene issues. Fingerprint: `296456de2d3ad1529eb976097b25f7241b6837b5acda6babd7f7876c75f336ce`. Report and hygiene files are beneath `.codex-build/reign-mcp/validation/20260914-064839-1c9cb5d6/`.

The extended probe retained at `server/out/data/tests/v/r-971e9e9c/relationship-throughput/transport.json` under that validation compared synchronous and asynchronous scalar requests with the same TLS settings, four write-buffer sizes and eight payload sizes. Both modes show the same approximately **44 ms** large-payload cliff. This rules out switching to asynchronous calls or increasing the write buffer as a demonstrated fix on this machine. Production connection, query, transaction and networking behavior remains unchanged. The exact responsible transport layer is unresolved. The full 100-day conception-bearing v3 equivalence/performance replay will use the final shared artifact; short smoke results cannot replace it.

## Validated handoff and measured transport cliff — 2026-09-14, 01:45 America/Chicago

`changed` Tier 3 Release **20260914-061208-8ecf3472** passed **233/233 relationship**, **62/62 persistence**, **565 MCP** and **11 kernel** checks in **779.843 seconds**, with zero hygiene issues. Fingerprint: `c8ce7a937c85f4b932ec5dc65aad31182fc7d984d0c66c0ce2fd7ba8e527c770`; report and hygiene files are beneath `.codex-build/reign-mcp/validation/20260914-061208-8ecf3472/`. The save task subsequently owned the separately authorized joint deployment of this exact artifact. Its stopped-state check found zero pending/processing relationship inputs in all eight registered schemas; deployment receipts are in `.codex-build/save-investigation-20260913/joint-deployment/`. The visible server was returned healthy on port 5101. This task performed no installation or lifecycle operation.

Five-day v3 smoke **verify-1789367771289-9e81efc4** passed **38/38 focused contracts** and all compared states. Candidate p95 **1,438.5653 ms**, original **1,977.9848 ms**, maximum queue wait **15 ms**, with 1,601–1,641 evaluations/day. No conceptions occurred during those five days, so full v3 conception-bearing replay remains required. The strict subsecond target is still unmet.

The retained `transport.json` demonstrated a repeatable size cliff with TLS enabled: small scalar requests around **0.3 ms**, but requests at least 16 KiB around **44 ms** for 16/64/256 KiB driver buffers. An 8 KiB buffer moved the cliff down to 8 KiB; increasing the buffer did not solve it. This establishes a transport-size effect, not its precise layer. No TLS, listener, WSL or PostgreSQL settings were changed. Simply splitting logical reads would need to preserve snapshot consistency; no such production change has been made. The next diagnostic compares synchronous/asynchronous execution with unchanged SQL/TLS, awaiting validation. Full replay, native time modes, 126-day soak and dedicated-database migration remain pending.

## Replay and transport checkpoint — 2026-09-14, 01:08 America/Chicago

Full v2 replay `verify-1789364746432-a4263024` from `20260914-053234-47adea8c` finished in 803.750 seconds. Its 32/32 focused contracts passed, but replay failed: candidate p95 **2,127.0277 ms**, original **2,697.5926 ms**, maximum queue wait **1,320 ms**. All compared state tables matched except conception commitments, starting on day 1014. Existing commitment rolls include random conception GUIDs in their seed; fresh IDs in each route were different random inputs. This failed run cannot establish equivalence. Replay v3 now captures/reuses original lifecycle conception IDs without changing production randomness, rejects missing/extra/duplicate attempts, and retains raw commitments/reservations. Its three new contracts and full replay are awaiting validation.

Many unrelated scoped reads, including empty standing results, repeatedly spent about 43 ms in execution. Npgsql 8.0.9 already enables TCP NoDelay and the current write/read buffers are 64 KiB. PostgreSQL listens only on localhost inside WSL. These observations suggest a transport/message-size effect but do not establish its cause. A bounded read-only loopback ReignValidation payload/buffer probe is prepared, preserving TLS and all installed/network/database settings; measure it before changing query transport.

Additional unvalidated retry safeguards advance the latest observation day even for unchanged facts, retain the original facts/cursor for recovered partial days, and protect history needed by the earliest pending day. Three new fixtures exercise these boundaries. User gameplay remains allowed during isolated verification; deployment, native testing and database migration are held.

Task: `01a09cb2-e59e-7330-9bee-3ad7d83b31ff`. User authorizes implementation and validation, then a pause BEFORE deployment. Do not replace installed files, provision WSL, switch databases, or run native acceptance on the candidate until that boundary is released.

## Acceptance and evidence

Agreed budget: 100 measured replay days at one day every two seconds, p95 relationship service strictly below one second, no growing backlog; both native fast-forward modes and a 126-day native soak; ordinary quiescence at most 30 seconds including the existing eight observations separated by two seconds. Saving is measured separately. Native acceptance and final database restore/cutover require deployment and remain pending at this handoff.

The prior deployed artifact averaged 4.515635 seconds/day over four native days at 1,593–1,616 pairs/day. It does not establish the new budget. Use `RELATIONSHIP_PROCESSING_REGRESSION_2026-09-13.md` and its retained evidence for the baseline. Do not claim an identical-input comparison from two separately advanced native saves.

## Implementation checkpoint

**User-requested transport pause — 2026-09-13, 21:43 America/Chicago.** No validation/test run remains active. Focused `changed` Tier 2 Release run `20260914-023621-73452500` passed **209/209 relationship tests** in **363.891 seconds**, with zero hygiene issues. Source fingerprint: `d71d27529338d9ff8973572eca4aec81a450790d68d51afb9efb00500296e3f0`. Report and hygiene evidence are in `.codex-build/reign-mcp/validation/20260914-023621-73452500/`; the compact receipt and resume checklist are `.codex-build/relationship-regression-20260913/transport-pause-validation.json` and `transport-pause-checkpoint.json`. The replay harness compiled but has not run. Full scope validation and performance/native acceptance remain pending. Resume only when the user returns; deployment and database migration remain paused.

First canonical candidate `20260914-020500-cd1fc58c` compiled the client, server, live-test controller and verification assemblies; 561 MCP and 11 kernel tests passed; hygiene had zero issues. Product quick verification passed 103/104. Its one failure was a stale Gauntlet inventory assertion expecting 21 prefabs while both current source and catalog contained 22 (the existing Tavern House addition). The assertion was aligned with the catalog's 22 prefabs/21 runtime movies, retaining the other contract checks. This failed report is not a deployable validation receipt.

Second batch adds dependency-scoped popularity recomputation, one expiration/standing reconciliation pass, lazy notable personality expansion, immediate startup/restore recovery with generation changes, and fresh-generation enforcement in the quiescence controller. New offline suites `relationship_throughput_contracts`, `relationship_throughput_smoke` (five measured days) and `relationship_throughput` (100 days) run through `reign_run_offline_verification` at tier `quick`, with its exact successful Release artifact and ordinary isolated-verification confirmation. Smoke establishes functional comparison only; it cannot pass the 100-day budget. The full suite fails if p95 service is at least 1,000 ms or a complete day accumulates in the fixed two-second schedule.

The synthetic replay preserves its exact generated input bytes, campaign ID, seed, original/candidate daily semantic hashes and phase timings under the verification sandbox. The original route restores the previous full lifecycle reads and full notable parsing; the candidate uses scoped/lazy reads. The report names excluded wall-clock/audit fields. This is not a benchmark of the old executable or a captured native campaign. Native capture, both time modes, 126-day soak and final frozen database restoration remain deployment-dependent proof.

Implemented, awaiting validation: batched historical identity preservation with serial oracle and rollback comparison; identity phase timings; co-presence-selected lifecycle loading with separate failed-flirt exclusions; ready-campaign discovery with stale-observation protection and 30-second durable reconciliation; shared database recovery probe; relationship queue age unaffected by retransmission; dependency-aware rollup selection and completion wakeup; uncached compact readiness view and per-condition observation times; native orphan-flag observation recovery; exact clan kingdom/day coverage; Save Sync gate/operation timing and repeated count removal.

Remaining before completion: complete and validate replay/performance evidence, dependency-scoped identity reconciliation and compact personality access where supported by equivalence tests, migration script contract coverage and restoration verification, final canonical validation and artifact handoff. This is a recoverable checkpoint, not a completion report.

## Readiness and diagnostics

`GET /world-test/overview?view=readiness&campaignId=...&timelineId=...` returns `reign_world_readiness_v1`, fresh clocks and gate counters, an observation ID/time and query duration. It bypasses the 30-second historical overview cache. All eight stable observations and native Court Life, Clan Accords, diplomacy, relationship, pressure, conflict and World History requirements remain. `blockedObservationMs` attributes sampled intervals to blockers; simultaneous conditions overlap and must not be summed.

Native targets gain `requires_observation` (default zero). An orphan chemistry flag is represented as explicit observation-required work, even if stored target/observed values match. Native pull must actually observe/apply and acknowledge the current revision before the flag is cleared. Existing flags must not be bulk-cleared as a diagnostic repair.

## Database preparation and deployment boundary

`ReignMcp/scripts/prepare-reign-database.ps1` defaults to read-only `Plan`. Mutating modes require the exact `prepare Reign database: <Mode>` confirmation. The script never switches launchers, overwrites target databases, drops source data, unregisters WSL distributions or controls Skyrim services.

After deployment authorization, provision a clean verified Debian 12 rootfs as WSL2 `Reign`, PostgreSQL 15 on port 5433, and the `dwemer` database owner required by existing runtime checks. Supply the runtime-compatible database password through SecureString, never a report or command line. Rootfs provenance and SHA-256 must be verified before import. Rehearse backup/restore/verification while retaining the source and all filesystem Save Sync archives. Restore only `Reign`, never the Skyrim distro or its unrelated databases. Validation uses a separate `ReignValidation` database.

Deploy the performance artifact first with the existing installed launcher. The source launcher retains `DwemerAI4Skyrim3:5432` defaults until cutover. It supports explicit distro/port environment overrides; configure `Reign:5433` only after that target has been provisioned, restored and verified. Do not mix these deployment stages or change the defaults while a shortcut still uses the source launcher.

Resume recovery (2026-09-13): the user's desktop `ReignBetaServer - Shortcut.lnk` pointed at the editable source launcher, so the premature `Reign:5433` defaults prevented startup. Restored the current defaults and redirected that shortcut to the existing installed launcher (original link backed up under the task evidence directory). Installed EXE SHA-256 remained `2B24114CBB58DD4C96A9830793955FF94CE99EE1627D0FF07AE061369DD23982`; no candidate binary or database migration was deployed. Verified `/health` on 5101 with the dedicated Control Center. The source CLI catch now sets a failing exit code for startup exceptions; previously an unavailable database returned exit zero despite printing a failure.

Five-day smoke on artifact `20260914-023621-73452500`, verification sandbox token `r-c2c12897`: 14/14 new correctness checks and the smoke comparison passed; compared semantic state matched. Candidate p95 1,446.1268 ms versus original-read-path 1,580.8515 ms. This is not the launch budget or a benchmark of the old deployed executable. Full-state hashing between arrivals inflated queue waits to 12,440 ms; the existing verification pruner then removed short-named sandboxes because it compared them with full run IDs. The summary report survived, but detailed input/phase files were lost. Source fixes retain referenced short sandboxes, bound daily comparisons to that day's chemistry/lifecycle rows plus full final state, and add co-present historical pairs, failed-flirt history and multiple personalities to the fixture. Re-run before making a final performance claim.

At final cutover: pause, prove quiescence, save/verify one rolling owned checkpoint within the 15-state capacity, close the visible Reign lifetime group, back up the final frozen source, restore to an empty dedicated target, verify schema/content/ownership/sequence state and snapshot restoration, then install the candidate launcher/configuration and visibly reopen. Keep the source intact. Once target gameplay writes exist, switching to the old source is not a safe rollback; preserve and transfer the newer state first.

## Resumed measurements — 2026-09-13, 22:39 America/Chicago

Focused Release validation `20260914-032652-22030a89` passed 211/211 relationship checks, hygiene zero issues, in 327.090 seconds. Fingerprint `4c1e747bcc0b1fb1fec9cc58188622559d4dc862f16aff539bedd7d0f8fa746a`. Report and hygiene files are in `.codex-build/reign-mcp/validation/20260914-032652-22030a89/`.

Its strengthened five-day replay retained its evidence under `server/out/data/tests/v/r-50f4826d/relationship-throughput/` in that validation folder: 4,305 NPCs, 82,073 historical pairs, 650 groups and 1,600–1,601 evaluated pairs/day. Both routes matched their daily and final compared state. Candidate p95 was **2,387.8584 ms**, original-read-path p95 **3,150.0348 ms**, and maximum observed candidate queue wait **1,320 ms**. The launch budget remains unmet. This fixture includes historical co-present pairs, failed flirt history and varied personalities; its cost is not directly comparable with the earlier weaker fixture. User gameplay was permitted concurrently, so these measurements include possible resource contention.

Candidate mean phases: hero hydration 477.2 ms, relationship-state loading 467.8 ms, finalization 486.8 ms (including flings and popularity), matching 181.8 ms, personality loading 146.0 ms. Automatic five-day decay is explicitly disabled in the daily path; its unused full-table loader is not a cause of these measurements and has not been changed.

Next candidate caches only parsing of exact stored hero documents (bounded LRU, independent deep copies, database still queried each day), and scopes the second transactional lifecycle/chemistry/native read to matched and independently co-present pairs. The original replay route keeps the full reads and uncached parsing as an oracle. Additional phase counters distinguish lifecycle, chemistry, targets, standing, provenance and telemetry costs. These changes and the new cache/workset contracts are awaiting validation. The retained report, input JSON and append-only samples are the evidence authority; no candidate files have been deployed.

## Further validation and save diagnosis — 2026-09-13, 23:27 America/Chicago

Read-cache validation `20260914-034057-c91b925e` passed 213/213 relationship checks in 317.823 seconds, hygiene zero, fingerprint `a68fc0d121292e5f82a344e8d948182ff9549cc4b6313d74d6febf44ed25decc`. Five-day smoke `r-e6eba641` matched its compared states but measured 2,051.2296 ms p95. Full 100-day replay `r-5a424391` took 516.494 seconds and **failed**: candidate p95 2,046.4845 ms, original-read-path p95 3,176.6626 ms, maximum observed queue wait 9,304 ms. Evidence is retained beneath that validation's `server/out/data/tests/v/` directory. Gameplay counters match on all 100 days; lifecycle hashes diverge from day 1011, coincident with random native-action/conception IDs. Exact semantic identity and payload equivalence still need proof. Daily hash observation also contributes to the observed queue wait. Neither issue permits claiming acceptance or weakening the budget.

Source changes awaiting validation preserve native observation-required work across all active projection writers, add six writer/receipt tests, and measure SQL setup/execution/materialization separately. Column names are now resolved once per result set and result dictionaries are pre-sized without changing column overwrite or null semantics.

The concurrent save investigation retained evidence in `.codex-build/save-investigation-20260913/REPORT.md`: construction occupied 381.092 seconds, the client registration timed out at 360.004 seconds, and the eventual snapshot body took 11.976 seconds. Source holds an HTTP campaign read lease across provider waits, strongly supporting lock contention as the cause; no deployed lock stack was captured. The late, unfinalized snapshot is not manually relabeled or finalized.

The pending repair releases only owned synchronous HTTP read access during API character-construction/social-event network waits and same-character/turn lock waits. It reacquires access before processing the result. A restore/import/drop generation invalidates suspended requests, and a sticky guard prevents a broad catch from publishing a stale response or failure. Snapshot creation leaves the generation intact. Controlled provider-free contracts exercise real Save Sync registration while a provider and duplicate wait, restore before success/failure, and ordinary provider failure. Codex conversation-state adapter waits and other provider types retain their existing exclusion pending separate proof. No installed runtime or user campaign has been changed for this repair.

Combined `all` Tier 4 Release `20260914-043124-488ee8e3` finished in 537.305 seconds with all 21 project operations successful (564 MCP, 11 relationship-kernel, 23 editor and 75 image-generator tests passed), hygiene zero, fingerprint `da7475f914480a9404b331caa8faa4205f71ff1ce1504803a43e84bfd1f7d255`. Offline verification passed 106/107: provider-wait contracts 5/5, progressive social replies 10/10 and the client delivery contract passed. The sole failure was the portrait pipeline contract looking for the face-height rule in its old file after it moved to the shared portrait core. The portrait task corrected that predicate while retaining all bounds and both consumer checks. This failed report is not a deployment receipt; corrected validation is pending.

Replay v2 is now prepared for validation: two functional routes compare daily state, while a third candidate route measures fixed arrivals without daily hash/file observer work. All routes must match full final state. Opaque native-action/conception IDs map to unique complete semantic records, with payload, referenced parentage, reservation and commitment comparisons. Four new regression cases reject semantic changes and ambiguous duplicates; raw referenced-state rows are retained for diagnosis. No performance success is claimed until this candidate is built and measured.

## Combined validation — 2026-09-14, 00:18 America/Chicago

Corrected `all` Tier 4 Release `20260914-044922-802002c1` passed all 21 project operations and **155/155 offline checks** in **1,400.624 seconds**. This includes 564 MCP, 11 kernel, 23 editor and 75 image-generator tests; the relationship lifecycle check contains **223/223 cases**, including the new native observation and semantic identity cases. Save/provider exclusion passed 5/5, progressive replies passed 10/10 plus the client contract, character profiles passed 18/18 and portrait derivatives passed 20/20. Hygiene reported zero issues. Fingerprint: `07867b5d3caec6a3af1c8f1eff76c4ac0f7ecf27c3e7b5cf892bcf8b6a58d5a0`. Report and hygiene evidence: `.codex-build/reign-mcp/validation/20260914-044922-802002c1/validation-report.json` and `repository-hygiene-report.json`.

The portrait owner separately ran `character_narrative` from the exact successful artifact: **78/78 cases**, run `verify-1789362894559-854505a5`, 36.648 seconds. ReignValidation was then released for the relationship v2 smoke. These passes establish offline correctness within their coverage; the subsecond performance target and native acceptance remain pending. Nothing has been deployed or migrated.

Additional accuracy investigation: daily input ingestion stores changed hero documents and updates the latest observed-hero table, while older queued days can hydrate missing heroes from that table. A dedicated arrival-order fixture must compare just-in-time ingestion with newer status changes arriving before older days finish. No failure has yet been demonstrated; the capacity replay ingests each input at service time and does not establish this property.

## Historical input correctness — 2026-09-14, 00:45 America/Chicago

The arrival-order concern is now demonstrated and repaired for relationship input profiles. The controlled fixture produces courtship-focus counts **1, 0, 1** for normal arrivals, the legacy latest-row loader under burst arrivals, and the corrected burst loader. A later marriage/pregnancy update suppressed the earlier opportunity under the legacy path. The corrected burst also matches the normal day's complete chemistry rows. This proves the specific profile-arrival defect; it is not a general proof for every ordering of independent identity, standing and world updates.

Versioned observations now preserve timeline/hero/day facts in the same transaction as daily input ingestion. Historical matching and fling inputs resolve the applicable version. Processing/completed retries preserve their input facts; cleanup retains a predecessor, recent history and future queued versions. Missing legacy history fails explicitly instead of substituting newer facts. Drain the installed queue before this schema update: facts already lost by an older build cannot be recreated. Additional retry-boundary review remains before final handoff.

`changed` Tier 3 Release **20260914-053234-47adea8c** passed **227/227 relationship cases**, **62/62 persistence cases**, **564 MCP** and **11 kernel** tests in **707.691 seconds**, with zero hygiene issues. Fingerprint: `91dd56f90b1c9c01c02f0c0eb72ef29c696e015a82fdcf9d7aa5329ffffbba8a`. Report and hygiene files are in `.codex-build/reign-mcp/validation/20260914-053234-47adea8c/`. The prior successful all/Tier 4 report retains wider unchanged-module evidence.

The preceding five-day v2 replay (`verify-1789363152296-3ab9aa52`, artifact `20260914-044922-802002c1`) matched all three routes. Candidate p95 was **2,060.3394 ms**, maximum queue wait **71 ms**, with 1,601–1,631 evaluations/day. The subsecond target remains unmet. This newer artifact additionally reuses ingestion's exact normalized documents with independent copies and records offline per-query read costs without raw SQL or parameters. Its full 100-day v2 replay is in progress; no new performance success or native acceptance is claimed. Deployment and database migration remain held.
