# Reign launch and repository cutover

The user authorized implementation on 2026-09-14 after the other tasks stopped. Development and testing remain local. After verified cutover, `Dwemer-Dynamics/Reign` (private) and `Dwemer-Dynamics/ReignServer` (public), both on `main`, are the only publishing destinations. The former workspace GitHub repository is retired from ongoing use; its local source snapshot remains recovery evidence. Do not push the unsplit history to either new repository.

`ReignRelease/release.json` records the exact destinations, release identity, platform and package boundaries. Internal ReignBeta module, assembly and save identities remain compatible. Reign contains the Bannerlord module and private integration/developer evidence. ReignServer contains the server, native portrait generator, vector worker, shared contracts and installer sources. Shared helpers have one source owner; linked compilation preserves their existing assembly identities.

The install target is Windows 11 x64 with Steam Bannerlord v1.4.8. The server runs locally, outside the module directory. Windows PostgreSQL and model/runtime dependencies must be supplied by the release package. Read-only content is separate from per-user state. AI credentials belong to the installing user. The private payload host is not selected; the same verified payloads must support offline installation.

## Recovery and baseline

Before launch edits, task `01a09688-4ec3-7f43-92c8-a9cf6a2be80d` preserved the current tracked authored files (including staged and unstaged completed fixes) under `D:/ReignLaunch/recovery/before-implementation-20260914/`. `snapshot.json` records file hashes. Legacy tracked `server/` runtime output is excluded from this source snapshot and all repository imports. Original Git HEAD is `b36da31a3317b3fc74e0440190f6a199091c99a2`; the snapshot also preserves subsequent working-tree changes.

The final dialogue fix was deployed with the relationship fixes: Release changed Tier 3 report `20260914-073242-8401bb5d`, fingerprint `6dfb1221f530097701f61243af36c9a90d4c75d89015802785b2352b393adaa6`. Earlier all/Tier 4 report `20260914-044922-802002c1` passed 155/155 offline checks. These are baseline evidence, not verification of the launch changes. Relationship p95 remains above its strict subsecond target; fresh native/provider acceptance remains separate.

## Required completion evidence

- Reviewed source ownership inventory, exact remote checks, no private client source in public exports, and redacted secret/large-file audits for each repository.
- Canonical all/Tier 4 validation of the final paired source, including installer/shared-build/ownership changes, with report, fingerprint, duration and hygiene evidence.
- Hash-locked client, server, native database, vector/model and shared-content payloads; dependency provenance and notices; no campaign, secret, backup or generated verification source in distributed payloads.
- Portable installation/repair and isolated database backup/restore proof, clean-machine dependency closure, compatible health response and preserved visible server/Control Center lifetime.
- Local developer workflow, manifests, scripts, recovery guidance and configured publishing targets cut over to the two new repositories. No continuing writes to the former GitHub repository.
- Explicit remaining native, provider, clean-computer or private-host publication gaps. Do not mark the roadmap complete on source compilation alone.

## Progress

Current publication decision, 2026-09-14: after the isolated reinstall, save migration and portrait-discovery repair recorded below, the user reported, "Everything appears to be working, go ahead and send it to the respective github folders". This explicitly authorizes the initial source uploads to private Reign and public ReignServer and supersedes the earlier upload hold. The application code remains the exact validated and installed pair: Reign `f723666ea9f1b6e2708c642af72ad714cbd6fdda` and ReignServer `98c7776c0912bb9ed3813e8e7d4ce5d4c769c551`; publication updates only documentation. User-reported local acceptance does not establish another-computer execution, detailed performance/save-reload acceptance or a private download host. Source publication excludes packaged payloads and all personal runtime state. The dated progress below preserves the earlier decisions and evidence as history.

2026-09-14: Reobserved no other active same-project tasks and an available validation lease. Obtained an all/Tier 4 Release implementation plan with complete coverage and no diagnostics. Began preserving source and separating shared compilation and private verification packaging. Repository cutover and release artifacts are not yet complete.

2026-09-14: User requires clean-install evidence before uploading either repository/package; a successful source build is insufficient. User also requested care with C: drive space. New source checkouts are `D:/Projects/Reign` and `D:/Projects/ReignServer`; release staging and test databases belong on D:. The current MCP connection still targets the old workspace, so it must be cut over before final validation and subsequent development. No upload is authorized before the clean-install requirement is met.

2026-09-14: Both source repositories are staged with the exact origins and five verified sibling projections. Full `all` / Tier 4 / Release validation `20260914-183955-c74fda5e` passed in 654.2 seconds, covering all 21 managed projects, 598 MCP tests and 155/155 offline checks. Source fingerprint: `70809bb955a8a16442cfe33b667cc02395699573303d134189fbb6fd13eccf5f`. Report and enforced paired hygiene are under `D:/Projects/Reign/.codex-build/reign-mcp/validation/20260914-183955-c74fda5e/` as `validation-report.json` and `repository-hygiene-report.json` (3,961 tracked files, zero untracked files or hygiene issues).

The native Windows database, per-user installation records, bundled vector/model runtime, protocol checks, dependency notices and safe installer/repair/removal functions are implemented. The first actual PowerShell 5.1 installer attempt exposed an empty-download preflight error; its fix and two regression cases are included in the passing report. Routine campaign metadata now avoids costly native Windows relation-size scans while storage reports retain actual byte counts. The native database archive round trip and fresh campaign migration are covered by offline validation; the separately bundled vector proof performs network-denied inference and persistent reopen with no Python on PATH.

Canonical package assembly `20260914-185211-2e178ae8` passed in 303.3 seconds using those exact validated artifacts. Its `package-report.json`, `assembly-proof.json`, Inno compiler log and `package/` are under `D:/Projects/Reign/.codex-build/reign-mcp/release-package/20260914-185211-2e178ae8/`. The package contains the compiled setup plus client, server, runtime, dependencies and portraits ZIPs. The portrait payload carries 1,228 complete shared sets and exact file timestamps. All external inputs and packaged files have recorded hashes. Initial installation into a fresh local filesystem fixture is in progress; this fixture is not a full game copy or another computer.

The local cutover is applied and recorded in `D:/ReignLaunch/local-cutover-proof.json`: the old C: origin is removed, recovery history remains, global/current-task roadmap pointers target D:, and machine-local MCP configuration targets validated D: assemblies. Full MCP transport proof passed against D: using the isolated native developer database on port 55433. The already-running connector still requires reload; canonical D: fallback validation supplied the evidence above. Historical source-control plans are marked superseded. No uploads have occurred. Clean-computer and native Bannerlord acceptance, final local installation proof and private payload hosting remain open.

The actual PowerShell 5.1 installation reached its committed installation record after native database provisioning and copying all payloads, then failed when replacing the completion journal. Its untyped null backup argument became an empty path in `File.Replace`. Evidence is `D:/ReignLaunch/acceptance/install-2.log`; the completed record, receipt and staging evidence were preserved. The replacement now supplies a typed null, cleans its own temporary file on failure, and has successful replacement/locked destination/preservation contracts. A new full validation and rebuilt installer are required before accepting installation or repair.

2026-09-14: Full validation `20260914-190738-e543ef84` passed in 666.5 seconds (598 MCP tests, 155/155 offline checks, enforced hygiene), followed by package `20260914-191958-bb46b929`. All 16,757 files from the earlier installed package and 12,278 portrait timestamps matched their manifests. Its native database, dedicated Control Center, local model query and coordinated shutdown passed; evidence is in `D:/ReignLaunch/acceptance/`. The corrected installer then completed repair successfully (`install-3-repair.log`). The first local source commits are Reign `83a90cb985c77e55d4cbab334f8c99914a939622` and ReignServer `f6a394b2d8b387c951b66ac3ed1387270c0e9ce7`; nothing has been uploaded.

The subsequent locale check found that the visible PowerShell launcher read its BOM-free UTF-8 record as ANSI on Windows PowerShell 5.1/code page 1252. Non-English paths were corrupted; explicit UTF-8 reading passed (`D:/ReignLaunch/acceptance/json-encoding-proof.json`). The launcher now reads UTF-8 explicitly, with a contract evaluating that actual reading expression. A fresh full-package test in a non-English folder follows validation. Earlier transfer candidates remain test evidence, not approved launch uploads.

2026-09-14: Full validation `20260914-193728-eb475103` passed (707.4 seconds, 598 MCP tests, 155/155 offline checks, enforced hygiene), followed by package `20260914-195057-b784fbdf` (310.3 seconds). The fresh Unicode-root installation caught a native PostgreSQL limitation before committing the record: its narrow Windows paths corrupted the executable/state paths, so initialization failed. The failed fixture remains intact as evidence. Program and data paths now require ASCII, with actionable wizard/preflight rejection before payload copying and direct provisioning guards. Spaces and apostrophes remain supported. The final fresh test will retain Unicode game and installation-record paths with compatible program/data roots. This is a documented installation constraint, not unrestricted Unicode support. No uploads occurred; clean-computer/native acceptance remains open.

2026-09-14: Final code validation `20260914-200909-0f9b405f` passed: `all` / Release / Tier 4, 700.7 seconds, all 21 projects and 22 operations, 600 MCP tests, 11 relationship tests, 23 editor tests, 75 native renderer tests and 155/155 offline checks. Fingerprint: `8403870f819cf3152551525d6830ba285e483e7b7e3df45f0254d30fbbf6e0c4`. The report and enforced paired hygiene (3,961 tracked files, zero untracked files/issues) are `validation-report.json` and `repository-hygiene-report.json` under `D:/Projects/Reign/.codex-build/reign-mcp/validation/20260914-200909-0f9b405f/`. Artifact source commits are Reign `c32c70cdfc159f5efd512d75e8f14337a71ebfd3` and ReignServer `09694806221508bb201d59fad403fda21dd5bec9`.

Canonical package `20260914-202123-b715963a` passed in 312.4 seconds. Its setup plus five ZIPs are copied as ordinary NTFS hard links into `D:/ReignLaunch/Ready/Reign-0.1.0-preview.1/` to avoid duplicate storage; copying the folder to another drive materializes the bytes. `transfer-folder-proof.json` records 14 files and 6,427,659,700 bytes. The compiled wizard rejected an unsupported program path at the folder page, showed the full corrective message and was canceled before installation (`acceptance/wizard-path-proof.json`, `wizard-6.log`).

The complete packaged PowerShell installer then succeeded in a new fixture with ASCII program/data roots and Unicode game/installation-record paths, including spaces and apostrophes. The completion journal was written and owned staging removed. `acceptance/mixed-installed-package-proof.json` verifies all 16,757 installed files and all 12,278 portrait timestamps; `install-5-mixed.log` contains the native PostgreSQL provisioning proof. The exact installed server, dedicated Control Center and local 384-dimensional model passed startup/inference. Reopening the launcher retained the same server and window. Closing the dedicated Control Center stopped all 21 owned processes and released ports 5101, 55432 and 8082; developer PostgreSQL on 55433 remained separate. Evidence: `mixed-runtime-start-proof.json`, `mixed-installed-model-proof.json`, `mixed-activation-proof.json`, `mixed-lifetime-before-close.json` and `mixed-unified-close-proof.json`, all under `D:/ReignLaunch/acceptance/`. No provider calls or native game advancement occurred. Earlier package `20260914-191958-bb46b929` also passed actual repair/uninstall preservation of edited portraits, settings, user files, encrypted database credentials and ownership (`repair-proof.json`, `repaired-files-proof.json`, `uninstall-proof.json`); those reports retain that earlier fingerprint.

The local source-control cutover is complete, but the existing normal-play shortcut and campaign database still use `DwemerAI4Skyrim3`/5432 and have not been migrated. The new package's native database uses 55432; developer validation uses 55433. Original play state remains preserved. The current Codex task is still attached to the recovery C: project; open `D:/Projects/Reign` for subsequent work and reload its configured MCP connection. Final disk observation: C: 78.6 GiB free, D: 203.5 GiB free. `D:/ReignLaunch/LAUNCH_RESULT.md` and `Ready/TEST_ON_ANOTHER_PC.md` identify the candidate and remaining acceptance. Both GitHub uploads remain held: another-computer/full native Bannerlord acceptance, existing-campaign migration, the separate native performance targets and private payload hosting remain open. The roadmap launch item stays unchecked.

## Permanent local installation and migration — 2026-09-14

The user's latest instruction supersedes restoring the original installation after a successful test: keep the entire new package and migrate the original saves. This cutover is complete. The complete current paths, package/source identities, exact evidence, rollback locations and performance-task handoff are in [REIGN_LOCAL_INSTALLATION_HANDOFF.md](REIGN_LOCAL_INSTALLATION_HANDOFF.md). Historical entries above retain their original chronology; their unmigrated status is superseded by this result. The portrait-discovery correction below subsequently replaced the installed package while preserving those data roots.

The real compiled installer exposed two final issues: Windows PowerShell 5.1 inherited an incompatible PowerShell 7 security module, and Inno showed an ordinary success page after backend failure. Host-module import and explicit failure-page/exit-code handling were implemented with a fresh-process negative control. Final `all` / Release / Tier 4 validation `20260914-215942-32f76582` passed in 747.558 seconds by report timestamps, with 601 MCP tests and 155/155 offline checks; enforced paired hygiene had zero issues. Fingerprint: `c24cfc974de5a1a1d27591da55010acd3ac5a95664cdb6d04e6242d7c8458417`. The report and hygiene are under `D:/Projects/Reign/.codex-build/reign-mcp/validation/20260914-215942-32f76582/`. Artifact commits: Reign `ea18cb82712aa9dc75479a8a5e542764d0cf8912`, ReignServer `85541f189d1c073640563fdc8839c4fec41a6090`.

Canonical package `20260914-221428-e7565d49` passed in 330.312 seconds. The current transfer folder remains `D:/ReignLaunch/Ready/Reign-0.1.0-preview.1`, with 14 files / 6,427,660,374 bytes and setup SHA-256 `daab3872125876e37ade09e9c5f800db2d697b044c0035d275ab29d5ddf72e74`. The corrected actual compiled failure case exited 1 and showed failure; the full success case exited 0, completed the journal and removed owned staging. All original modules, dependencies, user state and the entire game profile were isolated first. Native Bannerlord 1.4.8.119303 reached the Reign main menu with the new dependency modules and no original saves available. No campaign was loaded or advanced. Evidence: `D:/ReignLaunch/acceptance/real-install-20260914/fresh-install-acceptance.json`.

The original WSL database was backed up and restored into a separate native database. UTC-normalized schema and content fingerprints matched for all 4,002 tables before the name cutover; native ownership is `reign`. The old WSL database and fresh acceptance database remain retained. A total of 132,456 original user-state files / 6,828,055,782 bytes were copied with exact hashes and timestamps. Every other provider/preference value was retained while the old absolute Codex executable preference was changed to the installed bundled PATH. Active server/data are now `D:/Reign/ReignServer/versions/0.1.0-preview.1-c24cfc974de5` and `D:/Reign/Data`, using native PostgreSQL 55432. The installed client/dependencies remain in the actual game's Modules directory.

Post-migration proof confirms all 14 live native saves match, all four Save Sync ledgers are byte-identical and all 17 snapshot registry rows are unchanged. Normal startup removed only its reserved `.save-sync-work` schema; all six non-internal campaign rows and all other schema names remained identical. Four old rollback points were already unavailable in the original ledger and three stale registrations were preserved. No user-save cleanup was run. All 16,757 package file hashes and 12,278 portrait timestamps still match, and the live portrait catalog resolves all 1,228 sets from the new Content root. The migrated server, dedicated Control Center and local model are healthy, the game is stopped, and paid provider call count is zero. Evidence: `D:/ReignLaunch/migration-20260914/migration-startup-proof.json`, `runtime-proof.json` and `installed-package-after-migration.json`.

The original modules, profile, user data and desktop shortcuts remain recoverable at the exact locations recorded in `acceptance/real-install-20260914/isolation.json`. A full original-backup rehash passed before migration. Both desktop server shortcuts now use the installed launcher; the XML previewer selects the D: source. The idle relationship-performance task was not resumed; its native performance/campaign acceptance remains separate. One diagnostic metric still assumes the old shared-art relative root even though the actual installed portrait catalog and bytes pass; the handoff records this follow-up and the one-time native migration helper coverage gap. Another-computer execution, native campaign acceptance and private payload hosting remain open. No uploads occurred and the launch roadmap item remains incomplete.

## 2026-09-14: correct installation discovery after the user's campaign test

The user's normally launched game reported the missing module-relative shared portrait folder despite the server catalogue finding the content. Physical file-handle evidence established that setup, launched under the packaged Codex app, had written the old LocalAppData record into Codex's private MSIX storage. A separate WMI-launched Windows PowerShell/.NET Framework process confirmed the record was absent outside that merged view. The installed binaries were current; the defect was installation-record discovery.

The shared contract and all setup/launch/uninstall defaults now use `%USERPROFILE%/.reign/installation.json`. Bootstrap and default program/data locations also avoid AppData. Setup probes newly created files through their actual Windows handles before extracting payloads and verifies the published record before committing. Legacy ownership adoption is limited to ordinary repair with no current record or explicit override; updated runtime code never reactivates that old record after uninstall. BLSE processes now participate in the installer's stop guard. Existing selected D: roots and the previous save migration remain intact.

The fix passed all/Release Tier 4 `20260914-235951-39fb0dc0` in 678.062 seconds: 603 MCP, 11 relationship, 23 editor and 75 renderer tests, plus 155/155 offline checks. Paired enforce-mode hygiene passed with no issues. Fingerprint `ddf9ece1fdd5e180c14aeabbca1c557aaf94ada6a415bb7734ae205d6c75b0a2`; report and hygiene are under `D:/Projects/Reign/.codex-build/reign-mcp/validation/20260914-235951-39fb0dc0/`. Artifact commits: Reign `f723666ea9f1b6e2708c642af72ad714cbd6fdda`, ReignServer `98c7776c0912bb9ed3813e8e7d4ce5d4c769c551`.

Canonical package `20260915-001229-053ba96a` passed in 302.701 seconds. The actual compiled setup passed the invalid-game failure page/exit 1 and the real repair success page/exit 0. New active version: `D:/Reign/ReignServer/versions/0.1.0-preview.1-ddf9ece1fdd5`; completed transaction `D:/Reign/Data/setup/c8344755567d4868885ba67f5859a7c9.json`. The current transfer folder remains `D:/ReignLaunch/Ready/Reign-0.1.0-preview.1` (14 files / 6,427,666,449 bytes), setup SHA-256 `3c72a9496c0761e550d77b07e9351b909a2325645e68fa07bdefd3d3daf6a0aa`. Prior package, module, server, records and shortcut bytes remain recoverable.

The independent normal-process check loads the actual installed contract without any Reign environment override and selects `D:/Reign/Data/PortraitCache` for writable portraits. On 2026-09-15 the user superseded the separate shipped-content location: the complete 1,228-directory shared library is part of the directly distributable `Modules/ReignBeta/PortraitCache/_shared` tree, and `contentRoot` equals `moduleRoot`. The module/source inventory locks all 12,278 portrait files. Both desktop server shortcuts select the `.reign/setup` bootstrap. Native in-game portrait acceptance and another-computer execution remain distinct from these checks.

The complete pre/post comparison passed for 120,761 selected player files / 4,293,902,310 bytes, with no missing files, hash changes or timestamp changes. This includes native saves, Save Sync, campaign files, settings, prompts and personal portraits; database files and runtime caches were excluded. After comparison, the normal Windows process context launched the installed visible shortcut with no installation override. Health, provider middleware, background queues, memory worker, telemetry and prompt cache all responded successfully; no background or memory work was pending/failed. The visible dedicated Control Center displayed the correct D: data and unified lifetime status. The new server remains running and Bannerlord is stopped for the user to confirm the portrait fix in-game. Final evidence: `player-files-after.json`, `visible-launch.json`, `runtime-proof.json` and `control-center-observed.json` in the repair root. Initial uploads remain held.
