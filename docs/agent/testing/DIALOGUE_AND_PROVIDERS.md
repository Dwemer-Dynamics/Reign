# Dialogue and provider testing

Read only the procedure relevant to the current task. Existing provider, campaign, and deployment gates remain authoritative. Dated procedure history was retained during the 2026-09-12 guide split.

## Conversation naturalness and accepted gifts (2026-09-14)

Run manifest-selected Release validation, then the exact artifact's existing quick/offline `prompt_efficiency` suite with `reign_run_offline_verification`. `pipeline.prompt_caching` includes the captured Gavene answer/correction/group-loop regressions, ordinary dialogue and literal-count negative cases, production prompt variants, selective non-mutating Dynamic Characteristics projection/persistence, and accepted pernach candidate preservation at caps 3/10 with inventory-based item resolution. No new runner, listener, provider route or campaign operation is introduced. See [scope, evidence and limits](../DIALOGUE_NATURALNESS_FIX.md).

Preserve the existing second-attempt repair policy and do not reject a reply merely for length. Exact reviewed tone clauses are scoped at composition time without rewriting saved customization; historical rhetorical records are omitted from prompts, not deleted. Gift routing only preserves a planner candidate: normal consent, exact native item resolution, authority and execution remain required. Offline checks do not prove provider naturalness or native transfer. Follow existing provider/runtime/disposable-save gates for post-deployment acceptance; never auto-replay an old gift.

## Progressive social and wilderness replies (2026-09-13)

User priority (2026-09-13): realism and accurate, consistent roleplay take precedence over response speed or preferred length. Use prompts to guide length; do not reject an otherwise valid response solely for exceeding a preferred token or word target. High output ceilings are acceptable where provider/API requirements need them. Preserve substantive grounding, character knowledge, continuity and complete-response checks; an empty or truncated provider result is not a usable reply.

The normal group-turn path must display each successful reply before requesting the next character. `/events/social/turn` accepts opt-in `progressiveReplies=true` and an acknowledged `replyCursor`, while retaining one `turnId`. Each request yields one new durable speaker result with `hasMore=true`; a final request performs group adjudication. Legacy callers still receive a complete response. Focus/order is retained across steps, later speakers receive earlier attributed replies, and duplicate cursors replay stored results. The client deduplicates returned participants and awaits main-thread display before continuing. A later failure never removes already displayed replies. Control and native-save gates are unchanged.

Run the existing artifact-bound, provider-free `contracts` suite through `reign_run_offline_verification`. `contracts.social_event_progressive_replies` exercises actual turn orchestration with an injected responder in a unique isolated fixture: first-reply boundary, concurrent lost-response replay, invalid cursor, later failure/resume, final-reply-before-adjudication, prior-speaker awareness, completion replay, one player transcript line and legacy compatibility. `contracts.social_event_progressive_client` verifies the client/main-thread integration alongside compilation. Existing tests own cleanup in Verification Lab storage; do not call the production turn route merely to inspect a live turn, since it can generate replies or adjudicate relationships.

After coordinated deployment, native/human acceptance must show the first reply while the next character is still working, retain early replies on later failure, and show each reply once on completion. Social and generated-wilderness events use the same view model. Existing approved visual assets, geometry, typography, scroll behavior and portrait composition are unchanged; this is delivery behavior, not a visual redesign. Offline contracts do not prove native screen timing. The 2026-09-13 incident showed all three server replies persisted while one six-minute client request timed out: Gavene's provider call alone took 217,952 ms. Extending the aggregate timeout alone does not restore progressive display.

## Prompt composition and complete evidence (2026-09-08)

Use `reign_get_prompt_evidence` (`GET /audit/prompt`) with an exact correlationId and optional campaignId. Pin the returned `evidenceId` while paging `messageIndex`, `offset`, and `length` (1–12000 characters). Empty evidenceId selects the latest retained provider attempt. Content is redacted before storage/pagination, with exact redacted UTF-8 hashes; original character counts, rule/source/version metadata and omission reasons distinguish text from estimates. The per-campaign ledger retains the existing 8 MiB/4 MiB bounded audit policy. Missing/expired/pre-capture evidence is explicitly unavailable. No returned model reasoning, credentials, provider invocation, campaign mutation or arbitrary filesystem access is exposed.

Layout 9 retires only reviewed exact monolithic template hashes into an inactive byte-preserving archive. Unknown custom content stops activation before any file is moved. Shared rule dependencies render once; A-only/B-only coverage and scoped distinctions remain mandatory. Requests remain self-contained with subscription-safe implicit caching. Native wealth v3 separates cash, clan backing and nullable party cargo; appearance remains independent. Run manifest-selected validation, then the existing isolated `prompt_efficiency` and applicable `contracts`/`encountered_residents` suites. Provider/native acceptance still uses the existing cost/disposable-save gates. Details and migration ownership: [Prompt composition](../PROMPT_COMPOSITION.md).


For an exact `courtLifeTemplateId`, guarded `court_life_prepare` binds the one existing pending patronage encounter before attempting generation. `reusedExistingMatter=true` and `fixtureChanges.markerOnly=true` distinguish this from creating a fixture. The harness preserves its conversation, quotes, participants, receipts and native state; it records only the test marker and baseline fingerprint. `existing_patronage_binding_preserves_matter_and_native_state` verifies unchanged state and matter count. Continue from the returned conversation rather than replaying earlier turns.

Binding rejects multiple pending matches, an already-bound matter, committed effects, or a mismatch in campaign, timeline, reign, kingdom, source or capital. It cannot be combined with fixture gold, loyalty or captive mutations. Existing confirmation and disposable-save gates remain required. No pending matter is deleted and no production eligibility rule is waived. Empty template selection continues to use normal generation. Read-only `patronageEligibility` diagnostics still create no binding.
## Dialogue marriage execution (2026-09-09)

The identity contracts isolate the persisted day-50 court-audience prompt fixture from the day-10 compact-roster fixture in separate unique test campaigns. `court_audience_fixture_isolated` verifies that the roster campaign is still empty before synchronization. Existing confined test cleanup, same-clan/family identity checks and exact compact-roster counts remain required; no production identity policy changes.

Run the existing quick/offline `contracts` suite against the successful Release artifact. `contracts.dialogue_marriage` exercises the production router and action normalizer with the failed planner repair shape, exact player/NPC consent, missing/conflicting/third-party identities, stable hashed terms, and pending/failure/native-confirmed dialogue. Tests use isolated Verification Lab storage and never queue an action, invoke a provider or mutate a campaign. Canonical `source` and actor/target fields must not become guessed participant aliases. The existing authority receipt adds policy `personal_marriage_consent`; native `marriage_completed` evidence adds `alreadyMarried`.

After separately authorized deployment, use the existing provider and guarded disposable-campaign routes to verify accepted vows produce the exact marriage action and reciprocal native spouse IDs, ineligibility refuses execution, retry after the native clan change does not repeat marriage or alter hashed terms, unrelated heroes remain unchanged, and save/reload and the next prompt retain native facts. Offline proof covers normalization and reporting, not native marriage eligibility or model behavior. See [Dialogue marriage fix](../MARRIAGE_DIALOGUE_FIX.md). No new tool, confirmation, save namespace or cleanup path is introduced.

## Conversation history and earned familiarity (2026-09-09)

The quick previewer inventory contract pins the current machine catalog's 21 prefabs and 20 runtime movies. The former 20/19 expectation was stale; catalog-to-disk set equality and all existing preview, provenance and native-acceptance checks remain required.

The existing group-kinship assertion checks the current native-Hero family boundary and correction rule instead of retired wording. It still requires the current speaker's native links, withholds unidentified participants' private links, and forbids clan or name similarity as proof of kinship. The commoner household distinction remains intact.

Use the existing isolated `interaction_architecture` and `prompt_efficiency` suites against the successful Release validation artifact to verify transcript continuity. The former persists and reloads shared event beats, retains every speaker, deduplicates retries, preserves same-second append order and complete exchange boundaries, and checks session isolation in structured group history and both sides in the database history projection. The latter checks that player and NPC history appear exactly once in noble/commoner dialogue, party and event prompt envelopes, plus the independent correspondence memory/input envelope. Scene labels exercise the shared party builder; they do not constitute native acceptance of each interface.

The existing `contracts.sovereign_conduct` case covers gradual personal-affinity relief, unchanged initial authority caution, formal address and conduct limits, and rejection of relief for threats, coercion, captivity or unverified identity. Public standing alone cannot supply personal familiarity. Political posture evidence adds `authorityDanger`, `personalAffinity` and `familiarityReduction`; no persistent relationship schema changes. See [Event Chat continuity fix](../EVENT_CHAT_CONTINUITY_FIX.md) for the cause, affected routes and calibration.

These are provider-free, non-listening checks with isolated state. Native/model acceptance remains a separate check under the existing provider and disposable-save gates: inspect the next real prompt for both sides of the saved transcript and assess whether the NPC advances the conversation. Do not replay old dialogue or migrate a normal campaign to repair this read-time filter.

## Conversation intoxication and italic actions

See `ReignBetaServer/docs/ConversationIntoxication.md`. The provider-free `conversation_intoxication` quick/offline suite runs the production threshold, recovery, receipt/replay and formatter contracts in an isolated fixture campaign. It is also included in `contracts`. Use `reign_run_offline_verification` against the successful Release validation run, never the installed running server. Native and provider acceptance remain separate. The preview/catalog/font contract is `node ReignBeta/tools/GauntletXmlPreviewer/test-conversation-actions.mjs`; the shared sprite materializer now accepts `-Categories ui_reignbeta_action_fonts` to preserve unrelated atlas output.

The conversation `intoxication-actions` preview preset covers all five chat catalog entries (four prefabs). The shared rendered audit verifies three italic, marker-free action spans in each preset. Typography authority changes are limited to the eight body-widget rich-text substitutions with unchanged size/geometry; the audit also now inventories the pre-existing Royal Council rich-text widget.
# Codex SDK image provider (2026-09-09)

`reign-codex-image-compatibility-v1` is cataloged under `codexImages`. The explicit `codex_images` quick/offline Verification Lab suite runs the complete image provider contracts, including normal-profile routing, adult rejection, PNG and path bounds, official skill/reference handoff, isolation, timeout interruption and cleanup. Ordinary image contracts also include the new cases. Use `reign_run_offline_verification` against a successful Release validation run; it starts no listener and consumes no subscription usage.

After explicit subscription-usage authorization and deployment of the validated artifact, use the existing `reign_start_verification` tool with `suite=codex_images`, `tier=live-llm`, `liveCaseCap=1` for the first compatibility attempt, `repeat=1`, and the existing `start Reign verification` confirmation. A cap of 2 or 3 additionally covers scenery and event illustration. This suite is opt-in and does not run image requests in generic live-LLM checks. It stops after a failed attempt; cancellation interrupts the turn and stops the job-owned helper. It never changes a campaign or save.

Inspect the containing Lab report through `reign_get_verification_results`, and retain the sandbox's `codex-images/report.json`, reference and successful outputs. These record the runtime-managed model, transport, duration and hashes. The synthetic reference is a color palette: this check proves SDK transport/output compatibility, not native character likeness. Human visual acceptance remains separate. Run `portrait-profile-ui-contract.mjs` against the isolated exported Control Center fixture for nine provider/width cases, adult exclusion and saved normal/adult setting independence. No native interface, raster shell, aperture or atlas is changed by these web provider controls.


## OpenRouter providers (2026-09-09)

`reign-openrouter-providers-v1` adds independently saved NanoGPT and OpenRouter chat credentials and model selections while retaining Codex and custom APIs. `openRouterApiKey` is shared by OpenRouter chat and all four image profiles; blank/masked/null submissions preserve it, explicit clearing never falls back to another key. Legacy API keys migrate only to their endpoint owner once. `llmModelProfiles` stores only the twelve allowlisted model fields per provider; active flat model fields remain the runtime contract. OpenRouter uses its fixed HTTPS endpoints, prefixed chat model IDs, native reasoning controls including direct-selector exclusion, and reference-image requests to `/api/v1/images`.

Use the existing artifact-bound `interaction_architecture` offline suite and `contracts.image_generation_profiles` (included in quick `contracts`; the focused `codex_images` quick suite also includes all image adapter contracts). Injected image transport proves all five catalog models across four profiles, exact source reference bytes, endpoint/key selection, empty/error/echo rejection and timeout/missing-key behavior. `OpenRouterCatalogTests` checks catalog/help drift. No new tool, live profile, confirmation phrase or campaign mutation route is introduced. The existing GLM cache probe is enabled only for NanoGPT in the UI; its backend rejects non-NanoGPT endpoints before making a provider request.

The architecture suite emits `control-center.html`. Run `chat-provider-ui-contract.mjs` with that HTML path and an output directory; run `portrait-profile-ui-contract.mjs` on the same fixture. Both live under `ReignBetaServer/src/Modules/` (Platform and Portraits respectively) and intercept all requests. The chat matrix covers four providers at 1672/1024/600, draft switching, all model roles, masked-key preservation and save/reload; the image matrix covers NanoGPT/OpenRouter/AtlasCloud and normal-only Codex. Reuse `control-center-ui-contract.mjs` for full-page palette/navigation regression. MCP has no browser-preview tool; these documented shared fixture harnesses are the fallback. Native Bannerlord, portrait-layering and approved-raster fidelity are not applicable to these web form changes. Provider-backed output still requires a configured OpenRouter key and separately authorized paid smoke testing.

Image presets are exact matches verified from `GET https://openrouter.ai/api/v1/images/models` on 2026-09-09: `openai/gpt-image-1`, `google/gemini-3-pro-image-preview`, `bytedance-seed/seedream-4.5`, `bytedance-seed/seedream-5-0-pro`, `x-ai/grok-imagine-image-quality`. GPT Image 1.5, WAN and Flux Kontext exact variants were absent and are not silently mapped to another model. Existing AtlasCloud/NanoGPT catalogs remain intact. See https://openrouter.ai/docs/guides/overview/multimodal/image-generation for request capabilities.

## Codex performance controls and isolated comparisons

`reign-codex-performance-report-v1` is cataloged under `codexPerformance` in `reign.testing.json`. Run `reign_run_offline_verification` with the exact successful Release validation run ID and suite `codex_performance` for injected runtime, conversation, settings migration, dispatch-cap, and report contracts. Run `interaction_architecture` for the shared Control Center fixture, then its three browser contracts at 1672, 1024, and 600 pixels with all traffic intercepted. This is a web control change: native artwork raster/fidelity checks do not apply.

The optional provider comparison is explicit-only: `reign_start_verification`, tier `live-llm`, suite `codex_performance`, the usual `start Reign verification` confirmation, and `codexPerformanceJson`. The JSON must name the exact model, caseIds, full baselineOptions and variantOptions, the single changedOption, a mandatory totalProviderCallCap from 1 through 1000, and confirmation `run bounded Codex performance comparison`. The run-wide cap covers every physical turn/start across primary generation, repair, tier retries, probes, judges and repeat passes. Discovery and thread creation are not generation calls. Cancellation/closed budgets prevent later dispatch. The suite uses isolated Verification Lab campaign data, preserves the selected live provider/settings, and is never included implicitly in aggregate live suites.

Model checking is separate: POST `/api/codex/models/verify` requires `maxRequests:1`, `bounded:true`, and confirmation `verify one Codex model request`. The Control Center explains usage before confirmation. Discovery remains free. A timeout remains inconclusive; request-model echoes do not establish actual served model identity. No user entitlement or shared Codex configuration is modified.

Reports separate baseline/variant latency and quality outcomes, retain unknown usage/fingerprints as unknown, and keep clean attempts, successful repairs, accepted repair overrides, and unusable results separate. Runtime/conversation diagnostics contain no reasoning or rejected draft text. GPT-5.4 access, actual Fast-mode service, and live roleplay quality require later user-authorized tests. See `docs/agent/CODEX_PERFORMANCE.md` for controls and limitations.

The provider comparison plan also requires `validationRunId`: the server checks its own assembly SHA-256 against that successful Release run's exact artifact before dispatch. Source and validation fingerprints come from that report; they are never inferred from model settings. Supply this ID inside codexPerformanceJson (or the CLI `--codex-performance-plan` JSON file). Ordinary quick/offline contracts do not require or authorize a provider plan.

Injected Codex contracts are CLI-only: use artifact-bound `reign_run_offline_verification`. The listening server refuses this injected suite so its test transport cannot intercept gameplay requests. The dedicated Codex browser script wraps the shared provider script; run that wrapper once, then the separate full Control Center matrix.

The MCP supplies its trusted workspace as `validationWorkspaceRoot` so an installed server can locate the exact report. CLI comparison plans may supply that exact Reign workspace explicitly. The report verifies the running assembly hash and records the actual runtime executable hash when its path is known; accepted replies are retained only for later human roleplay evaluation.

Codex comparison fixtures must identify a shipped character or include a completed character snapshot (constructed, traits, and a complete narrative when using narrative v5). Each isolated arm preserves the supplied character stack; fixture personality authoring is not part of the latency comparison. Both arms must keep the same reasoning mode and schema version. Served-model consistency uses authoritative runtime evidence; a request-model echo leaves verification unknown.
