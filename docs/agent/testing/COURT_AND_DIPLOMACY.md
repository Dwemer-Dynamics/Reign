# Court and diplomatic testing

Read only the procedure relevant to the current task. Existing provider, campaign, and deployment gates remain authoritative. Dated procedure history was retained during the 2026-09-12 guide split.

## Court audience artwork (2026-09-09)

Cache identity uses whole-year appearance age; ordinary fractional aging does not trigger another scene. The native portrait maturity guard is preserved: unsupported infant/toddler references fail before a partial-cast request rather than borrowing an adult face. Missing references remain a distinct, observable artwork failure.

`reign-court-audience-art-v1` uses the existing castle-scene route with exactly one composite containing every involved courtier and the approved host-town hall. `CourtAudienceSceneTests` runs the production compositor, fake provider boundary, cache, failure and view-lifecycle seams through manifest-selected `changed` validation. No paid per-person portrait preparation occurs; missing cached portraits use bounded free native appearance captures. Native capture/age/likeness and provider image quality still require Bannerlord acceptance.

Existing `snapshot`, `noble_snapshot` and `court_life_snapshot` expose `audienceArt` with pending/ready/failed/disabled status and, when ready, the `CourtPetitionArt/<snapshotKey>.png.json` receipt. It contains complete participant IDs, host/culture, reference/composite/image hashes and logical input-image/scene-request counts. Logical counts are not billing receipts; retain actual provider receipts separately. Court Life snapshots remain read-only diagnostics while dialogue/art are pending. The fixed hall and textual international `sharedScene` never establish generated-image success. Dialogue/decision controls retain their independent readiness. Existing provider authorization, exact disposable-save and snapshot gates remain unchanged; no new endpoint, command or confirmation phrase. Verify full cast, cross-culture halls, responsive dialogue, cache/reload and close-during-generation after deployment, and keep the roadmap acceptance item open until that evidence exists.

## International settlement acceptance (2026-09-09)

Court decisions retain `acceptedPartyTiers` for every actual accepting participant on the same option and exact terms. A later winner or envoy reply cannot erase the losing household's mitigation. Changed options or terms discard earlier party evidence; a fresh acceptance from the same speaker replaces that speaker's tier. The current envoy acceptance remains the payment authorization even when a domestic noble speaks afterward. Actual payment recipient determines which household bears the losing consequence. Legacy decisions retain only their recorded speaker's evidence; completed judgments and payments are not replayed or retrospectively reinterpreted.

Use existing guarded `court_life_converse`, `court_life_choose_confirm`, `court_life_observe` and save profiles. Verify both speaker orders, actual recipient and exact terms, the losing party's applied relationship receipt, envoy authority, and fresh-process persistence. Preserve the original failure and verify a corrected case without repeating unrelated passed hearings. Core party-acceptance contracts cover evidence merge, recipient direction, changed terms, speaker identity and separate authorization selection.

## Diplomatic completion evidence (2026-09-09)

Delayed native treaty completion must settle its existing court matter automatically. Before any second confirmation, use `court_life_observe` to prove the original action is Completed, the court has one resolution receipt/history entry and its relationship adjustments, and no second action or treaty was created. The native background path captures the confirmed decision, terms and action identity before enqueue; it reconciles only the exact completed `court_decision` action in the same campaign, timeline and ruler scope. Old normalized proposals require matching saved agreement evidence. Pending Government authorization, unrelated actions, changed terms, failed/rejected actions and already settled matters do not qualify. Reconciliation waits for active conversations to finish and never calls the treaty executor.

Keep a pre-repair rolling save with the actual completed native treaty and unfinished court record when available. Reload that exact disposable save to prove automatic reconciliation, then test natural acknowledgement and a stable-resolution retry. Completed trade continuity includes its actual duration and supersedes old pending dialogue; an ordinary completed judgment does not imply a treaty. The core completion policy and `court_life_resolution` contracts cover status/identity/scope boundaries and exactly-once persisted trade knowledge. Existing guarded campaign and court profiles remain the only mutation routes.

For the asynchronous negative boundary, use `court_life_choose_confirm` with `courtLifeExpectation=pending`. Its `pending_diplomacy_confirmation_has_no_settlement_effects` assertion requires an unfinished real confirmation, a single Executing native action with Success=true/Completed=false, a pending court matter without settlement receipt or native committed flag, unchanged ruler gold, and no matter relation adjustments or history. `usedProductionChooseAndConfirm` records invocation; `productionConfirmationCompleted` and `expectedPendingDiplomaticAction` distinguish completion from the expected wait. Unrelated errors cannot pass this gate.

Government pending receipts identify the responsible kingdom and saved hearing through `governmentBusinessId`, `governmentKingdomId` and `governmentStatus` diagnostics. Only an awaiting-counterpart stage follows the linked counterpart hearing. Normal votes, reconsideration and authorization remain required. The Government snapshot contract checks current authoritative, individual-member/binding-authority wording and authority meaning; its obsolete ruler-may-agree literal is retired. This updates evidence and assertions, not government powers.

International observation adds `nativeState.international.nativeDiplomaticAction`: native service availability, matching action count, exact matterId_native ID, status, type, terms and execution snapshot. Resolved accepted diplomatic proposals must pass `accepted_diplomacy_has_one_completed_native_action` and `accepted_diplomacy_receipt_is_completed`. A successful choose-confirm operation does not prove completion. A `government_hearing_pending` receipt with Success=true and Completed=false is pending work; preserve Government authorization and advance only through the guarded campaign route. Verify no court settlement effects while pending, then exact native completion and effects once authorized. Re-prepare persistence fingerprints after the additional evidence; retain earlier failures and never infer historical completion from a progress receipt. No safety gate or mutation route changes.

## Binding an existing court encounter (2026-09-09)

Use guarded `court_life_preflight` with the desired `courtLifeSource` and optional `courtLifeTemplateId`. Its read-only `pendingMatterInventory` reports exact IDs, titles, states, completed turn counts and existing fixture bindings; at most 100 sorted matches are returned with totalCount and truncated. Narrow the template filter if truncated. Inventory membership does not waive binding eligibility.

Pass `courtLifeExistingMatterId` to `court_life_prepare` for International or Patronage. This exact selection never falls back to generation or refreshes world/history for a new fixture. It requires the armed run-owned Current, fixture confirmation, matching campaign/timeline/reign/capital and source, plus matching template when supplied. International scope checks the payload's player kingdom and ruler separately from the matter's foreign KingdomId. Committed, missing, ambiguous, already-bound or in-flight matters are rejected, as are fixture gold/loyalty/captive/age changes and participant selection. No baseline is mutated and no decision, payment, time advance or save is injected.

Successful binding stores only a test marker and the existing fingerprint. `existing_matter_binding_preserves_matter_and_native_state` checks unchanged matter/native fingerprint and matter count. Preserve the returned conversation and continue new turns through existing open/converse/choose-confirm/observe profiles. Original automatic Patronage template binding remains supported. MCP rejection and catalog tests are provider-free; native acceptance separately checks invalid-ID/template/already-bound rejection, marker-only preservation, natural conversation and actual effects on the enrolled disposable campaign.

## Testing an existing patronage encounter (2026-09-08)

## Patronage eligibility diagnostics (2026-09-08)

The guarded `court_life_preflight` profile and failed Patronage `court_life_prepare` responses include `patronageEligibility`. Supply `courtLifeTemplateId` to inspect one exact template; an empty preflight filter returns the bounded catalog. `unmetPrerequisites` distinguishes missing capital/ruler, spouse/child/coming-of-age prerequisites, prior creator work, missing public historical subjects, and `same_template_pending`. Reports include at most five public subjects and five pending matter IDs per template, plus the full pending count. Unknown template IDs return `templateKnown=false`.

These diagnostics are read-only: they do not create or bind a matter, inject history, remove pending matters, charge gold, or waive eligibility. The existing disposable-campaign gates still apply. Inspect the reason before retrying preparation; an existing pending matter is not evidence of missing history. Proclamations establish that the ruler made a public statement, not independent proof of every claim within it.
## Political pressure observation (2026-09-08)

Use `reign_get_world_details` with `subsystem="political pressures"` and optional `pair="actorKingdomId|targetKingdomId"` to inspect activity orientation. For `incident_completed`, actor/target follow the incident's origin/target; an incident originating in the player kingdom can change the foreign target's pressure toward the player. Read the nested `incident` object's `origin_stance`, `target_stance`, and each side's `*_pressure_before`/`*_pressure_after`, rather than treating the top-level origin values as the foreign result. Principal IDs, severity, trait provenance and narrative accompany those values. The join is confined to the same campaign and timeline; a missing record has `incident=null`. Compare with the current matrix in `reign_get_world_overview`.

The existing server limits the candidate activity window to its latest 1000 rows before filtering and pagination; an empty filtered page does not prove that older incidents never occurred. This is a bounded, redacted, read-only GET with no provider, game mutation, pressure adjustment, time advancement or save. The isolated `world_test` suite covers both-side detail, missing/cross-timeline records and non-mutation. Reuse organic incidents when their retained evidence proves a requirement; do not manufacture duplicate cases merely to fill the ledger.

## International payment identity (2026-09-08)

New international hearings prepare one concrete shared account through the production `POST /court/life/scene` route before their first speaker. It uses the same capital-court authority and interactive provider gates as court dialogue; existing guarded `court_life_open`/`court_life_converse` profiles exercise it without an override or a new confirmation phrase. The client sends the exact catalog template ID and premise. The server rejects an unknown or mismatched pair and applies any template-specific grounding before generic particulars, preventing a specialized plot such as a dynastic debt from collapsing into an unrelated fee or cargo dispute. `matter.PayloadJson.sharedScene` records `reign-court-life-scene-v1`: claimant side, shared physical situation, each household's attributed account, the question to decide, and evidence status. Alleged rates and other particulars remain claims; this operation creates no payment, proof, world-history event, or game action and does not alter offered terms.

For a contested case with player-paid compensation controls, the prepared foreign account must state the exact full requested settlement, named payer and named recipient supplied by the `compensate` option. Deposits, rates, damages and totals must make that request plausible. The `compromise` amount remains a separate ruler-offered alternative and cannot be described as paid or already agreed. This keeps generated particulars actionable through the displayed controls while preserving all payment and authority gates.

The server preserves the first valid scene in `court_life_turns` under a campaign-local, timeline-and-matter-qualified key, actor `case_scene`, phase `scene`; retries return that immutable account. The client copies it into the saved matter before displaying replies. Already-spoken legacy hearings retain their transcript and are never assigned a replacement story. Missing or oversized required fields fail before any speaker is displayed. The court interpretation contract run includes `court_life_scene` checks for complete distinct accounts, serialization, bounds, unknown claimant sides and exclusion of action fields. Native acceptance must additionally prove consistent roles and concrete competing claims, unchanged offered terms, retry/save/load preservation and no preparation effects. A structurally valid scene alone is not narrative acceptance.

Court audience art uses a versioned full-cast prompt contract. Every participant must appear once at a consistent apparent scale, entirely inside the image from hair to both feet, with floor visible around the feet and no near-foreground crop. Identity references control face, age, build and recognizable identity; cropped or underdressed references do not authorize missing body regions or garments. Every adult receives complete, modest, culturally plausible formal court clothing. A prompt-contract check proves these requirements and the render-contract revision invalidates older cached compositions. With image generation disabled, compilation and prompt checks do not prove a corrected provider rendering; retain the failed cached image and require a later image-enabled visual sample before closing the visual defect.

International judgment and referral candidates receive an independent authority check using only the current ruler message and offered options. NPC claims and memories of previous hearings cannot authorize a current ruling. The normalized decision retains `playerQuote`. For `refer`, `referralOptionId` and `referralTerms` identify the exact offered proposal requested by the ruler; missing, unknown or changed terms cannot dispatch a referral. A request to forward a complaint alone does not authorize a default payment proposal. The visitor should request exact terms instead. The existing court interpretation suite covers the authority gate, missing or fabricated quotes, disagreement between classifications, and referral term binding; catalog drift checks cover the unchanged harness routes.

Native acceptance must include an evidence question with a refusal of a particular remedy (no judgment selected), an explicit present ruling (correct judgment selected), an unspecified referral (no invented payment proposal), and an explicitly identified proposal referral (exact terms preserved). Retain failed attempts and rerun only affected failures or distinct branches. A deferred proposal is not a completed agreement; verify the later reply and separate final confirmation before any effect claim.

International monetary terms bind `payerHeroId`, `recipientHeroId` and `gold` through dialogue, review, referral and native execution. Outgoing settlements use the ruler's funds and may name either claimant; extortion pays the foreign sovereign. Gifts and ransoms retain the foreign-sovereign-to-player direction. The interpreter accepts a changed payment party only when the player's quoted proposal identifies a supplied person or unambiguous household. Changed terms require fresh assent. Old pending amount-only offers lose their old assent when exact parties are added; old referrals without payment identities cannot execute. Completed receipts are never migrated or replayed.

The existing court interpretation suite covers identity preservation, explicitly named counteroffers, fabricated/unspoken parties, payment direction and authority. Native acceptance uses the same `court_life_converse` and `court_life_choose_confirm` profiles and exact saved hearing. Inspect `nativeReceipt` for both party IDs and both before/after balances; a ransom has a nested `payment` receipt. Compare the intended recipient, the other claimant and the payer, then prove confirmation retry and fresh-process persistence without another debit. Keep pre-repair failed dialogue and save evidence; do not repeat completed unrelated template tests.

An `accept` decision that releases a captive without ransom is the player ruler's unilateral custody order. It does not require an ambassador to claim authority to bind a foreign sovereign. Ransom, monetary, trade, goodwill, compromise and changed-term agreements retain their existing explicit foreign-assent or sovereign-referral gates. Native acceptance must ask the envoy naturally about their authority, preserve an honest refusal to exceed it, then confirm the exact release and verify `releasedHeroId`, `isPrisoner=false`, `ransomGold=0`, no payment receipt, and no unrelated custody change.

## Exact relationship and favor observation (2026-09-08)

Use `reign_get_world_details` with `subsystem="relationship pair"` and `pair="heroA|heroB"` to inspect one exact pair, including a player relationship. Either identifier order is accepted. This avoids the ordinary passive relationship report's player exclusion and recent-row limit; those ordinary report rules remain unchanged. The `reign-relationship-pair-observation-v1` row includes the stored pair, both observer-specific effective attitudes, the family attention mirror, the favor contact clock, and established favor records for those two people, including forgotten records. A missing relationship is explicitly `hasPair=false`; observing it does not create one.

Compare the actual effective attitude with the family mirror before declaring a positive-relation decay test ready. Compare the saved favor contact anchor and current reputation status before and after the 30-day boundary. This diagnostic does not record contact, trigger expiration, modify affinity, call a provider, advance time, or save. Native progression and a fresh-process reload remain separate acceptance requirements. World Test and MCP catalog contracts cover this surface.

## Focused reputation reports (2026-09-08)

Use `reign_get_social_reputation_test_report` with the exact `caseId` and `includeSnapshots=false` when inspecting one Social Reputation result. This filters case evidence before export and omits campaign and longitudinal snapshots, avoiding the MCP response limit as a run accumulates snapshots. `reportFilter` declares the filter and omission; stored evidence is unchanged. The default remains the complete export. These are read-only options with no new enrollment, mutation, or cleanup gate.

The favor projection verifier compares the collapsed favor contribution separately from other standing sources, then checks the complete rounded total. A dynasty bonus must not cause a false failure, and ignoring that bonus must not hide double charges or incorrect rounding. The rumor self-tests cover these distinctions. Preserve the original failed report when correcting an assertion; do not rerun unrelated successful scenarios.

Every explicit forced fixture preparation emits new World History events. These are separate preparations, not delivery retries. The verifier uses the two native producer sequence receipts to identify the current preparation and waits for both corresponding server receipts; old completed receipts cannot satisfy that wait. It requires exactly two occurrences from the current preparation and two current established favorites, which may retain their original activation history. Reports retain all preparation events and occurrences and expose `preparationEventCount`, `uniqueSourceCount`, `currentPreparationEventIds`, `currentPreparationOccurrenceCount`, and `historicalOccurrenceCount`. Isolated checks cover repeated preparations, mismatched or absent sequence identities, and incorrect standing effects. Command-delivery idempotence is a separate contract. Generic social fixture scenarios require the complete enrollment at the run root as well as on steps; missing root enrollment now returns a normal rejection before campaign access.

## Court Life persistence row ordering (2026-09-08)

Court Life save comparison retains authoritative ages and hero existence in `nativeState.familyNativeAges`. When that field exists, the fingerprint excludes only the family mirror observation `worldDay` and cached `profile_json.age`; every other family value remains included, and raw evidence is unchanged. Isolated contracts require cached-age refresh to compare equal, while native age, child status, dismissal and row-content changes still fail. Identity-keyed members and visits remain order-independent. Prepare a new fingerprint before verifying in another game process; previous failed fingerprints are not upgraded or silently accepted.

## Sovereign recognition and castle scene prompting (2026-09-05)

Run `contracts.castle_scene_context` and `contracts.sovereign_conduct` through the artifact-bound Verification Lab `quick`, suite `contracts`. These execute room-routing, independent personality, equipment/hourly-outfit exclusion, preserved actions and kinship, exact embedded seven-culture Baths prompt hashes, formal opening and warning/intimidation checks. Baths, guest bedrooms and the royal bedroom select `adultScenery` (Adult Scenery & Events) without a caller provider override; ordinary rooms keep normal scenery and child-containing Family Chambers keep their separate path.

`contracts.sovereign_identity` runs the identity suite in quick/offline contracts; the same tests are also available as `pipeline.identity` using offline, suite pipeline. They cover authoritative qualified introductions, local civic office without name or formal affiliation, visiting/conquest/succession controls and evidence-preserving reconciliation through a fresh database connection. This is not native save/reload proof. The task capture manifest is `docs/agent/tasks/sovereign-castle-prompt-capture-20260905.json`; preserve all fourteen runtime baselines and recheck them immediately before any separately authorized deployment. Never overwrite newer prompt edits. Provider-backed dialogue, fresh image review and explicitly enrolled disposable-campaign native acceptance remain separate required coverage, not inferred from these contracts.

## Court-life acceptance preparation (2026-09-05)

International completion continuity (2026-09-09): the production resolution outbox carries `continuityVersion=1`, the native receipt, named payment parties and the actual audience witness IDs. The existing resolution endpoint checks native payment balance deltas before storing `reign-court-resolution-knowledge-v1` facts per campaign/timeline/matter/witness in `court_life_turns` (`phase=resolution`). Duplicate delivery retains one record. Individual and group live prompts include the latest six witnessed completions, which override stale plans claiming payment is still awaited. Private balances are excluded from those prompts. Legacy receipts without the new evidence remain compatible but cannot manufacture payment confirmation. Existing `--run-court-tests` covers confirmation, failed credits, acceptance without execution, fresh-connection persistence, retry and witness/timeline isolation. Extortion scene preparation and envoy briefing distinguish an informed coercive demand from an injury claim; payment does not create a peace treaty. Native acceptance must challenge the demand, complete the actual payment, inspect both balance changes and the follow-up individual/group prompt, then save/reload and confirm the demand remains completed. These are focused failure regressions using the existing guarded routes; no new runner or safety gate is introduced.

Enrolled `court` and `family-chambers` `ui_open` requires paused native time. The clock guard recognizes both the existing top-level campaign-test fields and nested `enrollment` with `campaignTestRunId`/`disposableSaveName` used by scenario files. It reasserts pause after exiting a native settlement menu and after outgoing screen cleanup, which can otherwise restore settlement waiting. Its `docketClock` evidence contains `worldDayBefore`, `worldDayAfter`, and `timePaused`; any opening-time drift fails. Pause and drain through the campaign-test controller before setup. Observation phases remain read-only; advance days only through the guarded campaign-test controller. This isolation applies to automated docket setup, without changing the player's ordinary Court time controls.

The existing `reign_start_ruler_docket_test` guarded controller now accepts `court_life_preflight`, `court_life_prepare`, `court_life_open`, `court_life_converse`, `court_life_choose_confirm`, `court_life_snapshot`, `court_life_observe`, `court_life_clock_observe`, `court_life_save_prepare`, and `court_life_save_verify`. The operation remains `ruler_docket_test`; its existing exact confirmation, enrolled campaign-test Current, active native-save comparison, and 1800-second command ceiling remain mandatory. Implementation prepares these routes; autonomous provider and campaign acceptance is explicitly deferred until the user supplies the post-deployment save and starts that phase.

Use `courtLifeSource` (`International`, `Family`, `Patronage`, `Visitor`), an exact `courtLifeTemplateId`, optional eligible `courtLifeHeroId` for family, and a distinct `fixtureRunId`. Preparation calls production eligibility and records the exact generated matter. International setup refreshes real world/personality state first. Missing relatives, historical facts, captives, ambassadors, or strategic conditions fail preparation; arrange legitimate preconditions through supported guarded fixtures and retry. `courtLifeFixtureGold` and `courtLifeFixtureLoyalty` default to -1 (unchanged); explicit nonnegative values set actual disposable campaign values and are recorded for rollback. These fixture changes never establish a decision, acceptance, completed performance, or relationship effect.

For an audience, prepare, open, and run `court_life_converse` once per natural ruler message using `rulerQuestion`. The host verifies the exact visible matter, waits 120–480 seconds according to participant count, requires provider-backed opening and replies, and proves one additional persisted player turn with exact text. If that exact pending audience reports `technical-failure`, the host uses the production technical-return control once, reopens the same bound matter, and lets its persisted per-speaker receipts resume the interrupted turn before evaluating readiness. It never applies this recovery to another, missing, resolved, or committed matter, and it reports `technicalAudienceRecovered` in the resulting evidence. Invoke `court_life_choose_confirm` with an exact current `courtLifeOptionId` only after conversation. It exercises the production review-selection and confirmation controls. No direct action call or forced NPC acceptance can satisfy this route. `court_life_snapshot` saves the native Gauntlet snapshot and structured state; visual fidelity still requires the normal rendered matrix and native visual checks.

For every multi-person Court Life audience, inspect each attributed reply as a separate speaker turn. First-person statements and stage directions may describe only the active speaker's words, gestures, clothing, equipment and actions. A reply attributed to one participant that narrates another attendee's gesture or possessions as its own is a failed presentation case even when the hearing's mechanics, ruling and persistence pass. Verify a repair with a distinct multi-person template rather than replaying the failed hearing.

The `reign-court-life-evidence-v1` result retains the complete attributed transcript, stable matter/template/source IDs, actual gold and settlement loyalty, participant locations/parties, commissions, durable works, recipient and directional-relation receipts, family attention mirror, visitor reservations, invitations, and history. `courtLifeExpectation` selects `pending`, `resolved`, `delivery_complete`, `arrived`, `departed`, `dismissed`, `neglected`, or `reconciled`; `snapshot` is observation only. `courtLifeExpectedTurns` can require an exact completed-turn count. Clock observation requires actual native time advancement plus a named behavior expectation. It never advances time itself.

Family preparation records `fixture.familyBaseline` for the exact campaign, timeline, ruler, member and visit. `dismissed` requires that visit's authoritative dismissed status and exactly one new dismissal beyond the baseline; `neglected` also requires the new zero-to-one flag transition across the actual patience threshold. First obtain a successful `neglected` observation before attempting the later natural Family Chambers reconciliation. This retains `fixture.familyNeglectEvidence` for the same visit, so `reconciled` requires a subsequent zero/zero member state even after that visit ages out of the server's recent-visit window. A preexisting dismissal, neglected member or initially clear member cannot satisfy a new transition. Missing, stale-scope or in-flight family evidence fails closed. `familyTransition` reports the baseline, current observation and retained proof. Snapshots do not create that proof.

For persistence, drain `HasPendingFamilyVisitWork`, `HasPendingInternationalCourtLifeWork`, `HasPendingCourtLifeDeliveryWork` and relation queues, run `court_life_save_prepare`, use the guarded checkpoint/restart route, then `court_life_save_verify`. A different game instance and identical paused state fingerprint are mandatory. Preserve the enrolled baseline; use one overwritten run-owned checkpoint and the existing exact cleanup marker route. Full release evidence still includes every international and patronage template, family ages and neglect transitions, 30-day favor expiry and jealousy, visitor personalities and stays, political-pressure chains, all native effects, and the 180-day natural ruler-play soak. Provider cooperation overrides may clarify one sampled reaction only; they cannot satisfy agency, trait, consent, pressure, payment, loyalty, arrival, or persistence assertions.

### Immediate noble visitors (2026-09-07)
Visitor court_life_prepare now calls production creation that teleports every eligible member to court immediately, without native time advancement. Its visitors_placed_immediately_without_time_advance assertion requires actual group residence, a Pending docket entry, unchanged time and a full stay starting at placement. An incomplete placement fails preparation and cancels/unwinds the visit; it cannot leave a scheduled arrival. Existing queued-save visits revalidate on their next processing pass without waiting for the former travel date. Use arrived observation immediately, natural conversation, and guarded advancement only for stay/departure checks. Save/reload after placement, during the stay, after invitations and after departure. Return prefers the safe original location, then a safe home; external duties/captivity win. Preserve old transit evidence as superseded by the user's revision.

Court Life captive setup: `courtLifeFixtureCaptiveHeroId` defaults empty (custody unchanged). Read `court_life_preflight.captiveInventory` for up to 64 sorted eligible IDs plus count/truncation, then make a quiescent guarded checkpoint before using an exact ID with International `court_life_prepare` and a RequiresCaptive template. Existing armed run-owned Current and fixture confirmations remain required. Candidates are actual adult foreign partyless nobles, excluding rulers, family, prisoners, assignments and quests; their peaceful kingdom must retain another eligible envoy and have no existing player-held captive. A selected hero participating in a pending matter is excluded; an unrelated pending matter in that kingdom does not exclude the hero. Preflight also reports exclusionCounts and up to 64 sorted existingPlayerHeldCaptives. Native TakePrisonerAction establishes custody; normal production eligibility and the exact captive binding must then pass. Capture intent and before/after custody are persisted as `reign-court-life-custody-v1`. Partial failures block reuse of that fixture ID: inspect the receipt and restore the guarded pre-fixture checkpoint before retrying; never save over that rollback point with a partial fixture. No payment, acceptance, pressure, war, time advancement or save is injected. International `nativeState.international` reports exact captive custody, foreign ruler gold and the player/foreign kingdom pair with `warStateKnown` and nullable `atWar`. Re-prepare save fingerprints after this evidence expansion; prior fingerprints are not fresh persistence proof. Native capture, real conversational payment/release and reload acceptance remain required in addition to catalog validation.

Court Life ruler recognition uses `formal_court_audience` only when structured courtLifeContext matches current authoritative native facts: campaign/timeline, actual speaker and ruler, ruler office and kingdom, capital/host settlement, observer location and observation day. The generic identity adapter preserves that context; formal presentation may upgrade an existing unknown acquaintance, but ordinary visits, free-text titles, another speaker or capital, stale observations and changed office/realm cannot. Existing `contracts.sovereign_identity` and `pipeline.identity` checks include `court_audience_*` qualification boundaries and actual prompt-adapter persistence through a fresh database connection. Native acceptance must inspect the real foreign envoy/visitor opening and its exact identity audit. Preserve previously verified ruling effects instead of repeating the judgment to retest recognition.

Production Test Lab `POST /tests/run` in forced canned mode uses `BuildTestMessages` and the same dialogue/event builders as game requests. It now also captures complete evidence labeled `captureStage=test_lab_assembly`; that label never claims a provider call. The existing MCP does not wrap this older Test Lab endpoint, so the before/after deployment comparison uses that documented API with fully supplied snapshots, a task-owned `verify_prompt_comparison_20260908` campaign namespace, no snapshot paths, `live=false` and `canned=true`. The comparison is restricted to the successful prompt phase, not the later action-normalization result of a deliberately action-free canned reply. Full baseline messages are available through this test route; the original in-game audit remains truncated. The installed server is verified after deployment through the same route and through isolated Verification Lab checks.

The focused quick/offline prompt_efficiency suite explicitly executes contracts.dialogue_prompt_budget and pipeline.prompt_caching. It measures assembled commoner/noble system prefixes (45000-character bound), not a sum of selected template files. A catalog-only result is insufficient proof. The existing pipeline suite still runs the same caching contracts as part of its wider coverage.



Government hearing integration (2026-09-08): The existing quick government suite now executes production hearing rules for the one seven-day nonurgent recess, urgent stage gates, eligible issue-support sponsorship, individual issue-specific votes, bounded private commitments, status-quo/stable-lot ties, and participant responsibility without duplicate role rewards. The Government manifest schemaVersion is 7 and contracts.hearingIntegration records the new rules. Native certification must exercise captured decisions, suppression of native controls/ballots, binding maximum-authority voting, actual individual-chat consent/payment/obligations, capital attendance and absentee votes, exactly-once effects, and reload. Old direct TryLobbyMember calls or a generic loaded-state assertion cannot satisfy this acceptance. The native feature fingerprint includes saved hearing cases and owned native decision ordering; attendance and commitments must also be bound. Government Trust is inert compatibility data. All live relations remain hidden; only dated spymaster discoveries are historical snapshots. No deployment or native test is authorized before the user's requested deployment pause/approval, and every later campaign operation still requires the existing exact enrolled disposable Current. Existing provider/native evidence and release gates are not waived by the new offline tests. GOV-NATIVE-008/009/010 use a two-pass acceptance protocol: first retain the exact member/matter/payment/receipt baseline and report pending failure; conduct ordinary Individual Chat, complete deal progress in production, and rerun the same case in the same native instance. Only a new scoped receipt counts; reserved context alone cannot mint consent. Cleanup removes its retained baseline. GOV-NATIVE-018 includes enforced native decisions and proves both NotifyPlayer and NeedsPlayerResolution suppression. The explicit constitutional action classifier and bilateral counterpart approvals have deterministic coverage; actual interception/execution still require native evidence.
