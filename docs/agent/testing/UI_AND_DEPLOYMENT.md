# Interface proof and deployment

Tavern cast deployment copies `ModuleData/reign_tavern_cast.json` from the exact validated client output inventory into the installed module's `ModuleData` directory. The transaction records it as `validated-client-content` in its support mappings, so source hash, build provenance, before/after evidence and rollback apply. This data file is outside the independently frozen 318-file core UI authority. The optional generated portrait pack requires its own reviewed artifact inventory and is not implicitly included with cast data.

The provider-free multiline regression is `node ReignBeta/tools/GauntletXmlPreviewer/test-rich-text-cover-children-contract.mjs --output <task-evidence-directory>`. It uses the existing local preview server and a dedicated headless Edge profile under that output directory. Eight real browser cases cover plain and position-offset TextWidget text and plain/action-formatted RichTextWidget text at widths 240 and 420. CoverChildren preserves intrinsic wrapping; inline formatting spans must not replace the full text's measured height. The test checks text, scroll and parent bounds and retained italics, writes `multiline-cover-children-contract.json`, and closes only its own browser. Shared renderer changes also require the complete catalog render matrix and applicable source, typography and fidelity audits; this test does not provide native campaign acceptance.

Read only the procedure relevant to the current task. Existing provider, campaign, and deployment gates remain authoritative. Dated procedure history was retained during the 2026-09-12 guide split.

## Control Center navigation (2026-09-09)

Full style extension (2026-09-09, requested after the navigation preview): all Control Center panels, headings, controls, tables, logs, character/standing cards and dynamic relationship matrices now use the canonical `palette.json` tokens. Legacy brown/bright-gold gradients and glow are removed at their original rules. The existing browser contract additionally audits computed foreground/background/border colors on every page, production-populated character and world renderers, focused/disabled inputs, hover/active buttons, checkboxes and file selectors. Brand and portrait pixels, page layouts, form values and backend behavior remain unchanged. `control_center_modern_style_contract` extends the existing architecture suite.

`reign-control-center-navigation-ui-v1` groups the ten everyday pages plus Diagnostics in the main navigation. Diagnostics exposes Overview (the original Diagnostics page), Logging, Log Viewer, Audit Viewer, NPC Dialogue Audit, World History, NPC Dialogue Lab, Relationship Director, World Test, Test Lab, Live Test Bridge and Verification Lab. Returning to Diagnostics restores the last opened child; switching pages preserves unsaved inputs. Existing loaders and backend operations retain their behavior.

Run the existing artifact-bound `interaction_architecture` offline suite. Its `control_center_navigation_preview` result names the exact emitted HTML fixture directory. Then run `ReignBetaServer/src/Modules/Platform/control-center-ui-contract.mjs` with that directory and an evidence output directory, using the existing Node/Playwright installation (`REIGN_PLAYWRIGHT_MODULE`; Edge is the default). The 66-case matrix checks every page at 1672, 1024 and 600 pixels, loader dispatch, keyboard access, selected states, remembered child, preserved drafts, navigation bounds and frozen palette tokens. Inspect the screenshots and `ui-report.json`. MCP has no web-preview command; this shared local fixture harness is the documented fallback.

All browser traffic is intercepted and page loaders are observed at their dispatch boundary. No provider, campaign, listener, save or live Control Center is contacted. This proves web navigation, not backend tool execution. No native prefab, portrait, approved-reference raster or Bannerlord adapter is changed, so native preview/fidelity and game acceptance are not applicable to this navigation edit. Install only validated artifacts through the visible unified lifecycle if deployment is requested.

Legacy audit: removed 202 lines of unbound director-sample and retired relationship-simulator JavaScript plus its unused chart/table/progress styles. The simulator controls were already absent and its four server routes explicitly return retired/no-state-change responses. Keep those compatibility responses. Relationship Director still observes current MBTI/lifecycle/political-marriage state; its Ambient Sample uses the current snapshot route. Test Lab still provides editable deterministic/provider replay cases and correlation import; Verification Lab orchestrates broader gated checks; World Test remains the passive observatory. Those active tools are not obsolete merely because their names overlap. NPC Dialogue Lab, Live Test Bridge, audits and log viewers remain reachable. No saved test cases, test history, runtime data or campaign content was deleted.

## Combined alpha deployment packaging (2026-09-06)

The shared `deploy-ui-transaction.ps1` consumes exactly two `conversationActions.runtimeFiles` authorities for the italic font definition and action brush, in addition to atlas-manifest assets. With the Government and Clan Accords interfaces included, the 2026-09-08 frozen counts are 311 core (21 prefabs, 52 atlas sheets, 233 sprite sources, three registry/manifest files and two client assemblies), 11 auxiliary, two fixed contracts (324 runtime authority files), plus 17 protected map assets. The server's validated `native-portrait-generator` directory is included under `server/app/native-portrait-generator`; its files use the same recorded artifact inventory, transaction, backup and hash checks. Plan/Apply still requires exact source and plan fingerprints, stopped runtime, backups and hash verification. The deployment inventory/contract regression checks the frozen count and style-contract hash against the current authorities before installation. No generated verification data is deployed.

### Native capture presentation contract

Every one of the 21 runtime interface captures exports its headless runtime snapshot before the screenshot. `ReignUiCalibrationOverlay` remains a provider-free XML Previewer state with `nativeInjection=false`; Bannerlord must never load it as a second movie or expose its launcher, panel, selection outline, or input canvas. `capture-native-matrix.ps1` fails before pixel capture if a snapshot contains any calibration-overlay movie, widget, action, or `supportUi` payload. There is no native `calibration-overlay` target and no native expand/collapse action.

The retained 3440x1440 engine snapshots report a 2580x1080 logical root at `UIContext.CustomScale=1.33333325`. A locked 16:9 workspace is therefore uniformly scaled from the 1080-pixel logical height and horizontally centered with expected ultrawide side gutters; that centering is not stretching or a production layout defect. Treat clipping, non-uniform scale, or geometry that differs inside the centered workspace as defects, but do not widen fixed production layouts solely to remove the expected gutters.

### Typography parity contract

The preview contract audit also emits `reign-ui-visual-style-contract-audit-v1`. It walks every standalone prefab and every cataloged native-augmentation source, rejects literal colors outside `artwork/ui-modern-style-kit/palette.json`, and rejects the legacy `ButtonBrush2` visual brush. Alpha-only modal dimmers may vary alpha only when their RGB identity is an exact palette token. White is limited to identity tinting of registered image sprites and declared Calibration Overlay controls. This check is structural: rendered preview, native capture, and human review remain responsible for proving that a registered image itself has the approved material, restrained gold intensity, geometry, and layering.

Typography uses a hybrid fixed-art/live-text contract. Fixed titles, section labels, ornament-integrated captions, and fixed control ornaments are exact approved pixels baked into their shells or registered sprites and must not survive as hidden or transparent runtime TextWidgets. Run `node ReignBeta/tools/GauntletXmlPreviewer/audit-typography-parity.mjs --output .codex-build/ui-preview/typography-parity-audit.json`. The auditor loads `calibration/full-catalog-typography-parity-contract.json`, discovers every current catalog surface, and compares every remaining `TextWidget` and `EditableTextWidget` by stable structural XML path. The current source inventory covers 22 catalog surfaces over 21 unique prefabs and 535 live widgets: 176 literal runtime strings and 359 dynamic-bound values; retained baseline differences remain audit failures until reviewed. `wilderness-event` is explicitly declared and audited as shared-prefab inheritance from the `social-event` typography authority. A `Text` value is dynamic-bound only when its decoded, trimmed XML value begins with `@`; only its wording may vary. Literal/bound mode drift fails. Every live text widget must explicitly declare `Brush.Font="ReignSerifDynamic"`, the packaged module-local Cormorant Garamond Medium SDF face; relying on `DefaultText` or `Info.Text` alone fails because native Gauntlet may silently fall back. Brush identity, declared face/weight/style, font size, exact frozen palette token, opacity, tracking/spacing, alignment, position offsets, and width/height/margin geometry governing wrapping must match exactly. The same audit verifies the FNT/page mapping, decoded literal glyph coverage, upstream provenance, OFL license, SpriteData registration, runtime manifest, and packed sheet pixels. Active synthetic outline, glow, blur, or shadow attributes also fail; absent or explicitly inactive zero values remain valid. Every mismatch is retained by screen, catalog surface, widget Id/path/line, attribute, expected value, and actual value. Run `node ReignBeta/tools/GauntletXmlPreviewer/test-audit-typography-parity.mjs --output .codex-build/ui-preview/typography-parity-self-test.json` for isolated positive and negative contract proof, including missing/wrong `ReignSerifDynamic` declarations, silent fallback risk, asset-chain drift, decoded numeric entities with missing glyphs, and active synthetic effects. Updating the baseline requires explicit `--write-contract ReignBeta/tools/GauntletXmlPreviewer/calibration/full-catalog-typography-parity-contract.json --confirm-freeze-current-typography`; replacing an existing baseline additionally requires reviewed use of `--force`. Never rebaseline merely to silence a divergence. This contract proves XML declarations and the packaged asset chain, but not native Gauntlet glyph resolution, face/weight, baselines, hinting, tracking, hierarchy, or rasterization. Those remain a reviewed native-capture and human-acceptance gate beginning at 2560x1600; defects found there must be corrected before the remaining supported 1080-or-higher resolutions are attempted, with approved-reference text-region comparison wherever dynamic-text rectangles exist.

For standalone native capture, `capture-native-matrix.ps1` verifies the requested catalog case against Bannerlord's configured `UIScale`. The snapshot's `UIContext.CustomScale` is a separate positive runtime-derived value, not the configured value; both identities are retained and the parity audit binds the recorded runtime value back to the exact snapshot. After each campaign `game start`, both provider-free campaign capture batches (`capture-native-matrix.ps1` and `capture-native-augmentation-batch.ps1`) self-arm the ReignLiveTest bridge for 240 minutes, require the resulting arm receipt to report `ok=true` and `armed=true` before the first `ui-open`, and fail closed otherwise. They do not accept stale external prior arming as a prerequisite. This arming authorizes capture UI operations only; it neither advances campaign time nor saves the campaign. Royal Council `ui-open` uses a non-persistent four-domain transcript fixture, exposes `royalCouncilCalibrationFixture=true`, and must reach a ready non-empty transcript with zero provider calls before capture; ordinary player entry remains production-backed. The screenshot helper makes its thread per-monitor-DPI-aware before measuring physical pixels, uses bounded attached-input-thread retries to acquire and prove Bannerlord foreground ownership, then restores the user's prior foreground window and DPI context; failed focus acquisition preserves readiness and snapshot evidence. When the verified engine render target exceeds the desktop and Windows reports clamped bounds, the helper triggers Steam's foreground DirectX backbuffer screenshot, requires the exact dimensions, converts it to the evidence PNG, and copies the source JPEG into the exact case evidence directory. It verifies the retained SHA-256 before removing only the exact new Steam screenshot and same-name thumbnail, records the original and retained paths plus removal receipts, and never performs a broad Steam-library cleanup. Full-content `PrintWindow` is restricted to exact off-screen clients and rejects blank output; on-screen cases retain the ordinary pixel-copy mode. Acceptance manifests are written directly beneath the batch `-OutputRoot`, the same evidence root consumed by `audit-native-parity.mjs`. A zero-stage batch retains every per-case path and original error, skips empty contact-sheet generation, and exits nonzero. It never advances or saves the campaign, and all staged captures remain `reviewed=false` until a human explicitly reviews and promotes them. Promotion requires `accept-native-case --reviewed --review-confirmation 'I confirm this native capture matches the approved Reign reference for all fixed visuals and typography'`. That command writes a versioned nine-point attestation covering exact fixed artwork, declared dynamic-region confinement, approved serif face/weight, hierarchy, capitalization/tracking, semantic gold/gray colors, alignment/baseline/wrapping, native glyph rendering, and absence of unexpected colors or shapes. Native parity rejects a missing, false, unknown, legacy, or incomplete attestation rather than treating a bare reviewed flag as acceptance.

Use `capture-native-display-matrix.ps1` when more than one catalog display case is required. It requires the exact confirmation `temporarily configure Bannerlord display matrix and restore it`, refuses a running game, and accepts only catalog `resolution@scale` keys. Before the first mutation it saves byte-for-byte `BannerlordConfig.txt` and `engine_config.txt` backups beneath the workspace evidence root. While Bannerlord is stopped, it changes only `UIScale`, `display_width`, `display_height`, and the validated capture `display_mode` (default `0`), delegates the case to `capture-native-matrix.ps1`, and stops Bannerlord again. Add `-NativeAugmentations` to delegate each case to `capture-native-augmentation-batch.ps1` instead. Its `finally` path restores both files from the retained bytes and verifies the original SHA-256 hashes even after a failed case. The `reign-ui-native-display-matrix-v1` checkpoint records its surface type, every child batch/contact-sheet path, capture mode, configuration restoration, and final game-stop proof. It never advances or saves a campaign, and it never marks a capture reviewed.

The lossless 16,384-pixel authored War Council master is exported as exactly sixteen RGB PNG 4,096-pixel runtime tiles. The map-specific client path passes the already-audited bytes directly to Bannerlord, bypassing the generic managed PNG decode/re-encode step. Record first-open and reopen latency from the live-test command plus RGL texture-load timestamps, retain exact PNG header/dimension and pixel-reconstruction audit evidence, and require a native screenshot proving the raster actually rendered.

The War Council uses one 44x close fixed scale. Its official land-gated river mask is carved at its actual source-variable widths into the same unified 16K land/water contour used by seas and lakes; rivers are not skeletonized or redrawn as fixed-width centerline overlays. Native acceptance must show a visible 108-pixel player main-party figure even when the player has no kingdom or subordinate clan parties, plus every other eligible player-realm and detected foreign party.

Family Chambers uses `ui-open --target family-chambers`, exact `select-adult`, `select-child`, `toggle-adult`, `toggle-child`, and `deselect` actions, the `familyChambers*` state fields, and a native full-screen snapshot. Prove linked parent/child selection, independent later deselection, and that automatic family highlighting may exceed four while `familyChambersCanEnter` stays false until one through four NPCs remain. Opening and roster actions are provider-free. Enter Scene is provider-backed and must run only with the applicable provider and guarded disposable-campaign gates; its acceptance requires the 1536x864 scene, age-appropriate child turns, safe child-only persistence, retained fixed-slot UltraFace reference evidence, exact-age refresh after two game years, and adulthood migration that retains only experiences from exact age five onward.

Training Yard uses `ui-open --target training-yard` from a guarded campaign at a town or castle, with `ui-action --target training-yard --text select-trainer --value <hero-id>`, `train`, and `stop`. The `trainingYard*` state fields prove roster availability, the active transition, status, and delivered XP. Provider-free preview covers empty, long-name, disabled, scrolling, and portrait-layering states without mutating campaign data. Live Train/Stop and hourly-XP proof requires an enrolled disposable campaign: verify the divide-by-six floor rate, full per-soldier stack multiplication, native threshold clamping, wounded inclusion, maximum-tier and upgrade-ready exclusion, ordinary wage/food passage, manual upgrades, and safe interruption on invalid trainer, party departure, combat, siege, close, or host loss.

Native War Council pointer acceptance records bounded receiver frame, held-frame,
press, move, release, scroll, logical-bounds, and last-pointer evidence through the
`warCouncilMapInput*` fields. A native drag must increment a press or held-frame
path and change the bounded offsets. Map-wheel proof must increment the scroll
path without changing the fixed scale.

War Council art validation uses the supplied dark/light carved miniature set only
for cavalry, infantry, archers, and ships. Towns, castles, villages, canonical
names, roads, bridges, mountains, forests, and terrain are part of the base raster
rather than runtime children. The render audit must prove the editor source hashes,
440 exact settlement coordinate placements, the complete 69,500-transform `flora.bin` source plus non-overlapping exact-root representatives,
39 strategic mountain anchors plus editor-height-derived mountain ranges, 39 bridge placements clustered into 33 physical
sites, seven material layers, zero procedural static placements, zero water
placements, 200/150/100-pixel town/castle/village target widths (75%/50%), and lossless reconstruction
of all 16 runtime tiles from the 16,384-pixel master.
Bridge art is a strict orthographic top-down deck footprint with restrained
cartographer ink: no visible arch, side elevation, façade, isometric angle, or
game-style 3D shading. Each bridge is rotated and sized to its measured official
river crossing and extends only far enough to meet both banks.
The land registration must keep all 440 authoritative settlement coordinates on
land. The vertically registered river texture must place 32 of the 33 physical
bridge sites within five world units of authored river water; the remaining
official coastal-approach site is reported separately. Every river crossing is
baked as an illustrated timber deck with rails, cross-planks, and abutments,
sized to the measured local channel width and extending onto both banks.

Current Social Reputation isolation supersedes the older namespace-only notes in
this section: prepare and arm a guarded campaign test first, pass its exact
`campaignTestRunId` to Social Reputation prepare and execution, and let MCP derive
the protected baseline, timeline, prefix, and run-owned `Current`. The native host
must independently match Bannerlord's exact active slot before mutation. A newly
owned Current on the same campaign/timeline/main-hero boundary may replace legacy
saved enrollment only through this proof, clearing stale receipts and overrides
with a `guarded_campaign_test_reenrollment` receipt. Checkpoint, restart, and
cleanup remain owned by the campaign-test controller.

Always follow the selected manifest’s exact confirmation, isolation, fixture, and cleanup contract.

The carry-forward surface also includes the audited `party_agency_hostility_language_fixture_only` scope. It requires identical client and server builds, rejects PA-LANG-017, PA-LANG-018, and the final-fingerprint case, and carries only unrelated durable passes with full provenance.

The audited `party_agency_partyless_invitation_fixture_only` scope is narrower still. It requires identical client and server builds, rejects PA-NATIVE-001 and the final-fingerprint case, and permits unaffected durable passes to survive a test-manifest correction that answers a partyless mercenary clan leader's identity, purpose, and compensation questions in natural player language.

The audited `party_agency_remaining_native_invitation_fixture_only` scope applies only to native cases that had not yet passed when the remaining invitation templates were expanded. It requires identical client and server builds; preserves PA-NATIVE-001, PA-NATIVE-002, PA-NATIVE-018, PA-NATIVE-021, and all language cases; and rejects every other native case plus PA-NATIVE-022. The detailed turns answer identity, purpose, tactical plan, compensation, independence, provisions, and civilian-safety questions before requesting explicit consent.

The audited `party_agency_ineligible_native_fixture_only` scope applies only to a client-side correction that synthesizes otherwise unavailable governor and prisoner roles and selects existing companions for fail-closed eligibility proof. It requires the server build to remain identical, permits the client build to change, and rejects all PA-NATIVE-008 variants plus PA-NATIVE-022. It cannot carry any ineligible-candidate result across that fixture repair.

The narrower audited `party_agency_companion_native_fixture_only` scope applies only when the guarded disposable fixture must synthesize a native player-clan companion because the enrolled baseline has none. It requires the server build to remain identical, permits the client fixture build to change, rejects only PA-NATIVE-008::companion and PA-NATIVE-022, and may preserve already-proven governor, prisoner, dead, child, and quest-bound rejection results.

The audited `party_agency_native_time_driver_only` scope applies only to the guarded disposable client harness that advances native campaign time for Party Agency cases. It requires the server build to remain identical, permits the client harness build to change, and rejects PA-NATIVE-009 through PA-NATIVE-016 plus PA-NATIVE-022. The time driver enters Bannerlord's native settlement wait when available, temporarily defers unrelated active guest review boundaries until the selected fixture reaches its boundary, restores those schedules before returning, and records both the wait route and deferral count in evidence. It never changes the enrolled baseline or carries an affected longitudinal result.

The audited `party_agency_save_roundtrip_state_driver_only` scope applies only to the client-side PA-NATIVE-017 save-stage fixture. It requires the server build to remain identical, permits the client fixture build to change, and rejects PA-NATIVE-017::Returning, PA-NATIVE-017::Completed, and PA-NATIVE-022. After natural dialogue has queued and accepted departure and Party Chat has closed, the fixture supplies one call through the production hourly lifecycle seam so a `Departing` record can enter `Returning`; the `Completed` variant then expires the return timer through the existing guarded tick. It cannot carry either affected phase, mutate the baseline, or substitute a direct guest action for natural consent.

For PA-NATIVE-017 carry-forward only, the audit treats the natural staging run and its post-restart verifier as one same-case, same-variant evidence set. The newest report must be completed, required natural player turns may come from the staging report, and every related report must pass the direct-action-bypass check. This preserves the intentional rule that staging remains non-passing until a guarded checkpoint and reload, without forcing an already-proven lifecycle phase to be replayed merely because its transcript and reload assertions are stored separately.

The audited `party_agency_simultaneous_review_queue_only` scope applies only to the client-side production repair that promotes every already-past-due active guest before the first review popup locks campaign time. It requires the server build to remain identical and rejects PA-NATIVE-013 plus PA-NATIVE-022. Single-guest fixed and open-ended review reports remain carryable only when their durable current-manifest transcript, native postconditions, and no-bypass evidence still pass; no review is promoted before its stored due timestamp.

The audited `party_agency_departure_language_fixture_only` scope applies only to natural player dialogue used by the disposable departure fixtures. It requires identical client and server builds and rejects PA-NATIVE-015, PA-NATIVE-016, every PA-NATIVE-017 lifecycle phase, PA-NATIVE-020, and PA-NATIVE-022. Unaffected passes remain carryable only when their durable current-manifest transcript and no-bypass evidence still pass. The revised fixture must settle its own promised compensation in natural language before requesting departure; it does not weaken the production rule that an NPC may refuse to leave.

The audited `party_agency_recovery_fixture_only` scope applies only to the client-side PA-NATIVE-020 recovery fixture. It requires the server build to remain identical, rejects every PA-NATIVE-020 variant plus PA-NATIVE-022, and preserves unrelated completed evidence. The fixture records the phase produced by natural departure dialogue. Death and capture are applied directly to an active or refusing guest so terminal recovery does not depend on consent to leave. Missing-source-party and invalid-return-target variants apply their guarded native failure state before a clearly recorded disposable-test transition into the production return lifecycle, after preserving the NPC response; this is fixture setup, not guest consent or a production departure action.

The narrower audited `party_agency_missing_return_recovery_fixture_only` scope applies only when the client recovery fixture changes the ordering for `PA-NATIVE-020::missing_source_party` and `PA-NATIVE-020::invalid_return_target`. It requires an identical server build, rejects those two variants plus PA-NATIVE-022, and preserves the already-proven death and capture variants. It may not carry either corrected missing-return variant.

The audited `party_agency_final_fingerprint_harness_only` scope applies only to the MCP-side final certification boundary. It requires identical client and server builds, rejects PA-NATIVE-022, and preserves all 59 prerequisite results after their durable reports are revalidated. The native final observation proves client runtime contracts; `reign_evaluate_party_agency_release_readiness` alone proves the MCP-owned prior-report set and immutable source, runtime, provider, catalog, Bannerlord, campaign, timeline, and disposable-save fingerprints.

For those two variants, the disposable fixture temporarily clears the record's source-party reference before native party destruction so the production protected-camp destruction patch cannot correctly block the test mutation. It records proof that the party was absent (and, for the invalid-target case, that both return references were invalid) before the production return tick. The missing-source variant then restores only the original now-missing party id so production resolves the real missing-party path; the receipt never treats a later native fallback or succession party as proof of the injected state.

The audited `party_agency_return_timing_evidence_only` scope permits only a client-harness repair to PA-NATIVE-015 return-distance positioning or its durable timing receipt. It requires an identical server build and rejects every PA-NATIVE-015 variant plus PA-NATIVE-022. The receipt records whether the planned duration came from the live Returning countdown or, when a short return completed before receipt assembly, from the persisted due day minus the fixture's paused agreement start. The manifest names `minimum`, `intermediate`, and `maximum` must exercise the 1-hour clamp, an unclamped duration, and the 12-hour clamp respectively.

The audited `temporary_guest_waiting_camp_encounter_exclusion_only` scope applies only to a client-side production Harmony repair and its native hostile-battle evidence. It requires the server build to remain identical, permits the client build to change, and rejects PA-NATIVE-009, PA-NATIVE-010, PA-NATIVE-018, every PA-NATIVE-019 battle variant, and PA-NATIVE-022. Those cases must be rerun because they own long-lived camp protection, camp interaction/finance neutrality, pre-warning combat exclusion, or native battle participation evidence; unrelated completed cases may be carried only after their durable reports pass current transcript and bypass checks.

The hostile-battle fixture does not rely on campaign army strength. After creating the requested native field, siege, raid, naval, or simulation map event, it sets Bannerlord's player side as the override winner and makes the opposing side surrender. A receipt is valid only when Bannerlord reports the player side as the winner, the main party belongs to the winning side, the main hero is not a prisoner, and the main party remains active. Every branch is then discarded by reloading the run-owned disposable checkpoint before another case starts.

Hostility warning cases PA-LANG-017 and PA-LANG-018 are self-contained and retain the ordinary safe-settlement start gate. Each creates a guarded foreign party-leader fixture, obtains consent through the manifest's natural production invitation, uses the fixed native-war fixture to queue a mandatory targeted review, and only then sends the manifest's natural warning-decision language. They do not rely on continuity from PA-NATIVE-018 and therefore never require replaying that already-proven battle case.

## Preview parity visibility

The previewer's **Every-interface parity gate** keeps provider-preview coverage for all 68 browser interface states while installed-runtime and reviewed-native readiness cover all 37 native surfaces (22 runtime interface states and 15 native augmentations). It identifies exact deployment and native-evidence pending targets, exposes their reasons, and sends the same bounded state with selected-widget Codex context. Provider-free capture mode disables the live refresh to prevent the 166-case matrix from multiplying readiness audits.

# Concurrent scene preparation and native action font binding (2026-09-06)

`ScenePreparationTests` under the existing MCP test project exercises the production scene coordinator with delayed providers: dialogue/art overlap, shared pending art on reopening, failure isolation and retry. It also resolves every custom font descriptor page to a registered SpritePart and source texture, catching the null-font-sprite RichText failure observed in Baths. Run through manifest-selected `reign_validate`; no provider or campaign is needed. Native mixed-action text rendering and art-first/dialogue-first/close-reopen acceptance remain separate. See `AI_ART_DIALOGUE_FLOW_AUDIT_2026-09-06.md` for the full system inventory.

`court_life_family_fixture` is a separately authorized household setup profile on the exact armed disposable Current. It requires an empty household and a living male ruler aged at least 32. It creates an adult wife through native marriage eligibility and MarriageAction, then a four-year-old daughter and fourteen-year-old son using native CreateChild at their final integer ages. It preserves the children's NotSpawned state, establishes parentage once and capital residence, and returns `reign-court-life-family-fixture-v1` evidence with native family inventory and durable creation IDs. Reusing the same fixture ID verifies/reuses that household instead of creating duplicates. Inspect partial failures before retry; restore the preparation checkpoint for conflicting identities. It never injects attention, favor, neglect, NPC acceptance, or court decisions, advances time, or saves. Drain production family synchronization before checkpointing. `court_life_preflight` reports native player/family inventory for independent checks after reload.

Creating a reusable named baseline is a separate user-authorized operation. Start a separate preparation run from the exact original baseline, create its guarded disposable Current, apply only the authorized household setup, drain and checkpoint, then save through native Save As and verify Save Sync and a different-process reload. Do not rename an acceptance save containing prior scenarios into a baseline. Preserve the original baseline and create another enrolled disposable Current from the new baseline before further testing.
Save Sync startup preservation is covered by the manifest-selected persistence `--run-save-sync-tests` cases `startup_database_failure_preserves_save_files` and `startup_missing_snapshot_preserves_legacy_recovery`. The tests inject a database probe exception and an absent legacy snapshot against retained file/ledger fixtures. Startup is observational and preserves those bytes; neither a database outage nor a missing schema authorizes deletion. Availability requires the PostgreSQL snapshot, campaign directory, and manifest. Missing file payloads require verified backup recovery before loading, without releasing alignment against unrelated current data.

The optional `courtLifeFixtureAgeRulerTo34` flag defaults to false and applies only to `court_life_family_fixture`. With explicit user authority to adjust the disposable test ruler, true sets an existing adult male ruler aged 18 through 34 to age 34 before household creation. The fixture records prior/resulting age and completion, requires the same flag on retry, and never repeats a completed adjustment, rejuvenates an older ruler, converts a child ruler, or advances world time. All empty-household, exact armed Current, confirmation, marriage eligibility, and separate named-baseline promotion gates remain in force. The default still requires age 32 or older and preserves age.

Household fixture origin selection uses the ruler culture native LordTemplates plus matching adult female noble source characters, requires native CreateSpecialHero to clone its source without changing the original hero, and records template/culture/origin-clan IDs. It prefers a same-culture clan in the player kingdom, then a same-culture clan in another kingdom at peace with it; hostile kingdoms remain excluded and native marriage eligibility is still required. Missing template and missing clan are reported separately before creating a wife.

## Scoped provider-free interface previews (2026-09-08)

Run `node ReignBeta/tools/GauntletXmlPreviewer/audit-rendered-preview.mjs --targets clan-accords,economic-report --output .codex-build/clan-accords-art/targeted-verified` against the existing preview server to check both interfaces at 1920x1080 and 3440x1440. A base ID includes every declared preview state; an explicit state or native augmentation ID selects that case. Unknown, empty or duplicate IDs fail before browser launch. `js/rendered-preview-targets.js` owns selection; `test-clan-accords-contract.mjs` covers base/state/augmentation selection, invalid input and retained full catalog counts. Scoped runs retain inventory, asset, binding and per-case diagnostics plus direct manipulation and custom scale checks. The report records `partialScope=true`, `requestedTargets`, and full `catalogInterfaceStateCount`, `catalogAugmentationCount`, and `catalogPrefabCount`; aggregate readiness is explicitly `not-run-partial-scope`. Store scoped reports outside `.codex-build/ui-preview` so every-interface readiness discovery cannot mistake a targeted artifact for the global report. Run approved-reference fidelity with the same IDs, the registered contract and each retained viewport. A scoped pass never establishes full-catalog completion or native acceptance; the unfiltered matrix and native evidence remain separate gates. This option authorizes no provider call, game mutation or deployment.

Government preview registration (2026-09-08): The current catalog contains 21 unique prefabs, 56 interface states and 15 native augmentations: the complete two-resolution matrix has 142 cases. Government includes its base hearing and nine declared variants, including government-proposals (20 targeted cases). Native acceptance covers 21 runtime interface targets plus 15 augmentations, spanning 20 runtime movies. Provider-free targeted evidence remains explicitly partial.

Compound artwork alpha verification uses the existing `audit-overlay-alpha.mjs --self-test` and ordinary audit command. A shape `compound` declares 1..16 noncompound `apertures`, each with shape and pixel aperture geometry; rectangle children may additionally declare `alphaFade`. The shared audit records each child in `analysis.apertures`, checks every child independently and tests the exterior against their union. Child threshold overrides are forbidden. Explicit fade pixels must match expected alpha within one rounding level, and the fully clear remainder must meet the ordinary transparency and bounds gates. Opaque corners, transparent RGB, and contamination checks remain enforced. Use this for integrated Government cards with both portrait and banner openings and for its intentional scene fade; never erase, flatten or relax checks to conceal real exterior leaks.
