# Characters and portraits

Read only the procedure relevant to the current task. Existing provider, campaign, and deployment gates remain authoritative. Dated procedure history was retained during the 2026-09-12 guide split.

## Character budget and portrait import recovery (2026-09-13)

Character creation defaults to a maximum of **50,000 output tokens per request**, including the provider's reasoning allowance. This supplies technical headroom; a complete response ends the work immediately. Following the user's 2026-09-13 clarification, prose length targets are soft prompt guidance: realism, native accuracy and consistent roleplay take priority. Narrative acceptance requires populated fields, clean references and consistent character structure; it does not require padding a summary, voice or concern to a preferred character count. Existing small settings migrate once on the updated server's normal settings load; `characterConstructionBudgetInitialized` prevents repeated migration. Later explicit lower settings remain available, and both settings and request construction cap higher inputs at 50,000. The existing Control Center number field now permits 50,000; its layout and artwork are unchanged.

`character_narrative` quick/offline contracts verify the real chat payload, full and targeted repair budgets, concise complete content, an empty `finishReason=length` response, checkpoint preservation, and complete-profile reuse. `authoringAttempts` now includes `maxTokens`. Complete content is accepted even when the provider reports `length`. An incomplete length-limited response that made progress repairs its missing sections; one that made no progress records `output_limit` and stops an identical retry. Required content, readable native references, identity/personality and concern-consistency checks remain in force. Construction engine 3 allows earlier parse failures, including the old 5,900-token failure, to retry at the next normal interaction without deleting profile state. The existing character-profile contracts verify one-time settings migration, the technical ceiling, old-engine retry and current failure retry policy. No provider is contacted by these contracts. Native response quality and actual model usage remain live acceptance.

`PortraitDerivativeCore.PortraitCompositionError` in `ReignPortraitDerivativeCore.cs` is the shared composition authority compiled into both client and server: one finite valid face; face height 8-18%, top 3.5-18%, center 35-65%. Both sides retain their master/source/receipt integrity checks. The portrait derivative contracts include Vaminesa's measured 8.89481008% face, invalid/non-finite/boundary inputs, client wiring and completed-result recovery. Terminal client product validation retires only the matching pending operation; resource or commit failures retain delivery recovery. Existing accepted images and earlier portraits are preserved. After deployment, the first Look can import the saved Vaminesa operation without another image-provider call. Native import/display is a separate acceptance step; the wiring check alone is not native proof.

The `contracts.native_portrait_product_pipeline` source contract verifies the shared bounds, both consumer calls and both project Compile links. Its evidence includes `sharedCompositionContractPath`, `sharedCompositionValid` and `clientProjectPath`; the existing identity-source, product-integrity and packaged-generator checks remain. Behavioral derivative tests separately exercise valid/invalid focus inputs.

Use the manifest-selected `changed` plan and `reign_validate`, then the immutable successful artifact's `character_narrative` and portrait suites if the plan did not run them. Preserve the user's deployment hold. Runtime settings migration happens only after the validated server is deployed and started through the visible unified launcher. Any provider-backed dialogue retry or new image request retains the existing usage and campaign gates.

## Encountered residents and Homes (2026-09-06)

The saved commoner household (`reign-commoner-household-v1`) resides in `profile.json.commonerHousehold`, already part of Save Sync. The resident suite adds 140 bounded deterministic examples of marital/parenthood/name variety and birth chronology, a JSON roundtrip plus native refresh/recruitment and reused-ID checks, and native kinship-repair boundaries. All five production conversation envelopes retain household canon despite empty native family fields. This creates narrative relatives only. A subsequent native conversation and the existing rolling checkpoint/reload are the brief live acceptance route; no separate household mutation or provider test gate is added.

`resident_native_leave` complements the native approach command when raw native key input is unavailable. Pass the same exact enrolled Current identity, `expectedGameInstanceId`, and confirmation `leave resident scene on enrolled disposable save`. It requires a paused friendly settlement mission with the native agent handler, no native/Reign conversation, no battle or MapEvent, and ready/aligned Save Sync. It checks every native mission-logic leave refusal/inquiry before requesting normal mission exit; it never dismisses a dialog, changes time, saves, promotes or recruits. Observe that the mission actually ended before calling the guarded rolling checkpoint: native SaveAs cannot start while the mission remains active. The shared save preflight now refuses an active mission with `nativeMissionActive=true`, `nativeSaveQueued=false`, before enqueueing SaveAs. Evidence schema `reign-resident-native-leave-v1` records identity and the exit request; it is not proof of completed exit or persistence.

For a bounded native approach when UI movement is unavailable, the shared LiveTest `run <scenario.json> --visible --json` controller accepts one `resident_native_approach` step in `individual_chat` mode. The currently exposed Reign MCP catalog has observation and campaign enrollment but no generic command dispatcher; this documented controller is the supported fallback. Read the exact enrollment through `reign_get_campaign_test_status` and a fresh target through `reign_get_live_test_status` first. Put the enrollment on the step with `campaignId`, `timelineId`, `mainHeroId`, `campaignTestRunId`, `savePrefix`, `disposableSaveName`, and `protectedBaselineSaveName`; use the enrolled Current, never a caller-inferred disposable name. Include `expectedGameInstanceId`, `agentIndex`, `sourceTemplateId`, `bodySha256` and exact confirmation `approach resident on enrolled disposable save`. The client independently requires the armed bridge, exact loaded Current/campaign/timeline/player, aligned Save Sync, paused peaceful settlement mission, no native conversation, and the same eligible adult body/template/index before movement. The command teleports the player to the existing agent and opens ordinary native dialogue. It makes no provider call, creates no identity or fixture, invokes no recruitment/action, and does not save or advance time. Consent, profile construction and recruitment must still use production conversations. The `reign-resident-native-approach-v1` report records player/target positions before and after, instance/save/enrollment and target identity. Stale or absent enrollment/target must fail before movement. Existing checkpoint/reload cleanup remains authoritative; no additional cleanup route is introduced.

Resident scene cleanup restores native role prototypes before forgetting personal scene bindings. Recruitment also removes the departing role's location object and origin/color bindings before fading that agent. Re-enter the scene after a real recruitment to check a distinct service replacement and absence of the former personal role; compilation or a contract audit alone does not establish native replacement behavior.

The resident suite also exercises `commoner_prompt.txt` through construction, legacy foundation and five production conversation envelopes (individual, Homes, party, social and correspondence). These provider-free checks verify editable/embedded registration, World Tone presence, individual personality, native occupation rather than mechanics-template role, recruitment/release state and noble/notable isolation. Runtime native acceptance needs only one subsequent resident exchange to confirm installed prompting; inspect the attributed request audit rather than asking a character to recite hidden instructions. Existing provider and disposable-save gates apply unchanged.

Native acceptance found that the default close source leaves footwear outside the reference. Resident snapshots now select `ai_source_resident_full_outfit_v1`, the complete native tableau at 768x1024, for initial and repeat portraits. The server requires that contract and records it in input/product provenance. Ordinary portraits retain their existing close preset. `PortraitSourceCharacterTests` covers profile selection, distinct framing hashes, current headwear/modifiers/dyes and preservation of an intentionally empty outfit through final renderer preparation. Use the existing background-source CLI on a retained resident snapshot for one provider-free full-body render; inspect head, outfit and feet before the bounded provider retest. No new command, cost gate or campaign mutation route was added.

The existing `reign_get_live_test_status` heartbeat exposes `runtime.encounteredResidents` (`reign-encountered-resident-runtime-v1`). It is read-only: at most 64 local identities and 64 eligible scene agents, native displayed names, source/skill template IDs, body/outfit SHA-256 values, availability/payment/release flags, pending/native conversation target, and Homes card/selected/session participant IDs. Use before/after observations to prove first promotion, exact appearance, scene return, selected-only hearing, Visit targeting, departure and save/reload. Snapshot retrieval grants no provider or campaign mutation authority and does not replace the exact enrolled disposable-save gate.

Homes preview variants inherit the approved `party-chat` reference and unchanged fixed-region mask through `asset-manifest.json.referenceVariants`; fidelity rejects a variant rendered from another prefab. Run the approved-reference audit with targets `party-chat,settlement-homes,settlement-homes-empty,settlement-home-chat`, the full-catalog fidelity contract and the complete rendered-preview report. `test-party-chat-card-contract.py` additionally checks the moving Visit control, original frame pixels and true-alpha aperture.

Run `contracts.encountered_residents` through the artifact-bound Verification Lab `quick`/`offline`, suite `encountered_residents` or `contracts`. It executes production name generation/similarity, home/lifecycle filtering, occupation routing, native public-authority selection and current-equipment portrait clothing policy without providers or campaign mutation. `PortraitSourceCharacterTests` additionally proves current headwear, equipment modifiers and dye retention on initial and repeat generation. The saved native registry uses bounded `ReignSavePayloadCodec` chunks under `_reign_encountered_residents_v1`; native/profile metadata is `reign-encountered-resident-v1` and resident clothing policy is `resident_current_native_equipment`.

Native/provider acceptance remains distinct: use an explicitly enrolled disposable copy of the user's baseline. The user requested only a short set of examples covering first-contact profile/identity, exact source appearance/outfit, remembered names and role service, scoped private Homes chat, Visit in person teleport/native dialogue, invitations/recruitment/release and one save/reload cycle. Do not broaden this into a campaign soak. Preserve the baseline and follow the existing pause, quiescence, capacity, lifecycle and provider gates. Implementation/evidence checkpoint: `docs/agent/ENCOUNTERED_RESIDENTS_IMPLEMENTATION.md`.

## Starting children in new campaigns (2026-09-05)

Save overflow regression (2026-09-06): `ReignSavePayloadCodecTests` exercises 120 households through the native signed-short entry layout and checks lossless chunk recovery. The adapter reads legacy `_reign_startingChildren_v1`, writes it empty, and persists bounded `_reign_startingChildren_chunks_v2` chunks. Native new-campaign save/reload is still separate acceptance. The same test class checks campaign-identity guards before character-editor polling and delayed command application; these source contracts do not substitute for native unload/reload observation. World diplomacy selftests cover rich-realm/poor-ruler rejection, exact balance, raised counteroffers, missing balance, free agreements, and reverse payer direction. Package negotiation receives `leaderGold`, while aggregate `treasury` remains strategic data.


`contracts.starting_children` in Verification Lab `quick`/`offline`, focused suite `starting_children` (also included in `contracts`), executes the production family planner and receipt executor without game or provider calls. It covers native/War Sails/added-family fixtures, ruler-couple and player exclusions, existing children, maternal birth chronology (including deceased children and half siblings), age bands, deterministic ordering, 120-household scale, partial-failure retries, saved receipt round trips and old-save no-ops. Its native source contract checks initialization order and the testing catalog; it is not native behavioral acceptance.

Observe `characterInitialization.startingChildren` through the existing `reign_get_live_test_status`. `reign-starting-children-runtime-v1` reports completion, counts, shortfalls, errors and the evidence path. New-campaign initialization emits `reign-starting-children-evidence-v1` under `%LOCALAPPDATA%/Bannerlord Reign/tests/starting-children/`, containing the before/after census, reproducible family plan and native hero receipts. Initialization state and receipts are saved; loading any existing save never starts or resumes seeding. There is no game-mutating seeding command or provider requirement.

After manifest-selected `changed` validation, run the artifact-bound Lab checks through `reign_run_offline_verification`. Native acceptance remains separate: use an explicitly authorized disposable **new** campaign when the visible game is free, inspect actual parent links, clan/culture/home and native child state, advance a 17-year-old through normal coming of age, save/reload and compare identities, and verify an older save gains no children. Check pregnancies and birth-event history are unchanged by seeding. Never restart, replace installed files, or repurpose a loaded campaign owned by another task to perform this acceptance.

Court-life provider-free interface verification uses the shared `ReignBeta/tools/GauntletXmlPreviewer` fallback because the MCP catalog has no preview command. Run `audit-preview-contract.mjs`, `test-court-life-preview-contract.mjs`, the existing portrait/layout contracts, the complete `audit-rendered-preview.mjs` matrix, then approved-reference fidelity. The Court Petition catalog now audits both production ViewModels and includes `court-life-international`, `court-life-family`, `court-life-patronage`, and `court-life-visitors` as browser-only states (`nativeInjection=false`). These checks consume no provider or campaign action.

Historical patronage fixture selection first awaits `GET /court/patronage/public-history` (`reign-patronage-public-history-v1`). The production cache uses the exact campaign, timeline, kingdom, state instance and current day. Verified victory, release, death, settlement transfer and public peace subjects come only from completed native events whose global or own-kingdom news has arrived. Missing prerequisites remain explicit ineligibility; private family history or a ruler's unverified proclamation cannot manufacture a qualifying victory. Later acceptance should arrange real native events on the armed disposable branch and wait for their actual public-news delay before preparing these templates.

## Background character portraits (2026-09-05)

Shared-library exception: existing `portrait.png` masters now drive shared rebuilds with saved physique, preserving face and outfit and bypassing clothing redesign. No shared source PNG is required or persisted. The `physique_shared_*` contracts in the existing portrait contracts cover source-free catalog readiness, AI-reference binding, provider parity, conflicting stats, read-only files and failed-write rollback. First-time shared portraits and campaign requests still use the native generator. This supersedes the shared-native-source reuse requirement below; it does not change scenery or authorize paid tests.

Native physique guidance: every character portrait stage includes the editable `portrait_body_appearance.txt` layer after source resolution. `reign-native-physique-v1` binds effective renderer weight/build (0–1, never percentages) and per-axis provenance to the source PNG hash. Receipts and stage evidence include `reign-portrait-physique-v1`, expanded layer text and its hash. Shared sources without matching physique metadata require fresh native rendering; existing AI portraits are not regenerated. Native stats override AI body drift even in clothing edits; scenery is excluded. `PortraitSourceCharacterTests` and the `physique_*` checks under the existing adult portrait contracts prove units, boundaries, cache binding, prompt assembly and receipt evidence through manifest-selected validation. Human/provider acceptance of lean-muscular and heavyset-low-muscle pairs remains separately cost-gated; prompt checks are not body-measurement validation.

All character portrait generation actions use `ReignPortraitBridge.RequestPortrait` / `TryRequestPortrait` and the bundled native source resolver. `contracts.background_portrait_entrypoints` audits native dialogue, individual/party chats, social-event eyes, petitions, player creation and debug actions. `background_source_routing` in portrait derivative contracts proves no captured-image prerequisite, renderer-failure propagation and scenery isolation. `PortraitSourceCharacterTests` proves fresh characters, campaign identity, facial metadata and current-clothing/fallback behavior. Run these through manifest-selected Release validation; no provider is needed.

The generator accepts `--character-snapshot <absolute-json-path>` with schema `reign-native-portrait-snapshot-v1`, matching campaign and hero IDs, native body key, age, gender, culture, weight/build and optional civilian equipment. It writes the same source/result files as the existing background command. The client snapshots native data on the game thread and binds cache writes and recovery to the original campaign. Missing equipment receives stock renderer clothing; actual AI clothing remains prompt controlled. Explicit facial data takes precedence over a generated court appearance. The legacy source-mode setting cannot enable a screen-capture fallback.

For provider-free rendering proof, run the exact validated bundled generator's background command with a retained snapshot and output/result paths in the task evidence directory. This CLI starts no campaign and consumes no provider quota. Run the Lab with `tier=quick, suite=contracts`; `portraits_images` is a check classification, not a runnable suite selector. A real provider generation still requires the existing cost gate. Native acceptance after this deployment is assigned to the user: native Look, social and party eyes, individual chat, player creation, debug conversation request and Control Center. Verify no screen change, retained input, a single request on repeated clicks and live portrait refresh. The user explicitly deferred the petition manual retest; its automated route coverage remains required.

## Portrait clothing edit profiles (2026-09-04)

The Control Center has four independent image profiles: normal portraits (`portrait`), adult portraits (`adultPortrait`), normal scenery/events (`scenery`), and adult scenery/events (`adultScenery`). Both adult profiles initialize to AtlasCloud `bytedance/seedream-v5.0-pro/edit`; existing normal selections remain intact. Adult scenery is settings-only in this version. NanoGPT and AtlasCloud share one model catalog across all profiles. Reign Image Generator integration and its start/status routes are retired; legacy selections migrate to AtlasCloud Seedream v5, without deleting the separate generator installation.

Known adult female portrait subjects (ageYears >= 18) with physical confidence >= 61 use two sequential image calls. The normal call omits the confidence prompt; its validated full-body output becomes the adult profile's image reference. The second prompt uses the existing 61–80 or 81–100 clothing band and explicitly preserves face, identity, hair, anatomy, pose, lighting and composition. Only validated edited results are published. An edit refusal, timeout, prompt failure or invalid output uses the validated normal image with a warning. Existing cached portraits are not regenerated automatically.

`contracts.image_generation_profiles` includes the provider-free adult portrait contracts in Verification Lab quick/offline. These cover thresholds, age/gender, prompt separation, shared catalog parity, serialization independence, retired-provider migration, reference handoff and failure fallback. Run through `reign_run_offline_verification` or canonical validation; do not invoke test executables directly. `generationStages` and `warning` are additive response and stored product-receipt evidence; stage source/output hashes describe the chain while the existing native-source/base-prompt receipt hashes stay compatible. Shared batch status exposes warnings separately from failures.

Provider-free success does not prove face fidelity. Bounded live examples require existing provider-cost gates and visual review of the full body and face against the normal image; prompting alone does not enforce identical pixels. Native acceptance, if performed, uses an enrolled disposable campaign. The web profile controls have no registered native raster reference; inspect their responsive layout, focus, provider changes and full option catalogs against the modern palette, and report native raster contracts as inapplicable to these HTML settings controls.

The portrait contracts export the actual rendered HTML to the run's data/tests/verification/portrait-profiles/control-center.html. Run ReignServer/src/Modules/Portraits/portrait-profile-ui-contract.mjs with that HTML path and an evidence directory using Node and Playwright (REIGN_PLAYWRIGHT_MODULE may select a bundled package). The harness intercepts every request and never connects to the installed server; it verifies all four catalogs, provider visibility, independent save/reload, and captures 1672-, 1024-, and 600-pixel layouts for each provider.


## Wanderer population (2026-09-08)

Run the artifact-bound Verification Lab quick/offline suite `wanderer_population` (also `contracts.wanderer_population` in contracts). It exercises production name/shape uniqueness, every template/culture combination, population balancing, safe current-town-menu priority, active-mission/siege mutation gates, rotation eligibility, contact idempotence, death policy, JSON save roundtrip, profile preservation and isolated contact-history scope. No providers or native campaign mutations.

Read `reign_get_live_test_status` -> `runtime.wandererPopulation` for loaded cultures/templates, per-town available counts, generation failures, history-pending protection and at most 32 local identities. `/dialogue/history` has an observational `wandererContactOnly=true` branch requiring exact campaign, active timeline, hero and player. Unknown history preserves protection. Saved identities live in `_reign_wanderer_population_v1`; generator updates never reroll them. Actual Reign chat protects its speaker from rotation and all death causes except old age; mere inspection, automatic openings and bystanders do not.

Deployment and native acceptance are separate: after authorized deployment, use the exact guarded disposable Current to verify that an underfilled current town tops up from its town menu, remains mutation-free inside the active tavern mission, and presents 3-5 physical approachable tavern heroes across cultures after scene entry. Also verify movement with unchanged identity, real chat protection and network recovery, recruitment/dismissal, death-policy gates and a fresh-process save roundtrip. Pause, drain queues, leave missions and use the existing rolling checkpoint/cleanup routes. Preserve the enrolled baseline; never infer disposal authority from a name. See `docs/agent/WANDERER_POPULATION.md` for limitations and evidence.


## Character narratives and interests (2026-09-09)

Dynamic construction uses `single_call_v1`: a fresh complete profile normally requires one provider call, with at most three total attempts and only incomplete sections/numbered concern slots requested on repair. IDs, ratings and the trio are local. The initial shipped roster is authored in the implementation task; the provider authoring route is for isolated future-generation verification, not rebuilding the initial pack. Preserve earlier complete output as backup. Authoring batch results include `providerCalls`, `elapsedMs`, `reused`; checkpoint attempts include `durationMs`, `requestCharacters`, `responseCharacters`, `requestedSlots`. Measure cold samples separately from zero-call reuse; one-call contracts do not prove an absolute latency guarantee.


`reign_inspect_character_narrative` reads the complete base/effective narrative, defining trio, retained development projection and full-roster diversity counts through `GET /characters/narrative`. It never materializes a character or calls a provider. Supply a hero ID and an optional campaign ID; absent campaign reads the shipped catalog. Missing data is explicit.

`reign_author_character_narratives` uses the installed Character Construction provider/model, with exact confirmation `author character narratives using configured provider` and trusted verification/offline permissions. It runs only a successful Release validation artifact, without a listener, in the ReignValidation database and a build-root-only runtime. Batch size is 1-25, offset selects catalog order, and authoringRunId is a simple confined task name. One writer holds the run lease. Each profile resumes validated life/interests stages; three content-validation failures stop that stage without generic substitutions. A provider transport or availability failure stops immediately and retains the completed stages. Provider settings are read without modification. No campaigns, deployed files or active catalog are changed. Output and evidence: `.codex-build/reign-mcp/narrative/<authoringRunId>/batch-<offset>.json`. A catalog is assembled only after all profiles pass. Cancelled batches retain completed profile checkpoints. Authoring can incur provider usage; offline contracts cannot invoke this path.

Run `character_narrative` through the artifact-bound quick Verification Lab and manifest-selected validation. The contract covers weighted influence, stable semantic selection, complete generation, privacy/relevance rules and gated development. Provider dialogue behavior and native fresh-campaign save/reload acceptance remain separate requirements. Earlier campaigns retain v4; only the native persisted fresh-campaign marker enables v5. Deployment remains a separate stopped-runtime operation using successful validated artifacts.

The `operation` parameter accepts `author` (default) or `evaluate`, with the same exact provider confirmation for both. Authoring uses at most three independent profile workers; stages within each profile remain sequential, batch writers hold an exclusive run lease, and completed checkpoints are reused only when current native facts, personality, library and blueprint fingerprints match. The staged v5 catalog references one validated `narrative/profiles/<hero-hash>.json` per character and assembles only when the entire roster is complete. `evaluate` uses the saved Dialogue model plus an independent Character Construction judge for three ordinary scenarios and three 1/5/10 interest contrasts per selected character. Case replies, judgments and fingerprints are retained under `evaluation/`; structural, privacy and canon checks require 100%, behavior at least 95%. This is provider evidence, not native game acceptance.

The narrative library now contains 664 hobbies across 32 families and 1,904 other concepts. Children under twelve use authored play, attachment and learning concepts; native age governs voice and interpretation. Adult options include status-seeking, traditional, selfish and conflicted motives as well as generous ones. Native rulers receive an additional provider case with a consuming kitten interest during an urgent military supply discussion. Previous failed provider evidence is retained before a retry.

The provider-free `character_narrative` suite emits `data/tests/verification/character-narrative/control-center.html` and `fixture.json`. Run `ReignServer/src/Modules/Characters/narrative-editor-ui-contract.mjs` with that fixture directory and an evidence directory, using bundled Node/Playwright (`REIGN_PLAYWRIGHT_MODULE` may point to the installed package). The harness intercepts every browser request; it does not open the installed Control Center or start a listener. It checks 1672/1024/600 widths, current/expanded/long-text/history states, first/last cards, escaping, editing, add/remove, private history labels, hidden construction metadata, and legacy empty state. Reports and screenshots are written only to the selected evidence directory. This web-editor change adds no native shell or portrait aperture; native campaign acceptance remains a separate deployment follow-up.

Authoring resolves family, clan and kingdom display names before sending native facts to the provider. `ProfileLibrary/narrative/native-context.json` records the source XML hashes and the one omitted native age (Suran); an absent source age must not become a newborn age. Internal identifiers and damaged Unicode are rejected in authored prose. The original identity catalog remains unchanged.

For v5 dynamic characters, first interaction waits for the complete validated life and interests stages using saved Character Construction settings; partial checkpoints are never marked ready. Shipped v5 characters materialize their authored narrative without a provider call. Campaign metadata carries the native saved narrative version to unencountered characters; an explicit native older-save marker remains authoritative. Earlier campaigns continue using the preserved v4 catalog.

The narrative contract includes isolated PostgreSQL persistence and replay, stale-editor revision invalidation, retention of active development beyond the ordinary 250-row projection limit, and preservation of superseded history after a manual correction. Values are rated priorities, not automatically absolute prohibitions. Development requires a completed personally relevant event from the active native timeline, or coherent observations across three encounters spanning thirty days. The editor matrix separately verifies readable multiline Life & Voice fields and all three development history states.

`catalogDiversity.quality` uses `reign-narrative-quality-v1`. It reports `nearDuplicateLifePairCount`, bounded `nearDuplicateLifePairs`, `mostRepeatedConcernSentences`, `profilesWithGameWording` and `fieldDiversity` for speech styles, social masks, tell sets and private backstories. Life similarity uses four-word shingle Jaccard >= 0.85; inspect flagged pairs and repeated wording, because distinct strings alone do not establish varied people. The quick contract exercises near-copy detection, unrelated prose, empty populations and idempotent mechanical copy edits. `proseNormalizationVersion=1` changes only game framing while preserving names, quantities, ratings, meanings and selected IDs. Replaying a complete matching checkpoint applies those copy edits without another provider call. Runtime intensity emphasis and omission of the duplicate unranked legacy motivation list are separately covered; provider cases must still demonstrate both engaged personal interests and restraint during duties.

Once a complete v5 catalog is present, the same quick suite compares every roster entry's identity, `sourceFacts`, traits, MBTI and source against the preserved v4 catalog and emits `data/tests/verification/character-narrative/roster-audit.json`. This is a deterministic data check, not a provider dialogue test for each person. The check is conditional on the v5 catalog actually being assembled; pre-assembly contract success cannot establish full-roster preservation.

Government hearing text entry (2026-09-09): use reign_start_government_test profile hearing_compose with the exact already-open businessId and a playerMessage of 1-1200 characters. Both MCP and native client require the armed run-owned Current. The native screen must already show that interactive hearing; the operation never opens, creates, selects, submits, votes, or calls a provider. Observe the unsent text, then use native Speak for production submission. Retain native screenshot and transcript evidence; composition alone is not dialogue acceptance. This route supports environments where desktop Unicode typing fails although physical key input works.


Character narrative dream compatibility is local, personality-aware selection before the single construction request. The reviewed dream-compatibility.json covers every adult dream; low compassion cannot receive a genuinely philanthropic dream and low Court honor cannot receive an integrity-based dream. The character_narrative offline suite checks exclusions, supporting-trait weighting, trait-band boundaries, policy coverage, and zero incompatibleDreamCount in the shipped roster audit. This adds no provider-consuming operation and does not change deployment or campaign gates.


Adult desires and values also use reviewed personality eligibility from ReignServer/profiles/narrative/concern-compatibility.json. Required traits must exceed40; requireNotHigh excludes an opposing trait in the61-100 high band. Neutral craft, comfort and autonomy remain available. The existing offline character_narrative suite covers care/honor distinctions, upper and lower boundaries, mixed-personality populations and complete catalog rule coverage. The roster audit adds incompatibleConcernCount and bounded incompatibleConcerns item references alongside the existing dream-only evidence. The initial roster must report zero incompatible concerns. This is local selection and read-only audit; no new provider, runtime, save or deployment authority is introduced.


Narrative factual prerequisites are declared in ReignServer/profiles/narrative/fact-compatibility.json. Kin-dependent concepts require known siblingIds, olderSiblingIds or livingParentIds; an absent field is unknown and selects another concept, never an invented relative. Premade enrichment uses matching native parent IDs, ages and living status without changing the source facts. Dynamic inputs with no structured kin evidence retain the many non-kin concepts. Explicit minimum ages prevent adolescent past apprentice-supervision claims. Existing character_narrative contracts cover unknown and younger-only siblings, known older kin, living-parent and mentorship-age boundaries, and immutable enrichment. The complete roster audit adds incompatibleFactCount and bounded incompatibleFacts; require zero. No provider calls, database writes, native changes or new authority are introduced.

The narrative fact policy also accepts an optional maximum age. Routine requests to be picked up when tired are limited to age six and younger; older children retain the broader child concern pool. This is an offline eligibility rule with no additional provider or campaign authority.

Court Life snapshot diagnostics (2026-09-09): `court_life_snapshot` retains the exact visible fixture matter and armed disposable-save gates, but can capture a resolved or busy audience. A read-only picture requires neither `AutomationCanDecide` nor completed provider replies and never proves a completed turn, acceptance, or decision authority. `court_life_converse` and `court_life_choose_confirm` retain their full readiness gates. Use `court_life_observe` with `resolved` for resolution receipts and the guarded save/verify pair for persistence. Native acceptance captures the resolved audience before it closes on the next distinct untested encounter, retaining the original ferry failure. Resolved matters cannot be reopened through production eligibility; never replay a judgment or relax eligibility to obtain a picture. Family rule coverage includes patience 0, 1, 10, 11, 40, 41, 50 and 100, zero-before-first-dismissal, and the production missing-trait default of 50; native family transitions remain separate evidence.
